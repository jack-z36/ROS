---
tags:
  - term-explainer
  - pi05
  - data-definition
source: [[部署推理数据流框架|部署推理数据流框架]]
---

# 模式

> [!abstract] 核心定义
> 部署运行时的行为级别配置，决定 VLA 输出是只打日志还是真正发布命令。

## 数据结构

| 字段名 | 类型 | 含义 | 是否必填 |
|--------|------|------|----------|
| `dry-run` | str | 只打日志，不发布控制命令 | 否 |
| `shadow-run` | str | 影子运行，可发布候选命令 | 否 |
| `safe-run` | str | 安全过滤后发布命令 | 否 |

> [!info] 结构说明
> 模式是 runtime config 中影响发布行为的状态字段。

## 具体数值示例

> [!example]- 点击展开具体数据实例
> ```json
>
> {
>   "runtime.mode": "safe-run",
>   "publishes_command_topics": true
> }
> ```
>
> 该示例展示该术语在 [[部署推理数据流框架|部署推理数据流框架]] 中承担的数据契约角色。

## 具象隐喻

> [!tip] 生活场景类比
> 像汽车档位：空档可以启动发动机，行驶档才把动力传到车轮。
