# 执行记录

## 2026-05-06

- 操作：补充场景二输出程序与文件清单。
- 新增文档：`/home/hit/ROS/DOCS/阶段一：数据采集/场景二/输出程序与文件.md`。
- 内容：整理一键启动相机节点涉及的 `start_all_sensor.sh`、总 launch、总配置、状态检查脚本、残留清理脚本和被 include 的相机 launch。
- 结果：已在 `当前进度.md` 中加入该清单索引。
- 说明：本次只修改 DOCS 文档，没有修改 ROS 节点、launch 或配置代码。

## 2026-04-24 15:00:38 CST

- 操作：根据用户提供的“场景二目标描述文档：双 Baton Mini 接入 FASTUMI 数据采集链路”整理并写入场景二目标描述。
- 核对：已阅读公共规则、场景二固定文档、Octopus 架构文档、`baton_mini_sdk_demo` 架构与运行指南。
- 发现：场景二公共定义是“一键启动所有相机 ROS 节点”，本次目标描述聚焦其中的 Baton Mini 双节点阶段。
- 发现：`baton_mini.launch.py` 当前只声明并传入 `server_ip` 与 `local_ip`；`ros_interface.cpp` 支持读取 topic 参数，但 launch 文件尚未暴露。
- 校准：用户样本中的 `fast_odometry` 已按当前源码事实统一为 `fast_odom`。
- 结果：已完成 `/home/hit/ROS/DOCS/阶段一：数据采集/场景二/目标描述.md`。

## 2026-04-24 15:02:48 CST

- 操作：根据用户提供的“场景二背景文档：双 Baton Mini 上位机发布 Topic 方案”整理并写入场景二背景信息。
- 核对：已阅读公共规则、场景二固定文档、Octopus 架构文档、`baton_mini_sdk_demo` 架构与运行指南。
- 处理：将 FASTUMI 论文相关内容写成项目背景理解，未写成已由本仓库代码验证的事实。
- 结果：已完成 `/home/hit/ROS/DOCS/阶段一：数据采集/场景二/背景信息.md`，并补充路线 A / 路线 B、Octopus 录制链路、上游约束、当前代码事实和历史决策。

## 2026-04-24 16:02:31 CST

- 操作：实现 场景二 一键启动入口。
- 新增：`/home/hit/ROS/start_all_sensor.sh`，作为用户入口，负责 source ROS2 Jazzy 与工作区环境，并调用总 launch。
- 新增：`/home/hit/ROS/launch/all_sensor_nodes.launch.py`，读取集中配置后同时 include 两个 Baton Mini 节点和两个 GoPro 节点。
- 新增：`/home/hit/ROS/config/all_sensor_nodes.yaml`，集中维护 Baton Mini IP、本机 IP、node name、topic，以及 GoPro video device、namespace、camera name、frame id。
- 补充：`/home/hit/ROS/DOCS/阶段一：数据采集/场景二/执行约束.md`，明确 `.sh`、总 launch、配置文件三层职责与默认配置。
- 验证：执行 `python3 -m py_compile launch/all_sensor_nodes.launch.py src/baton_mini_sdk_demo/launch/baton_mini.launch.py src/gopro_camera_launch/launch/gopro_pose_record.launch.py` 通过。
- 验证：执行 `source /opt/ros/jazzy/setup.bash && colcon build --packages-select baton_mini gopro_camera_launch` 通过。
- 验证：执行 `ros2 launch launch/all_sensor_nodes.launch.py --show-args` 通过，能识别 `config_file` 参数。
- 验证：执行 `ros2 launch baton_mini baton_mini.launch.py --show-args` 通过，确认 Baton Mini launch 暴露 `server_ip`、`local_ip`、`node_name` 和 topic 参数。
- 验证：执行 `ros2 launch gopro_camera_launch gopro_pose_record.launch.py --show-args` 通过，确认 GoPro launch 暴露 `video_device`、`camera_namespace`、`node_name`、`camera_name`、`frame_id` 参数。
- 未验证：尚未连接真实硬件执行 `./start_all_sensor.sh` 长时间运行；Baton Mini 网络连通、GoPro `/dev/video*` 实际编号、Octopus topic 可见性仍需现场验证。

## 2026-04-24 16:07:50 CST

- 操作：按用户要求先停止节点并确认 topic 状态。
- 检查：`ros2 node list` 初始可见 `/octopus`、`/picotele_*`、`/realsense/*`、`/xr/pico` 等节点，`ros2 topic list` 初始可见 Baton Mini、inspire、realsense、XR 等 topic。
- 判断：本机进程表未发现这些 ROS 节点对应的本地进程；Baton Mini topic 的 publisher 显示为 `_NODE_NAME_UNKNOWN_`，属于设备侧或远端 DDS endpoint。
- 操作：执行 `ros2 daemon stop` 刷新 ROS2 daemon。
- 操作：通过 `nmcli device disconnect enxcec0e18c1eb0` 与 `nmcli device disconnect enxce4a3ca21672` 断开两块 Baton Mini USB 网卡。
- 结果：Baton Mini 相关 topic 已从普通 `ros2 topic list` 中消失。
- 现状：普通 ROS_DOMAIN 下仍能发现 Wi-Fi 侧远端 `/octopus`、`/picotele_*`、`/realsense/*`、`/xr/pico` 等节点和 topic；本机无对应可杀进程，未断开主 Wi-Fi。
- 补充验证：使用 `ROS_LOCALHOST_ONLY=1 ros2 node list --no-daemon --spin-time 1` 时，本机节点列表为空；使用 `ROS_LOCALHOST_ONLY=1 ros2 topic list --no-daemon --spin-time 1` 时仅剩 CLI/ROS 基础暂态 topic `/parameter_events` 与 `/rosout`。

## 2026-04-24 16:10:37 CST

- 现象：用户执行 `./start_all_sensor.sh` 时报错：`/opt/ros/jazzy/setup.bash: 行 8: AMENT_TRACE_SETUP_FILES: 未绑定的变量`。
- 原因：`start_all_sensor.sh` 使用 `set -u`，而 ROS / ament 的 `setup.bash` 会读取可选环境变量 `AMENT_TRACE_SETUP_FILES`；该变量未设置时会触发 nounset 错误。
- 修复：在 `/home/hit/ROS/start_all_sensor.sh` 中新增 `source_setup_file()`，source ROS 与工作区 setup 文件时临时执行 `set +u`，source 完成后恢复 `set -u`。
- 修复：根据上一轮实际网卡观测，将 `/home/hit/ROS/config/all_sensor_nodes.yaml` 中 Baton Mini right / left 的 `local_ip` 从旧值校准为 `192.168.1.11` 与 `192.168.2.11`。
- 验证：`bash -n start_all_sensor.sh` 通过。
- 验证：在 `set -euo pipefail` strict 模式下手动 source `/opt/ros/jazzy/setup.bash` 与 `/home/hit/ROS/install/setup.bash` 通过，输出 `setup-source-ok`。
- 验证：`config/all_sensor_nodes.yaml` 可被 Python YAML 正常解析，当前 Baton Mini `local_ip` 为 `192.168.1.11` 与 `192.168.2.11`。
- 注意：两块 Baton Mini USB 网卡当前仍为 disconnected：`enxce4a3ca21672`、`enxcec0e18c1eb0`；启动前需要恢复连接。

## 2026-04-24 16:23:54 CST

- 现象：用户反馈普通 `ros2 topic list` 仍能看到 `/inspire/*`、`/picotele/*`、`/realsense/*`、`/xr/pico/*` 等 topic，并要求删除订阅这些 topic 的节点。
- 排查：`ros2 node list --no-daemon` 可发现 `/octopus`、`/picotele_*`、`/realsense/*`、`/xr/pico` 等节点，但本机 `ps` 未发现对应本地进程，仅发现 `ros2-daemon`。
- 判断：这些 topic / node 来自同一 ROS_DOMAIN 下 Wi-Fi 侧远端系统或 DDS 发现结果，不是 `/home/hit/ROS/start_all_sensor.sh` 启动出的本机相机节点；本机无法直接 kill 远端节点。
- 处理：`start_all_sensor.sh` 默认设置 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST` 并停止 ROS daemon，脚本自身的检查不再受 Wi-Fi 侧远端 ROS 图污染。
- 验证：`ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST ros2 topic list --no-daemon --spin-time 1` 在未启动相机时仅剩 `/parameter_events` 与 `/rosout`。
- 现象：第一次短时运行一键脚本时，GoPro left 使用 `/dev/video6`，`v4l2_camera` 报 `Failed mapping device memory`，未发布 `/gopro_left/image_raw` 与 `/gopro_left/camera_info`。
- 排查：`v4l2-ctl --device=/dev/video5 --all` 显示 `/dev/video5` 是第二个 UGREEN 采集卡的 `Video Capture` 节点；`/dev/video6` 是 `Metadata Capture` 节点。
- 修复：将 `/home/hit/ROS/config/all_sensor_nodes.yaml` 中 GoPro left 的 `video_device` 从 `/dev/video6` 改为 `/dev/video5`。
- 增强：新增 `/home/hit/ROS/scripts/all_sensor_status.py`，由 `start_all_sensor.sh` 调用，启动前检查 Baton Mini IP、GoPro 视频设备和 Video Capture 类型，启动后检查 4 个期望节点及其 topic。
- 验证：执行 `bash -n start_all_sensor.sh` 通过。
- 验证：执行 `python3 -m py_compile scripts/all_sensor_status.py launch/all_sensor_nodes.launch.py` 通过。
- 验证：执行 `python3 scripts/all_sensor_status.py preflight --config config/all_sensor_nodes.yaml` 通过，确认 Baton Mini right / left IP 可达，GoPro right `/dev/video4` 与 GoPro left `/dev/video5` 均为 Video Capture 设备。
- 验证：执行 `STARTUP_WAIT=15 timeout --signal=INT --kill-after=5s 35s ./start_all_sensor.sh`，终端输出 `4/4 个节点全部通过检查`：
  - `Baton Mini right`: `/baton_mini_right`，topics `5/5`
  - `Baton Mini left`: `/baton_mini_left`，topics `5/5`
  - `GoPro right`: `/gopro_right/gopro_right_camera`，topics `2/2`
  - `GoPro left`: `/gopro_left/gopro_left_camera`，topics `2/2`

## 2026-04-24 16:39:24 CST

- 现象：用户使用 `ROS_LOCALHOST_ONLY=1 ros2 topic list` 仍看到 `/baton_mini_left/stereo3/*`、`/baton_mini_left/stereo4/*`、`/inspire/*`、`/realsense/*` 等 topic，质疑脚本节点运行位置。
- 校正：`ROS_LOCALHOST_ONLY=1 ros2 topic list` 默认仍可能复用已存在的 `ros2-daemon`；如果 daemon 之前在普通发现范围下启动，会继续显示普通 ROS 图中的 topic。
- 验证：执行 `ros2 daemon stop` 后，再执行 `ROS_LOCALHOST_ONLY=1 ros2 topic list --no-daemon --spin-time 1`，只剩 `/parameter_events` 与 `/rosout`。
- 验证：执行 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST ros2 topic list --no-daemon --spin-time 1`，只剩 `/parameter_events` 与 `/rosout`。
- 排查：当前本机 `pgrep -af 'all_sensor_nodes|baton_mini|v4l2_camera_node|gopro'` 未发现一键脚本残留的本地相机节点进程。
- 结论：`start_all_sensor.sh` 启动的两个 Baton Mini SDK 节点运行在本机 Ubuntu 上，不运行在相机内部；它们通过 `server_ip` / `local_ip` 与相机硬件通信。
- 区分：脚本内本机 SDK 节点的期望 Baton topic 是 `/baton_mini_left/imu`、`/baton_mini_left/odometry`、`/baton_mini_left/fast_odom`、`/baton_mini_left/image_left`、`/baton_mini_left/image_right` 等；用户列表中的 `/baton_mini_left/stereo3/*`、`/stereo4/*`、`camera_left_info`、`stereo3_ctrl` 更像相机内部或远端 ROS2 栈的话题，不是本机 SDK Demo launch 显式配置出来的 topic。
- 修正：更新 `start_all_sensor.sh` 的提示，要求另一个终端验证前先执行 `ros2 daemon stop`，再执行 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST ros2 topic list --no-daemon`。

## 2026-04-24 16:47:07 CST

- 现象：用户再次执行本机范围 topic 查询，列表中只剩当前场景二目标相机 topic 与 ROS 基础 topic。
- 观察到的 Baton Mini topic：
  - `/baton_mini_left/fast_odom`
  - `/baton_mini_left/image_left`
  - `/baton_mini_left/image_right`
  - `/baton_mini_left/imu`
  - `/baton_mini_left/odometry`
- 观察到的 GoPro topic：
  - `/gopro_left/camera_info`
  - `/gopro_left/image_raw`
  - `/gopro_right/camera_info`
  - `/gopro_right/image_raw`
- 观察：GoPro 图像还伴随 `/compressed`、`/compressedDepth`、`/theora`、`/zstd` 等图像传输相关派生 topic。
- 判断：远端 `/inspire/*`、`/picotele/*`、`/realsense/*`、`/xr/pico/*` 等污染 topic 已不在该列表中；场景二一键启动后的本机相机 topic 集合已基本符合预期。

## 2026-04-24 16:50:37 CST

- 现象：用户指出当前 topic 列表中没有 `/baton_mini_right/...`。
- 排查：本机进程中只剩 `/baton_mini_left`、`/gopro_right/gopro_right_camera`、`/gopro_left/gopro_left_camera`；没有 `baton_mini_right` 进程。
- 排查：最新 launch 日志 `/home/hit/ROS/log/start_all_sensor/20260424_164255.log` 中显示右侧 Baton Mini 进程启动后连接失败：
  - `Failed to connect to server at 192.168.1.10 :9996`
  - `Failed to connect to server at 192.168.1.10 :9994`
  - `login fail`
  - `process has finished cleanly`
- 验证：`192.168.1.10` 可以 ping 通，但 TCP 端口 `8000`、`9994`、`9996`、`9997`、`9998` 均为 closed；左侧 `192.168.2.10` 的这些端口均为 open。
- 判断：右侧 Baton Mini 的 USB 网络层可达，但设备侧 SDK / ROS 数据服务端口未开放或服务未运行，因此本机 SDK 节点无法登录并退出。
- 修复：增强 `/home/hit/ROS/scripts/all_sensor_status.py` 的启动前检查，Baton Mini 除 ping 外还检查 TCP 端口 `8000`、`9994`、`9996`、`9997`、`9998`。
- 修复：增强 `/home/hit/ROS/start_all_sensor.sh`，启动后检查失败时默认停止已启动节点并返回非零，避免留下半启动状态；如确需保留半启动调试，可设置 `STOP_ON_FAILURE=0`。
- 验证：`python3 scripts/all_sensor_status.py preflight --config config/all_sensor_nodes.yaml` 当前会明确报错：`FAIL Baton Mini right: 设备 TCP 端口未打开 server_ip=192.168.1.10, ports=8000, 9994, 9996, 9997, 9998`。

## 2026-04-24 16:55:52 CST

- 现象：用户停止半启动状态后再次执行 `./start_all_sensor.sh`，预检报两台 Baton Mini 的 `local_ip` 未绑定，且设备 TCP 端口未打开。
- 排查：重新插拔 / 重连后，NetworkManager 给两块 USB 网卡分配的新地址变为 `192.168.1.12` 与 `192.168.2.12`，不再是旧配置中的 `192.168.1.11` 与 `192.168.2.11`。
- 修复：将 `/home/hit/ROS/config/all_sensor_nodes.yaml` 中两台 Baton Mini 的 `local_ip` 改为 `auto`。
- 修复：增强 `/home/hit/ROS/launch/all_sensor_nodes.launch.py`，当 `local_ip: auto` 时通过 `ip route get <server_ip>` 自动解析本机源地址并传给 Baton Mini SDK 节点。
- 修复：增强 `/home/hit/ROS/scripts/all_sensor_status.py`，当 `local_ip: auto` 时使用相同方式自动解析并检查当前本机 IP。
- 验证：语法检查通过：`bash -n start_all_sensor.sh` 与 `python3 -m py_compile scripts/all_sensor_status.py launch/all_sensor_nodes.launch.py`。
- 验证：在端口开放时，预检可识别 `local_ip=192.168.1.12 (auto)` 与 `local_ip=192.168.2.12 (auto)`。
- 后续观察：一次短时启动后，两台 Baton Mini 设备仍可 ping 通，但 TCP 端口 `8000`、`9994`、`9996`、`9997`、`9998` 均再次变为 closed；当前判断为设备侧 SDK 服务/数据端口未开放，需要重启或重新插拔 Baton Mini 设备后再启动。
- 修复：将 `start_all_sensor.sh` 默认 `STARTUP_WAIT` 从 `8` 秒调整为 `20` 秒，并将 ROS 图查询等待默认值调整为 `5` 秒，减少启动后过早判定 topic 缺失。
- 修复：为 `start_all_sensor.sh` 的 cleanup 增加防重入保护，避免连续 Ctrl+C 时重复打印多次 `停止 all sensor launch...`。

## 2026-04-24 17:01:49 CST

- 现象：用户再次执行 `./start_all_sensor.sh`，两台 Baton Mini 仍报 TCP 端口未打开；GoPro right / left 均通过预检。
- 复核：本机无残留 `start_all_sensor`、`all_sensor_nodes`、`baton_mini`、`v4l2_camera_node` 进程。
- 复核：两块 Baton Mini USB 网卡已绑定并路由正确：
  - `192.168.1.10` 使用本机源地址 `192.168.1.12`
  - `192.168.2.10` 使用本机源地址 `192.168.2.12`
- 复核：两台 Baton Mini 均可 ping 通，但 TCP 端口 `8000`、`9994`、`9996`、`9997`、`9998` 均为 closed。
- 结论：当前不是本机 ROS 节点或 launch 残留问题，也不是 GoPro 问题；阻塞点是两台 Baton Mini 设备侧 SDK 服务未开放。
- 修复：增强 `/home/hit/ROS/scripts/all_sensor_status.py` 的错误提示；当 ping 通但 TCP 端口关闭时，明确提示“网络可达，但 Baton Mini 设备侧 SDK 服务未开放”，并建议重启或重新插拔 Baton Mini 后等待 10-20 秒再运行脚本。

## 2026-04-24 17:15:24 CST

- 现象：用户重启/恢复 Baton Mini 后再次执行 `./start_all_sensor.sh`。
- 预检：两台 Baton Mini 均通过：
  - right 自动解析 `local_ip=192.168.1.13`，`192.168.1.10` 可 ping，TCP 端口 `8000`、`9994`、`9996`、`9997`、`9998` 均 open。
  - left 自动解析 `local_ip=192.168.2.11`，`192.168.2.10` 可 ping，TCP 端口 `8000`、`9994`、`9996`、`9997`、`9998` 均 open。
- 预检：GoPro right `/dev/video4` 与 GoPro left `/dev/video5` 均存在且为 Video Capture。
- 运行：4 个本机节点均启动：
  - `/baton_mini_right`
  - `/baton_mini_left`
  - `/gopro_right/gopro_right_camera`
  - `/gopro_left/gopro_left_camera`
- 验证：本机 ROS 图中已出现两组 Baton Mini topic 与两组 GoPro topic，包括：
  - `/baton_mini_right/imu`、`/baton_mini_right/odometry`、`/baton_mini_right/fast_odom`、`/baton_mini_right/image_left`、`/baton_mini_right/image_right`
  - `/baton_mini_left/imu`、`/baton_mini_left/odometry`、`/baton_mini_left/fast_odom`、`/baton_mini_left/image_left`、`/baton_mini_left/image_right`
  - `/gopro_right/image_raw`、`/gopro_right/camera_info`
  - `/gopro_left/image_raw`、`/gopro_left/camera_info`
- 验证：执行 `python3 scripts/all_sensor_status.py postlaunch --config config/all_sensor_nodes.yaml` 输出 `4/4 个节点全部通过检查`。
- 备注：GoPro 标定文件缺失警告仍存在，但不影响 image_raw / camera_info topic 发布；Baton Mini 日志中偶发 `IMU data receive error` / `Data format error`，后续长时间稳定性验证时继续观察。

## 2026-04-24 17:30:12 CST

- 现象：用户指出当前发布 topic 太多，只需要 Baton Mini 位姿数据与 GoPro 视频流数据，并希望之后能方便调整每个 topic 的名称和是否发布。
- 修改：将 `/home/hit/ROS/config/all_sensor_nodes.yaml` 的 topic 配置改为 `enabled` + `name` 结构。
- 默认策略：Baton Mini right / left 只发布 `/baton_mini_right/odometry` 与 `/baton_mini_left/odometry`；`imu`、`fast_odom`、`image_left`、`image_right` 默认关闭。
- 默认策略：GoPro right / left 只发布 `/gopro_right/image_raw` 与 `/gopro_left/image_raw`；`camera_info` 默认关闭。
- 修改：增强 `/home/hit/ROS/launch/all_sensor_nodes.launch.py`，从集中 YAML 读取 topic 名称和发布开关，并传给各子 launch。
- 修改：增强 `/home/hit/ROS/src/baton_mini_sdk_demo/launch/baton_mini.launch.py`，新增 `publish_imu`、`publish_odometry`、`publish_fast_odom`、`publish_image_left`、`publish_image_right` 参数。
- 修改：增强 Baton Mini ROS 接口，未启用的 publisher 不再创建；同时在 `baton_mini.cpp` 中按开关关闭设备侧 IMU、fast odom、image 接收，避免无用数据流继续进入本机。
- 修改：增强 `/home/hit/ROS/src/gopro_camera_launch/launch/gopro_pose_record.launch.py`，新增 `image_raw_topic` 与 `camera_info_topic` 重映射参数。
- 修改：GoPro 不再把 `v4l2_camera_node` 直接暴露在公开 namespace；现在先在隐藏 namespace `/_all_sensor_internal/gopro_<side>` 中采集，再由 `image_topic_relay` 只转发配置启用的公开 topic。
- 新增：`/home/hit/ROS/src/gopro_camera_launch/gopro_camera_launch/image_topic_relay.py`，用于把隐藏 GoPro 原始图像 topic 转发到公开 `/gopro_<side>/image_raw`。
- 修改：增强 `/home/hit/ROS/scripts/all_sensor_status.py`，启动后只检查配置中已启用的 topic。
- 修复：将 GoPro 设备路径从容易漂移的 `/dev/video*` 改为 `/dev/v4l/by-path/...video-index0`，当前 right 指向 USB 口 `3.2`，left 指向 USB 口 `3.4.4.2`。
- 验证：`python3 -m py_compile launch/all_sensor_nodes.launch.py scripts/all_sensor_status.py src/baton_mini_sdk_demo/launch/baton_mini.launch.py src/gopro_camera_launch/launch/gopro_pose_record.launch.py` 通过。
- 验证：`bash -n start_all_sensor.sh` 通过。
- 验证：`source /opt/ros/jazzy/setup.bash && colcon build --packages-select baton_mini gopro_camera_launch` 通过。
- 验证：三个 launch 的 `--show-args` 通过，确认新增 Baton publish 开关与 GoPro topic 重映射参数已进入安装空间。
- 验证：状态脚本按新配置计算出的期望 topic 为 4 个：`/baton_mini_right/odometry`、`/baton_mini_left/odometry`、`/gopro_right/image_raw`、`/gopro_left/image_raw`。
- 验证：按 by-path 设备路径重新执行 `python3 scripts/all_sensor_status.py preflight --config config/all_sensor_nodes.yaml` 通过；当前仅提示上一轮旧 GoPro right 进程仍占用 `/dev/video4`。
- 操作：停止上一轮旧 `start_all_sensor.sh` / `all_sensor_nodes.launch.py` / GoPro 节点后，确认 localhost 范围 topic 只剩 `/parameter_events` 与 `/rosout`。
- 验证：执行短时真实硬件启动，新最小 topic 策略通过：
  - `Baton Mini right`: `/baton_mini_right`，topics `1/1`
  - `Baton Mini left`: `/baton_mini_left`，topics `1/1`
  - `GoPro right`: `/gopro_right/gopro_right_camera`，topics `1/1`
  - `GoPro left`: `/gopro_left/gopro_left_camera`，topics `1/1`
- 验证：节点运行期间执行 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST ros2 topic list --no-daemon --spin-time 3`，普通可见 topic 只剩：
  - `/baton_mini_left/odometry`
  - `/baton_mini_right/odometry`
  - `/gopro_left/image_raw`
  - `/gopro_right/image_raw`
  - `/parameter_events`
  - `/rosout`
- 验证：Baton Mini 日志显示已按配置关闭无用设备流：`close imu receive`、`close fast odom receive`、`close image receive`。
- 修复：增强 `start_all_sensor.sh` 的停止逻辑，总 launch 现在运行在独立进程组中，cleanup 会对整个进程组发送 `INT`，必要时再发送 `TERM`，避免子节点残留。
- 收尾：短时验证结束后 timeout 触发 cleanup，当前无残留 `start_all_sensor`、`all_sensor_nodes`、`baton_mini`、`v4l2_camera_node`、`image_topic_relay` 进程；localhost 范围 topic 重新只剩 `/parameter_events` 与 `/rosout`。
- 判断：`/home/hit/ROS/src/gopro_camera_launch` 当前仍有用；总 launch 仍通过它启动 `v4l2_camera_node`，并复用该包的 GoPro 参数、安装与单相机调试入口。除非以后把 GoPro launch 逻辑合并进总 launch 或新包，否则不应删除。

## 2026-04-28 14:53:20 CST

- 现象：用户询问当前进程中是否有残留 ROS，并要求如有则编写清理脚本。
- 排查：`pgrep -af 'ros2|launch|all_sensor_nodes|start_all_sensor|baton_mini|v4l2_camera|gopro|image_topic_relay|pressure_driver|octopus|fastdds|dds|rviz|rqt'` 发现仍有一套 all-sensor 相关进程在运行：
  - `ros2 launch /home/hit/ROS/launch/all_sensor_nodes.launch.py`
  - 两个 `baton_mini` 节点
  - 两个 `v4l2_camera_node` 节点
  - 两个 `image_topic_relay` 节点
  - 一个 `pressure_driver_node` 节点
  - 一个 `ros2-daemon`
- 新增：`/home/hit/ROS/cleanup_ros_residue.sh`，用于清理当前工作区一键启动链路残留进程。
- 行为：脚本优先对总 launch 进程组发送 `SIGINT`，等待退出；仍残留的采集子进程再发送 `SIGTERM`，最后必要时发送 `SIGKILL`；默认还会执行 `ros2 daemon stop`。
- 验证：`bash -n cleanup_ros_residue.sh` 通过。
- 验证：`./cleanup_ros_residue.sh --dry-run` 只匹配当前 all-sensor launch 进程组、相关采集子节点和 `ros2-daemon`，未执行实际停止。

## 2026-04-28 15:00:00 CST

- 现象：用户要求改写 `/home/hit/ROS/start_all_sensor.sh`，使其在运行所有节点之前先编译所有节点。
- 修改：`start_all_sensor.sh` 新增自动编译流程，默认 `AUTO_BUILD=1`。
- 行为：脚本现在先 source `/opt/ros/jazzy/setup.bash`，再在 `/home/hit/ROS` 执行 `colcon build`，编译成功后再 source `/home/hit/ROS/install/setup.bash`，最后继续执行硬件预检与总 launch。
- 行为：默认编译 `colcon` 在工作区能发现的全部包；如需临时只编指定包，可设置 `BUILD_PACKAGES="baton_mini gopro_camera_launch hwk_pressure_interfaces hwk_pressure_driver"`。
- 行为：如需跳过自动编译，可设置 `AUTO_BUILD=0 ./start_all_sensor.sh`。
- 验证：`bash -n start_all_sensor.sh` 通过。
- 备注：当前本机仍有上一轮 all-sensor 进程残留，未直接执行新脚本启动；正式运行前建议先执行 `./cleanup_ros_residue.sh --dry-run` 确认后再执行 `./cleanup_ros_residue.sh`。

## 2026-04-29 16:35:07 CST

- 现象：用户反馈 MCAP 文件中 `/gopro_left/image_raw` 为 30Hz，但 `/gopro_right/image_raw` 为 60Hz，要求全部改成 30Hz。
- 排查：源码配置 `/home/hit/ROS/src/gopro_camera_launch/config/gopro_camera.yaml` 已为 `time_per_frame: 1/30`；工作区根安装空间 `/home/hit/ROS/install/gopro_camera_launch/.../gopro_camera.yaml` 也已为 30。
- 排查：总配置 `/home/hit/ROS/config/all_sensor_nodes.yaml` 中 `gopro.right.frame_rate` 与 `gopro.left.frame_rate` 均为 `30`，总 launch 会将该值传给 GoPro 子 launch。
- 排查：发现旧包内安装副本 `/home/hit/ROS/src/gopro_camera_launch/install/gopro_camera_launch/share/gopro_camera_launch/config/gopro_camera.yaml` 仍残留 `denominator: 60`，且旧 launch 未执行启动前 `v4l2-ctl --set-parm`。
- 修复：将该旧安装副本的 `gopro_camera.yaml` 同步为 `image_size: [640, 480]`、`time_per_frame: 1/30`。
- 修复：将该旧安装副本的 `gopro_pose_record.launch.py` 同步为当前版本，启动 `v4l2_camera_node` 前先执行 `v4l2-ctl -d <device> --set-parm 30`，并支持 `frame_rate`、namespace、node name、topic 参数。
- 验证：全局检查 `config`、`launch`、`src/gopro_camera_launch`、`install/gopro_camera_launch` 范围内不再存在 `frame_rate: 60`、`denominator: 60` 或 `--set-parm 60`。
- 验证：`python3 -m py_compile launch/all_sensor_nodes.launch.py src/gopro_camera_launch/launch/gopro_pose_record.launch.py install/gopro_camera_launch/share/gopro_camera_launch/launch/gopro_pose_record.launch.py src/gopro_camera_launch/install/gopro_camera_launch/share/gopro_camera_launch/launch/gopro_pose_record.launch.py` 通过。
- 验证：`bash -n start_all_sensor.sh` 通过。
- 实机观察：当前驱动层查询显示 right 采集卡仍为 `60.000 fps`，left 为 `30.000 fps`。
- 尝试：对两路执行 `v4l2-ctl -d <device> --set-parm 30` 时返回 `Device or resource busy`，说明当前采集卡正被运行中的相机节点占用，不能热修改。
- 结论：配置层已统一为 30Hz；当前这一轮正在运行的 right 仍为 60Hz，需要停止 GoPro/all-sensor 节点后重新执行 `./start_all_sensor.sh`，启动前置 `v4l2-ctl --set-parm 30` 才会对 right 生效。
