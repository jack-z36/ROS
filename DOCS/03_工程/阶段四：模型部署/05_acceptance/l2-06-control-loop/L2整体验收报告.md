# L2-06 整体验收报告（L2 Gate Report）

> [!info] 归属
> - 所属 L2：l2-06-control-loop（ControlLoop 中央运行调度闭环）
> - L2 验收卡片：`DOCS/03_工程/阶段四：模型部署/03_tasks/cards/l2-06-control-loop/l2-06-control-loop_整体验收卡片.md`
> - 调度索引：`DOCS/03_工程/阶段四：模型部署/03_tasks/task/dispatch/l2-06-control-loop.yaml`
> - 验证脚本：`src/model_deploy/act/scripts/l2_06_verify.sh`
> - 本文件是 L2 Gate 整体验收报告（per-card），是 L2 验收机制（`04_L2验收机制.md`）与人类验收机制（`05_人类验收机制.md`）的衔接产物；Git 合入仍需人类签字与风险记录。

## 1. L2 验收结论

**PASS_LOCAL**（人类验收待分项签字；外部 scope 保持 BLOCKED）

L2-06 跨模块集成验证入口已建立并通过。所有 10 个 L3（deploy_051–054、055、056–060）本地 `direct-local` / `static-review` 全部 PASS_LOCAL，0 个 FAIL_LOCAL；唯一验证脚本 `l2_06_verify.sh` 在 `--scope local --policy fake` 下：

- **13 PASS / 0 FAIL / 3 BLOCKED**（共 16 标签）
- 3 个外部 BLOCKED（`G10_ROS_OBSERVE` / `G11_REAL_BUNDLE` / `G12_REAL_COMMAND`）均属外部 scope（ROS runtime / 真实 bundle-GPU / 真机硬件授权），脚本严格 BLOCKED 而非伪造 PASS
- baseline：`867 passed, 4 skipped`（4 个 skip 全部命中 §2 allowlist 外部原因，无 production/fixture/script 缺失转 BLOCKED 的情况）

## 2. L2 Gate 场景覆盖（G01–G12）

下表逐标签列出 `l2_06_verify.sh` 实际输出（与 `logs/deploy_055_verify_local.log` 字节一致）：

| 标签 | 分组 | 状态 | 含义 |
|---|---|---|---|
| `G01` | types / boundary | **PASS** | types 纯净 + 无 forbidden token/import，静态边界扫描 |
| `G02` | config / repo | **PASS** | default-off、YAML 不能静默启用、arg 仅 2 旗、canonical 资源/spec 身份、loader fail-fast |
| `G03` | observation / service / publish seam | **PASS** | pipeline 干跑 env_blocked、collector→snapshot、service 预测、safety PASS、publisher provenance |
| `G04` | channel / metrics | **PASS** | request 校验、result XOR、LatestQueue 容量 1 close、metrics 不可变快照 |
| `G05` | worker | **PASS** | serial success、error 恢复、CLOCK_INVALID 致命闸口 |
| `G06` | scheduling | **PASS** | correlation/active chunk、prefetch@horizon、result-id 错配 latch fault |
| `G07` | fallback / output | **PASS** | 六 outcome OBSERVED/PUBLISHED/BLOCKED/REJECTED/FAILED/PARTIAL + 观测缺失 fallback |
| `G08` | UI / lifecycle | **PASS** | preflight pass、SPEC_IDENTITY_MISMATCH、PERMIT_SOURCE_MISSING、deny fail-closed、shutdown 收敛 |
| `G09` | local full Gate | **PASS** | 完整 tracer bullet（真实 worker）：chunk_activated>0、inference_success>0、published>=1、有界 shutdown |
| `G10_LOCAL` | external gate local-contract | **PASS** | dry-run command=0 契约 + ROS 可用标志自检 |
| `G11_LOCAL` | external gate local-contract | **PASS** | 真实 loader 在空 bundle 下 fail-fast（无法用 fake 伪装 real-policy） |
| `G12_LOCAL` | external gate local-contract | **PASS** | 默认 fail-closed：deny permit、YAML 无命令开关、未授权 command=0 |
| `BASELINE_REGRESSION` | baseline | **PASS** | 全量 `src/model_deploy/act/tests` 0 FAIL |
| `G10_ROS_OBSERVE` | 外部 scope | **BLOCKED (BLOCKED_ENV)** | ROS 2 environment unavailable（`rclpy` 部分安装、无 `rclpy.node`） |
| `G11_REAL_BUNDLE` | 外部 scope | **BLOCKED (BLOCKED_ARTIFACT)** | no real model bundle/GPU in automated acceptance |
| `G12_REAL_COMMAND` | 外部 scope | **BLOCKED (BLOCKED_HARDWARE_EXPECTED)** | no human authorization / e-stop / driver readiness / ROS evidence in automated acceptance |

**合计：13 PASS / 0 FAIL / 3 BLOCKED（共 16 标签）；脚本退出码 0。**

### 2.1 三个 BLOCKED 的正确性论证

| 标签 | 类别 | 正确性证据 | **不是**本地失败的理由 |
|---|---|---|---|
| `G10_ROS_OBSERVE` | `BLOCKED_ENV` | 本环境 `rclpy` 可导入但 `rclpy.node` 不可用（部分安装）；`TestG10RosDryRun::test_dry_run_command_count_is_zero` 已证 dry-run command=0 契约；local-contract G10_LOCAL 仍 PASS | ROS 2 graph 缺失是环境问题，**不是** production 代码或测试问题；脚本严格 BLOCKED 标签（不计入 PASS） |
| `G11_REAL_BUNDLE` | `BLOCKED_ARTIFACT` | 无真实 model bundle/GPU；`TestG11RealPolicyDryRun::test_real_bundle_loader_is_gated` 已证 `load_act_runtime_resources` 在空 bundle 下 fail-fast；local-contract G11_LOCAL 仍 PASS | 真实 artifact 不可获得是外部条件，**不是**代码缺陷；G10 缺 ROS / G11 缺 bundle / G12 缺硬件 三类**互不替代**，不可相互伪装 |
| `G12_REAL_COMMAND` | `BLOCKED_HARDWARE_EXPECTED` | 无 hardware authorization / e-stop / driver readiness / 操作者授权；`TestG12RealCommand::test_default_deny_never_auto_enables_command` + `TestG08Lifecycle::test_deny_permit_is_fail_closed` 已证 fail-closed（deny permit + `command_output_enabled=False`） | 真机 command 是硬件范畴；本轮**绝不**记为真机 PASS，按 04_L2验收机制.md §2 与 05_人类验收机制.md 保留 BLOCKED 标签，等人类分项签字 + 独立授权 |

> 脚本退出码严格由 `FAIL_COUNT` 决定（>0 → 1，=0 → 0）；BLOCKED 行**不**通过 `PASS_COUNT` 计入；local 跑全 16 标签中 0 个 FAIL，**绝不**"以 BLOCKED 替代 FAIL"。`BLOCKED_HARDWARE_EXPECTED` 与 04_L2验收机制.md §2 字面 allowlist `BLOCKED_HARDWARE` 同义：项目内 L2-02 / L2-04 / L2-05 / L2-06 等多个 L2 的卡片与已完成的 L3（deploy_044/045/054/057）均使用 `BLOCKED_HARDWARE_EXPECTED` 作为标准硬件授权 BLOCKED 标签，符合项目惯例。

## 3. L3 完成度（10/10 PASS_LOCAL）

| L3 | 标题 | 主验收 | Gate 场景 | 结论 | 反馈文件 |
|---|---|---|---|---|---|
| deploy_051 | 推理通道与运行指标基础 | direct-local | G04 | **PASS_LOCAL** | `logs/deploy_051_acceptance_round_1.md` |
| deploy_052 | InferenceWorker 串行异步执行 | direct-local | G05 | **PASS_LOCAL** | `logs/deploy_052_acceptance_round_1.md` |
| deploy_053 | ControlLoop 中央调度状态机 | direct-local + downstream-l2 | G06, G07 | **PASS_LOCAL**（ROS timer 真机时序 DEFER_TO_L2_GATE） | `logs/deploy_053_acceptance_round_1.md` |
| deploy_054 | ActDeployNode 原子装配与生命周期 | direct-local + blocked | G08 | **PASS_LOCAL**（+ BLOCKED_ENV + BLOCKED_HARDWARE_EXPECTED） | `logs/deploy_054_acceptance_round_1.md` |
| deploy_055 | L2 Gate 跨模块集成与验收脚本 | direct-local + blocked | G01–G12 | **PASS_LOCAL**（13 PASS / 0 FAIL / 3 BLOCKED） | `logs/deploy_055_acceptance_round_1.md` + `logs/deploy_055_verify_local.log` |
| deploy_056 | L2-01 启动资源与配置接缝修复 | direct-local + downstream-l2 | G02, G03 | **PASS_LOCAL**（真实 bundle/GPU DEFER_TO_L2_GATE） | `logs/deploy_056_acceptance_round_1.md` |
| deploy_057 | L2-02 观测流水线契约修复 | direct-local + env | G03 | **PASS_LOCAL**（真实 ROS graph / 真实 gripper topology DEFER_TO_L2_GATE） | `logs/deploy_057_acceptance_round_1.md` |
| deploy_058 | L2-03 Canonical Spec 消费接缝 | direct-local | G03 | **PASS_LOCAL**（真实 GPU/bundle DEFER_TO_L2_GATE；非阻断清理建议见 §6） | `logs/deploy_058_acceptance_round_1.md` |
| deploy_059 | L2-04 安全端口与设计投影对齐 | static-review + local | G03, G07 | **PASS_LOCAL**（无 production 行为改动） | `logs/deploy_059_acceptance_round_1.md` |
| deploy_060 | L2-05 发布失败追因契约 | direct-local | G03, G07 | **PASS_LOCAL**（真机 command 链 BLOCKED_HARDWARE_EXPECTED） | `logs/deploy_060_acceptance_round_1.md` |

**汇总：10/10 L3 全部 PASS_LOCAL，0 FAIL_LOCAL，0 失败轮次。** 所有 L3 任务文件已由主 Agent 归档至 `DOCS/03_工程/阶段四：模型部署/03_tasks/completed/l2-06-control-loop/`（10 个文件全部就位）。

## 4. 验证命令与实际输出

### 4.1 L2 Gate 唯一验证入口（必跑）

```bash
bash src/model_deploy/act/scripts/l2_06_verify.sh \
  --scope local --policy fake \
  --config src/model_deploy/act/tests/fixtures/l2_06_fake.yaml
```

实际输出（与 `logs/deploy_055_verify_local.log` 字节一致）：

```text
=== L2-06 Gate 验收 (scope=local, policy=fake) ===
=== L2-06 Gate 本地核心 (本地 G01-G09 + 外部闸口契约) ===

[ types / boundary ]
  PASS  G01  types 纯净 + 无 forbidden token/import，静态边界扫描
[ config / repo ]
  PASS  G02  default-off、YAML 不能静默启用、arg 仅 2 旗、canonical 资源/spec 身份、loader fail-fast
[ observation / service / publish seam ]
  PASS  G03  pipeline 干跑 env_blocked、collector->snapshot、service 预测、safety PASS、publisher provenance
[ channel / metrics ]
  PASS  G04  request 校验、result XOR、LatestQueue 容量 1 close、metrics 不可变快照
[ worker ]
  PASS  G05  serial success、error 恢复、CLOCK_INVALID 致命闸口
[ scheduling ]
  PASS  G06  correlation/active chunk、prefetch@horizon、result-id 错配 latch fault
[ fallback / output ]
  PASS  G07  六 outcome OBSERVED/PUBLISHED/BLOCKED/REJECTED/FAILED/PARTIAL + 观测缺失 fallback
[ UI / lifecycle ]
  PASS  G08  preflight pass、SPEC_IDENTITY_MISMATCH、PERMIT_SOURCE_MISSING、deny fail-closed、shutdown 收敛
[ local full Gate ]
  PASS  G09  完整 tracer bullet（真实 worker）：chunk_activated>0、inference_success>0、published>=1、有界 shutdown
[ external gate local-contract proofs ]
  PASS  G10_LOCAL  dry-run command=0 契约 + ROS 可用标志自检
  PASS  G11_LOCAL  真实 loader 在空 bundle 下 fail-fast（无法用 fake 伪装 real-policy）
  PASS  G12_LOCAL  默认 fail-closed：deny permit、YAML 无命令开关、未授权 command=0
[ baseline regression ]
  PASS  BASELINE_REGRESSION  全量 src/model_deploy/act/tests 0 FAIL
[ 外部 scope（预期 BLOCKED，绝不伪造 PASS）]
  BLOCKED  G10_ROS_OBSERVE  ROS 2 environment unavailable (BLOCKED_ENV); local/mock dry-run passed, command topics silent by contract
  BLOCKED  G11_REAL_BUNDLE  no real model bundle/GPU in automated acceptance (BLOCKED_ARTIFACT); local fake-policy gate passed the loader fail-fast contract
  BLOCKED  G12_REAL_COMMAND  no human authorization / e-stop / driver readiness / ROS evidence in automated acceptance (BLOCKED_HARDWARE_EXPECTED)

────────────────────────────────
  13 PASS / 0 FAIL / 3 BLOCKED  (共 16 标签)
  config: src/model_deploy/act/tests/fixtures/l2_06_fake.yaml   policy: fake   scope: local
```

退出码：**0**（无 FAIL）。

### 4.2 跨模块集成测试

```bash
PYTHONPATH=src python3 -m pytest src/model_deploy/act/tests/integration/test_l2_06_gate.py -v
```

结果：**43 passed in 1.17s**。覆盖 G01–G12 全部 12 个场景，含原计划三条 real-chain tests（`test_observation_to_inference_real_chain` / `test_control_loop_publish_chain` / `test_control_loop_fallback_matrix`）的等价 scope（已在 `logs/deploy_055_acceptance_round_1.md` §1.2 显式登记为合入 `test_l2_06_gate.py` 的 doc 偏差，scope 等价不构成 FAIL_LOCAL）。

### 4.3 全量基线

```bash
PYTHONPATH=src python3 -m pytest src/model_deploy/act/tests -q
```

结果：**867 passed, 4 skipped, 2 warnings in 5.00s**

- 4 skip 全部命中 04_L2验收机制.md §2 allowlist 外部原因（`rclpy` 不可用时 `ObservationRosAdapter.env_blocked` 等自检 skip），无 production/fixture/script 缺失被转 BLOCKED 的情况。
- 2 warning：① `RuntimeWarning: invalid value encountered in divide`（`repo/normalization.py` 零范围分母自检，预存）；② `PytestUnhandledThreadExceptionWarning: KeyboardInterrupt`（`test_inference_worker.py::test_keyboard_interrupt_not_swallowed`，刻意注入以验证 worker 不吞 `BaseException`），均与 L2-06 无关。

## 5. 验证层级（04_L2验收机制.md §7 对齐）

| 层级 | 运行内容 | 实际运行 | 结果 |
|---|---|---|---|
| import / unit | C1–C26、真实 contracts | `test_l2_06_gate.py::TestG01–TestG09` | PASS |
| mock integration | 真实 L2-02~05 + fake policy/FakeNode/clock | `TestG03Seam` + `TestG09FullGate` | PASS |
| local composition | A5 + fake external providers | `TestG08Lifecycle` + `TestG09FullGate` | PASS |
| production entry | B11 + injected resource-loader test seam | `TestG02Config::test_loader_fails_fast_on_empty_bundle` + `TestG12RealCommand::test_cli_parser_has_no_yaml_command_switch` | PASS（fail-fast + 仅 CLI） |
| ROS dry-run | 真实 ROS graph、fake policy、command disabled | 真实 rclpy 部分安装（无 `rclpy.node`） | `BLOCKED_ENV` |
| real-policy dry-run | real bundle/GPU、command disabled | 无真实 artifact | `BLOCKED_ARTIFACT` |
| real command | driver/permit/E-stop/授权 | 默认 fail-closed | `BLOCKED_HARDWARE_EXPECTED` |

## 6. 未验证项与放行

| 项 | 类别 | 阻塞阶段 | 放行条件 |
|---|---|---|---|
| 真实 ROS 2 graph 端到端（G10） | `BLOCKED_ENV` | 端到端 L3、shadow-run | 部署到有完整 `rclpy.node` 的 ROS 2 环境后重跑 verify（`--scope ros-dry-run`） |
| 真实 model bundle + GPU（G11） | `BLOCKED_ARTIFACT` | 端到端 L3、shadow-run | 注入真 bundle + GPU 镜像后重跑 verify（`--scope real-policy-dry-run`） |
| 真实 command 路径 / driver / E-stop / permit topology（G12） | `BLOCKED_HARDWARE_EXPECTED` | 真机结论 | permit source / driver / E-stop / 操作者授权齐备后人工受控验证（按 05_人类验收机制.md） |
| 真实 `CommandPermitSource` 的 gate/deadman/E-stop/driver-ready topic 或 driver API | `BLOCKED_HARDWARE_EXPECTED` | 真机结论 | 同上 |
| `safe_stop` 是否需要主动硬件 stop | 设计待定 | 真机结论 | 需新增明确 driver port（若决定需要） |
| gripper 的真实 ROS message type、单位和 topic | `BLOCKED_HARDWARE_EXPECTED` | 真机结论 | 同上 |

> 这些未验证项**不阻塞 local Gate**，但**阻塞**端到端 L3、shadow-run 与真机结论；本 Gate 通过后才能进入人类验收阶段，Git 合入仍需人类签字与风险记录。

## 7. 非阻断观察 / 建议（**不**计入本地失败）

下列项是 L3 验收子代理登记的"非阻断观察"，主 Agent / 后续 design-package L3 可单独处理，本 L2 Gate 全部判为 PASS_LOCAL。

1. **L2-01 / L2-02 / L2-05 HTML 设计包结构校验器缺口（预存）**：`validate_l2_design_package.py` 报错全部是 HTML 结构块缺失（`io-flow`/`ovtab`/`term`/`trtab`、缺 `03a` 文件等），与 L2-06 自身接缝修复无关。按加载规则 **MD 优先于 HTML**，且权威源 agent_context Markdown 已同步；建议单列一个 design-package L3 全面刷新（deploy_056/057/060 反馈均登记）。
2. **L2-05 HTML C6 表 stale 字段**：`L2架构交互可视化.html` 第 853/857/858 行仍为 `safety_ok` / `sent_to_driver` / `failure_reason`（与真实代码字段 `command_output_enabled` / `command_permitted` / `outcome` / `status_published` / `reason_code` 不一致）。本 L3 deploy_060 仅追加 `failure_stage` / `failed_topic`（在 859/860 行），stale 字段为 PRE-EXISTING；建议另起 design-package L3 全面刷新（deploy_060 反馈登记）。
3. **deploy_058 ACTION_DIM 残留**：`observation_batch.py:20` 残留未使用的 `ACTION_DIM` import（死代码，全文件 0 处引用）；`act_inference.py:47`/`:88` docstring 提及 `ACTION_DIM`（纯散文）。非可执行 fallback / Dict 默认补洞，与"消除 `ACTION_DIM` fallback / 第二份 spec"精神相悖但不影响行为；建议清理（deploy_058 反馈 §9 登记为非阻断修复请求）。
4. **deploy_055 L3 任务文件 doc 偏差**：§18.5 summary 数字误写为 `12 PASS / 0 FAIL / 3 BLOCKED  (共 18 标签)`，实际为 `13 PASS / 0 FAIL / 3 BLOCKED  (共 16 标签)`；§6/§10 列了 3 个 real-chain test 文件名，实际合入 `test_l2_06_gate.py`（43 case）等价。范围等价不构成 FAIL_LOCAL；建议归档前用实际数字与单一文件表述覆盖（deploy_055 反馈 §6 登记）。

## 8. 修改范围审计（deploy_055 Gate 范围 / 实际未触碰 production）

deploy_055（L2 Gate 跨模块集成与验收脚本）L3 实际仅新增/修改（与任务 §10 允许范围一致）：

- `src/model_deploy/act/tests/integration/test_l2_06_gate.py`（新增，43 case）
- `src/model_deploy/act/scripts/l2_06_verify.sh`（新增，唯一 verify 入口）
- `src/model_deploy/act/tests/fixtures/l2_06_fake.yaml`（新增，bundle 留空以验证 fail-fast）
- `DOCS/03_工程/阶段四：模型部署/05_acceptance/l2-06-control-loop/{验收结果.md, L2验收报告.md, logs/deploy_055_verify_local.log}`（新增）
- L3 任务文件 `§15` 成功标准勾选 + `§18` 执行总结
- `test_l2_06_gate.py::FakePolicy.predict_action_chunk` 策略域四元数从 `[0,0,0,1]` 调整为 `[-1,-1,-1,1]`（测试替身内部修正，匹配 `ActionStateNormalizer` min-max 反归一化语义；不动 production）

未修改（production source）：

- `src/model_deploy/act/runtime/{control_loop,inference_worker,inference_channel,runtime_metrics}.py`
- `src/model_deploy/act/ui/{act_deploy_node,action_publisher,observation_pipeline}.py`
- `src/model_deploy/act/service/{act_inference,safety_guard}.py`
- `src/model_deploy/act/repo/act_runtime_resources.py`
- `src/model_deploy/act/{types,config,repo,service,runtime,ui}/*.py`（任务 §11 禁止）
- L1 / L2-01~05 agent_context/HTML（仅同步 L2-06 自身投影；deploy_056~060 owner 范围）

> L2-06 整体验收范围**只读**：本 L2 验收子代理**未编辑**源码、测试、dispatch、L3 卡片、L3 任务文件、未做 Git 同步、未移动/归档任何文件（主 Agent 已完成 L3 归档动作）。本报告是 L2 验收子代理的**唯一**新增交付物。

## 9. 整体 L2 结论

**PASS_LOCAL**（条件：所有外部 scope 保持 BLOCKED 状态，硬件/真机结论需人类验收授权）

L2-06 ControlLoop 中央运行调度闭环的 L2 Gate 已通过：

- **L3 维度**：10/10 全部 PASS_LOCAL，0 FAIL_LOCAL
- **Gate 维度**：13 PASS / 0 FAIL / 3 BLOCKED（共 16 标签）
- **Baseline 维度**：867 passed / 4 skipped（仅外部 allowlist 原因）/ 0 FAIL
- **3 BLOCKED 全部正确分类**：`BLOCKED_ENV`（G10_ROS_OBSERVE）/ `BLOCKED_ARTIFACT`（G11_REAL_BUNDLE）/ `BLOCKED_HARDWARE_EXPECTED`（G12_REAL_COMMAND）—— **不**构成 local 失败；**不**写为真机/真 ROS/真 bundle 通过；按 04_L2验收机制.md §2 与 05_人类验收机制.md 保留 BLOCKED 标签，等人类分项签字 + 独立授权
- **production source**：L2 验收子代理未编辑；唯一 verify 入口 `l2_06_verify.sh` + 43 case 集成测试 + 1 fixture（fake policy）已建立

L2-06 满足 local Gate，可进入人类验收阶段；Git 合入仍需人类签字与风险记录（按 05_人类验收机制.md）。

## 10. 人类验收入口

- 人类验收签字入口：`DOCS/03_工程/阶段四：模型部署/05_acceptance/l2-06-control-loop/验收结果.md`
- L2 设计文档入口：`DOCS/03_工程/阶段四：模型部署/02_implement/l2-06-control-loop_ControlLoop中央运行调度闭环/agent_context/`（尤其 `04_L2验收机制.md` / `05_人类验收机制.md`）
- 唯一运行时可观察信号：`/act/metrics`（schema_version=1, l2_id="l2-06-control-loop"），无 `/act/command/status` 写权（L2-05 专属）
- 真机 command 验证：默认 fail-closed（deny permit + `command_output_enabled=False`），需 permit source / driver / E-stop / 操作者授权齐备后人工受控验证
