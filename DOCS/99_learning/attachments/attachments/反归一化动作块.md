---
tags:
  - 附件
  - pi05
  - deploy
---

# 反归一化动作块

> [!abstract]
> 模型一次推理生成的未来动作序列，但不直接整块发送

## 基本信息

| 属性 | 值 |
| --- | --- |
| 变量名 | `action_chunk, actions, ActionChunk.actions` |
| 参考系 | 不适用；这是程序数据流节点，不是几何坐标变换结果 |
| 相对原点 | 不适用 |
| 物理锚点 | 纯数学对象，承载控制目标 |
| 阶段属性 | 推理后中间态 |
| 是否最终输出 | 部分是 |
| 数据类型 | `np.ndarray shape=(chunk_size, 14)` |
| 数据结构 | 二维矩阵；每一行 14 个标量共同描述一个未来控制步的双臂动作 |
| 所在文件 | `pi05_test/pi05/deploy/src/pi05/deploy/models/policy_loader.py:63-78; inference_worker.py:74-86` |
| 现实含义 | 模型一次推理生成的未来动作序列，但不直接整块发送 |

## 关键澄清

### 1. 它在哪个参考系下？

不适用。这里的“参考系”不是机器人空间坐标系，而是程序流水线中的数据阶段。

### 2. 它相对哪个原点？

不适用。它不是空间点或位姿变换结果。

### 3. 它对应哪个物理点 / 物理对象？

纯数学对象，承载控制目标。如果它是缓存、请求或 batch，则无对应物理点，这是纯数学对象 / 程序容器。

### 4. 它是不是最终输出？

部分是。

### 5. 它不是什么？

它不应被误解为“整个推理程序的最终命令”。只有 ROS 控制命令输出才是下游执行系统真正收到的命令。

## 对应源码

```python
action_chunk = self.action_normalizer.unnormalize(norm_chunk).numpy(); return action_chunk[: self.output_chunk_size]
```

## 一句话说清楚

> 模型一次推理生成的未来动作序列，但不直接整块发送

## 在数据流中的位置

- 上游：策略推理batch
- 下游：单步14D候选动作
