# 当前进度

## 当前状态

场景六已建立文档目录和固定五类文档。数据清洗交互式启动器已经可用；配置/标定向导已从“选择闭合/张开 MCAP”重构为浏览器版实时 GoPro 标定中心，但尚未用真实硬件完成现场标定和全量 49 个 MCAP 的最终批量验收。

## 已完成

- 梳理 `/home/hit/ROS/src/data_clean` 功能：
  - 位姿 topic 的 TCP 变换。
  - GoPro 图像中的 ArUco 夹爪宽度提取。
  - 输出 MCAP 保留原始 topic，并新增 gripper width topic。
- 建立独立 conda 环境：
  - `/home/hit/ROS/.conda-envs/data-clean`
- 修复并验证运行环境问题：
  - MCAP zstandard 解压依赖。
  - Octopus schema 缩进兼容。
  - OpenCV ArUco segfault 问题。
- 完成交互式启动器：
  - `./start_data_clean.sh`
  - `core.mcap_clean_launcher`
- 已验证：
  - `--dry-run --latest 5`
  - `--latest 1`
  - `--dry-run --all --workers auto`
  - `DATA_CLEAN_RAW_JSON=1 --latest 1`
  - 配置错误时的 topic 检查失败提示。
- 已修复一次输入目录错位问题：
  - 原始 49 个 MCAP 曾位于 `/home/hit/ROS/mcap_cleaned/mcap`。
  - 已移回 `/home/hit/ROS/mcap`。
  - `./start_data_clean.sh --dry-run --latest 1` 已重新验证可扫描 49 个文件。
- 已实现实时 GoPro 标定中心：
  - `./start_data_clean.sh --calibrate`
  - 交互菜单 `c`
  - 新模块 `core.mcap_calibration_wizard`
  - 支持生成 `config/data_clean_calibrated.yaml`
  - 支持左右手独立 `pose_streams[].transform`
- 已新增 GoPro-only 启动入口：
  - `./start_gopro_only.sh`
  - `scripts/start_gopro_only.py`
  - 只启动左右 GoPro 节点，供实时标定使用。
- 标定中心现在支持：
  - 分项状态：左/右夹爪、左/右 TCP。
  - 浏览器页面实时显示当前手 GoPro 画面，不再依赖 OpenCV GUI 窗口。
  - 实时夹爪宽度标定，不再选择闭合/张开 MCAP。
  - 浏览器 TCP 截图点选标定。
  - 自动按每个 pose stream 的单位转换 TCP 偏移。
  - 部分保存配置，覆盖前自动备份旧的 `data_clean_calibrated.yaml`。
- 已验证：
  - 旧 smoke 配置仍可 `--dry-run --latest 1`。
  - 新标定状态结构可正确判断四项完成后才显示“已标定”。
  - `./start_gopro_only.sh --help` 可用。
  - `./start_data_clean.sh --dry-run --latest 1` 仍可扫描 49 个 MCAP 并通过首个 topic 检查。
  - 当前抽样判断：`/baton_mini_left/fast_odom` 位姿量级按 `mm` 处理，`/baton_mini_right/fast_odom` 位姿量级按 `m` 处理。

## 正在处理

- 等待用户连接并启动左右 GoPro 后，使用实时标定中心完成现场夹爪和 TCP 标定。

## 未完成

- 未使用实时 GoPro 标定中心生成正式 `/home/hit/ROS/config/data_clean_calibrated.yaml`。
- 未完成左右 TCP 截图点选并确认真实偏移值。
- 未完成全部 49 个 MCAP 的正式清洗验收。
- 未验证清洗后 MCAP 是否满足后续训练、分析或回放程序的最终格式要求。

## 风险

- 当前 `config/data_clean_smoke_test.yaml` 名称仍带 `smoke_test`，容易让用户误解它只是测试配置；但它目前被作为默认配置使用。
- `overwrite: true` 会覆盖已有同名输出，适合快速迭代，但正式数据管理时需要谨慎。
- 默认并行 6 个文件可能对磁盘和内存造成压力；如果批量清洗不稳定，可改用 `--workers 1` 或更小数值。
- 不要把原始输入目录放到 `mcap_cleaned` 里面；启动器默认只扫描 `/home/hit/ROS/mcap`。
- TCP 截图点选需要浏览器页面；如果浏览器无法打开，只能完成夹爪终端兜底，不能完成 TCP 点选。
- TCP 单位已改为自动按 pose stream 单位换算，但仍建议在真实清洗后抽样检查 TCP 输出是否符合预期。

## 下次优先事项

建议下次优先：

1. 确认左右 GoPro 已连接、供电、HDMI 输入正常。
2. 运行实时标定中心：

   ```bash
   ./start_data_clean.sh --calibrate
   ```

3. 先执行 `1 夹爪宽度实时标定`，再执行 `2 TCP 截图点选标定`。
4. 用 `./start_data_clean.sh --dry-run --all --workers auto` 复核全量计划。
5. 用小批量命令验证稳定性：

   ```bash
   ./start_data_clean.sh --latest 3 --workers 3
   ```

6. 再执行全部清洗：

   ```bash
   ./start_data_clean.sh --all --workers auto
   ```

7. 对清洗后 MCAP 抽样检查 topic 与消息数量。
