# 执行记录

## 2026-05-06

- 操作：补充场景一输出程序与文件清单。
- 新增文档：`/home/hit/ROS/DOCS/场景一/输出程序与文件.md`。
- 内容：整理逐个启动 4 个相机节点涉及的 Baton Mini、GoPro launch、主程序、配置、运行指南和目标 topic。
- 结果：已在 `当前进度.md` 中加入该清单索引。
- 说明：本次只修改 DOCS 文档，没有修改 ROS 节点、launch 或配置代码。

## 2026-04-24 13:54:09 CST

- 操作：根据用户提供的样本文档，整理并写入场景一目标描述。
- 核对：已阅读公共规则、Octopus 架构、场景一固定文档、`gopro_camera_launch` 架构与运行指南、`baton_mini_sdk_demo` 架构与运行指南。
- 发现：`gopro_camera_launch` 当前默认发布两个话题：`/gopro/image_raw` 和 `/gopro/camera_info`。
- 发现：`baton_mini_sdk_demo` 源码当前默认快速里程计话题为 `/baton_mini/fast_odom`，不是 `/baton_mini/fast_odometry`。
- 结果：已在 `目标描述.md` 中区分“本项目 right/left 期望命名”和“当前代码默认发布话题”。

## 2026-04-24 14:57:43 CST

- 操作：根据用户提供的 Baton Mini 双相机接入方案阶段总结，整理并写入场景一背景信息。
- 核对：已阅读公共规则、场景一固定文档、`baton_mini_sdk_demo` 架构与运行指南，并核对 Baton 官方 ROS2 多机通信、多设备接入、USB Demo 文档。
- 发现：本项目选择“Ubuntu 上位机运行 SDK Demo，并由上位机发布 ROS2 topic”路线，而不是直接依赖设备内部 ROS2 topic。
- 发现：右相机配置为 `192.168.1.10` + `baton_mini_right`，左相机配置为 `192.168.2.10` + 网关 `192.168.2.1` + `baton_mini_left`。
- 发现：`baton_mini.launch.py` 当前只声明并传入 `server_ip` 与 `local_ip`；topic 参数虽然在源码中支持读取，但 launch 文件尚未显式暴露。
- 结果：已在 `背景信息.md` 中记录双相机 IP、namespace、技术路线、验证流程和后续 SDK 节点参数。

## 2026-04-24 15:29:56 CST

- 操作：用户确认 4 个相机已全部连接后，逐个测试 BatonMini right、BatonMini left、GoPro right、GoPro left 是否能发布 ROS2 topic。
- 启动前检查：
  - BatonMini right 网卡本机 IP：`192.168.1.11`，设备 IP：`192.168.1.10`，ping 成功。
  - BatonMini left 网卡本机 IP：`192.168.2.11`，设备 IP：`192.168.2.10`，ping 成功。
  - GoPro 采集卡视频设备：`/dev/video5` 与 `/dev/video4` 是 Video Capture；`/dev/video6`、`/dev/video7` 是 metadata capture。
- 修改：
  - `src/baton_mini_sdk_demo/launch/baton_mini.launch.py` 暴露 `node_name` 和 5 个 topic 参数，用于 right/left 分别命名。
  - `src/gopro_camera_launch/launch/gopro_pose_record.launch.py` 暴露 `camera_namespace`、`node_name`、`camera_name`、`frame_id`，用于 right/left 分别命名。
  - `src/baton_mini_sdk_demo/src/baton_mini.cpp` 启动后自动执行 stereo 图像接收，相当于自动执行交互命令 `4`。
- 构建：
  - `source /opt/ros/jazzy/setup.bash && colcon build --packages-select baton_mini gopro_camera_launch`
  - 结果：构建成功。
- BatonMini right：
  - 启动命令使用 `server_ip:=192.168.1.10 local_ip:=192.168.1.11 node_name:=baton_mini_right`，topic 前缀为 `/baton_mini_right/...`。
  - SDK 连接成功、登录成功，自动打开算法、IMU、fast odom、stereo 图像接收。
  - 已验证可 echo：`/baton_mini_right/imu`、`/baton_mini_right/fast_odom`、`/baton_mini_right/image_left`、`/baton_mini_right/image_right`。
- BatonMini left：
  - 启动命令使用 `server_ip:=192.168.2.10 local_ip:=192.168.2.11 node_name:=baton_mini_left`，topic 前缀为 `/baton_mini_left/...`。
  - SDK 连接成功、登录成功，自动打开算法、IMU、fast odom、stereo 图像接收。
  - 已验证可 echo：`/baton_mini_left/imu`、`/baton_mini_left/fast_odom`、`/baton_mini_left/image_left`。
  - `ros2 topic list` 中可见 `/baton_mini_left/image_left`、`/baton_mini_left/image_right`、`/baton_mini_left/imu`、`/baton_mini_left/odometry` 以及算法侧 topic。
- GoPro right：
  - 映射：`/dev/video5` → `/gopro_right/...`。
  - 已验证可 echo：`/gopro_right/image_raw`、`/gopro_right/camera_info`。
- GoPro left：
  - 映射：`/dev/video4` → `/gopro_left/...`。
  - 已验证可 echo：`/gopro_left/image_raw`、`/gopro_left/camera_info`。
- 注意：
  - GoPro 启动时提示缺少默认相机标定文件 `/home/hit/.ros/camera_info/ugreen_25173:_ugreen_25173.yaml`，但不阻断 `image_raw` 和 `camera_info` 发布。
  - BatonMini 和 v4l2 节点退出时有时不能在 SIGINT 后立即结束，会被 launch 升级到 SIGTERM/SIGKILL；本次测试结束后确认无残留节点。
  - 本次只验证 ROS2 topic 发布，尚未验证 Octopus 订阅和 MCAP 录制。

## 2026-04-24 15:41:11 CST

- 操作：根据用户要求，整理 GoPro 两个节点的运行方式文档。
- 文档：`/home/hit/ROS/DOCS/场景一/GoPro两个节点运行说明.md`。
- 内容：记录 GoPro right/left 的 `/dev/videoX` 映射、launch 启动命令、topic 验证命令、相机标定 warning 和 Octopus 边界。
- 结果：后续可直接按该文档逐个启动或复查两个 GoPro 节点。
