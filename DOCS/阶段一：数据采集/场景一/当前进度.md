# 当前进度

## 已完成

- 已建立场景一固定文档结构。
- 已完成场景一目标描述文档整理：`/home/hit/ROS/DOCS/阶段一：数据采集/场景一/目标描述.md`。
- 已完成场景一背景信息文档整理：`/home/hit/ROS/DOCS/阶段一：数据采集/场景一/背景信息.md`。
- 已对齐 GoPro 当前代码事实：单个 GoPro 节点默认发布 `/gopro/image_raw` 与 `/gopro/camera_info`。
- 已对齐 BatonMini 当前代码事实：默认话题为 `/baton_mini/...`，快速里程计默认话题为 `/baton_mini/fast_odom`。
- 已记录 Baton Mini 双相机基础配置：right 使用 `192.168.1.10` + `baton_mini_right`，left 使用 `192.168.2.10` + `baton_mini_left`。
- 已补充场景一执行约束。
- 已补齐 BatonMini 和 GoPro 的 right/left 启动命名参数。
- 已新增 GoPro 双节点运行说明文档：`/home/hit/ROS/DOCS/阶段一：数据采集/场景一/GoPro两个节点运行说明.md`。
- 已新增场景一输出程序与文件清单：`/home/hit/ROS/DOCS/阶段一：数据采集/场景一/输出程序与文件.md`。
- 已完成 4 个相机节点逐个 ROS2 topic 发布验证：
  - BatonMini right：`/baton_mini_right/imu`、`/baton_mini_right/fast_odom`、`/baton_mini_right/image_left`、`/baton_mini_right/image_right` 可发布。
  - BatonMini left：`/baton_mini_left/imu`、`/baton_mini_left/fast_odom`、`/baton_mini_left/image_left` 可发布，`/baton_mini_left/image_right` 已出现在 topic 列表。
  - GoPro right：`/dev/video5` 发布 `/gopro_right/image_raw`、`/gopro_right/camera_info`。
  - GoPro left：`/dev/video4` 发布 `/gopro_left/image_raw`、`/gopro_left/camera_info`。

## 正在处理

- 尚未进行 Octopus 订阅和 MCAP 录制验证。
- GoPro 相机标定文件缺失警告尚未处理，但不影响当前图像 topic 发布验证。

## 下一步

- 启动 Octopus，确认能看到并订阅 4 个相机节点的目标 topic。
- 在 Octopus 中执行开始录制和停止录制，确认输出 `.mcap` 文件。
- 如后续需要更稳定的退出流程，补充 BatonMini SDK 节点的 shutdown 处理。
