# 当前 L2 acceptance 入口

本目录只存放新版 ACT 功能闭环 L2 的验收材料。

每个新版 L2 必须包含：

```text
05_acceptance/<new-l2-id>/验收结果.md
05_acceptance/<new-l2-id>/L2整体验收报告.md
05_acceptance/<new-l2-id>/人类验收清单.md
```

旧 `l2-01-types` 等 layer-based 验收材料已隔离到 `_legacy_layer_based_act/`，不得作为当前 Gate 或合入依据。
