# 场景五当前进度

## 当前状态

场景五已进入第一阶段执行：已完成一次当前机器硬件身份扫描，并新增硬件身份扫描 / 映射校验脚本和映射模板。

已新增场景五输出程序与文件清单：`/home/hit/ROS/DOCS/阶段一：数据采集/场景五/输出程序与文件.md`。

本场景目标已修正：最终仍是把真实触觉传感器数据稳定发布到固定 topic，但触觉部分的主路径改为由 `hwk_pressure_driver` 运行时读取传感器协议内序列号，并按独立配置文件中的“序列号 -> topic”表直接分发数据。

当前最新状态是：场景五目标描述已从“序列号 -> 现实 id -> topic”修正为“序列号 -> topic”。`r1` 已通过 HWK 协议内 `CHANNEL=0x01 CMD=0x05` 查询到芯片 UID `00600012-3035510B-3735333`，目标绑定关系为 `00600012-3035510B-3735333 -> /pressure/right_hand/gripper_1`；`hwk_pressure_driver` 已实现启动时读取 UID 并按 UID 映射到固定 topic 的逻辑，且 `r1` 已实测能发布真实压力帧到 `/pressure/right_hand/gripper_1`。

最新补充：用户通过 `/dev/ttyUSB1` 查询到 `HWK_CHIP_UID=007B0036-3035510B-3735333`，并要求填写为 `r2`；当前已写入 `/home/hit/ROS/config/hardware_identity_map.yaml`，目标 topic 为 `/pressure/right_hand/gripper_2`。

## 已完成

- 已创建 `/home/hit/ROS/DOCS/阶段一：数据采集/场景五`。
- 已创建场景五五个固定文档：
  - `目标描述.md`
  - `背景信息.md`
  - `执行约束.md`
  - `执行记录.md`
  - `当前进度.md`
- 已在 `/home/hit/ROS/AGENTS.md` 中加入场景五索引。
- 已在 `/home/hit/ROS/DOCS/public_rules.md` 中加入场景五摘要和 DOCS 管理规则。
- 已记录用户目标：硬件信息映射为规定设备名和固定 topic。
- 已记录触觉现实编码：
  - `l1`
  - `l2`
  - `r1`
  - `r2`
- 已记录 GoPro 侧识别对象是 HDMI 采集卡硬件信息。
- 已确认当前配置中 GoPro 主要使用 `video_device`，触觉主要使用串口 `port` 和 udev / by-path 路径。
- 已新增硬件身份扫描 / 映射校验脚本：`/home/hit/ROS/scripts/hardware_identity_scan.py`。
- 已新增硬件身份映射模板：`/home/hit/ROS/config/hardware_identity_map.yaml`。
- 已执行当前机器硬件扫描，结果显示当前没有 `/dev/ttyUSB*` 触觉串口设备。
- 已确认当前 V4L2 只看到内置摄像头 `Integrated Camera`，没有看到目标 GoPro HDMI 采集卡。
- 已执行映射模板校验，校验按预期失败：6 个逻辑设备的 `match` 字段尚未标定。
- 在用户表示硬件已接入后再次扫描，仍未看到 CH340 `1a86:7523` 或 HDMI 采集卡；`lsusb` 也未列出目标硬件。
- 用户明确只接入 `gopro right` 和触觉 `r1` 后，已扫描到 1 个 CH340 串口和 2 个 UGREEN 25173 capture 设备。
- 已在 `/home/hit/ROS/config/hardware_identity_map.yaml` 中绑定 `pressure.r1`。
- 已在 `/home/hit/ROS/config/hardware_identity_map.yaml` 中绑定 `gopro.right`。
- 已调整 `/home/hit/ROS/scripts/hardware_identity_scan.py`，使 `ID_PATH` 映射校验时优先显示对应 `by-path` 路径。
- 用户补充了 HWK `CHANNEL=0x01` 设备信息查询协议，已新增 `/home/hit/ROS/scripts/hwk_query_device_info.py` 查询脚本。
- 已验证当前 `r1` 能响应压力数据通道 `CHANNEL=0x02`。
- 已验证当前 `r1` 能响应设备信息通道 `CHANNEL=0x01 CMD=0x05`，芯片 UID 为 `00600012-3035510B-3735333`。
- 已确认当前 `r1` 设备信息响应的 `frame_type` 为 `0x02`，与用户文档中 ACK `0x03` 的描述不一致；脚本已兼容 `0x02` 和 `0x03`。
- 已将 `pressure.r1` 的映射从 USB 拓扑 `ID_PATH` 调整为协议内 `HWK_CHIP_UID=00600012-3035510B-3735333`。
- 已扩展 `hardware_identity_scan.py validate`，使其能在校验串口设备时调用 `hwk_query_device_info.py` 查询 `HWK_CHIP_UID`。
- 已按用户新设想重写 `/home/hit/ROS/DOCS/阶段一：数据采集/场景五/目标描述.md`：触觉部分目标改为 `hwk_pressure_driver` 读取序列号并根据“序列号 -> topic”配置直接分发。
- 已在 `hwk_pressure_driver` 协议层加入 `CHANNEL=0x01 CMD=0x05` UID 查询能力。
- 已将触觉驱动改为启动时先读取 `HWK_CHIP_UID`，再根据 `/home/hit/ROS/config/hardware_identity_map.yaml` 选择 publisher。
- 已将 `pressure_sensors.yaml` 改为候选串口发现模式：串口只负责打开设备，不再决定发布 topic。
- 已将 `start_all_sensor.sh`、`all_sensor_status.py` 和 `all_sensor_nodes.launch.py` 接入硬件映射校验与 resolved GoPro 设备路径。
- 已为 `gopro.left` 补充当前 UGREEN 采集卡 USB 拓扑 fallback 映射。
- 已在 `/home/hit/ROS/config/hardware_identity_map.yaml` 中绑定 `pressure.r2`：`007B0036-3035510B-3735333 -> /pressure/right_hand/gripper_2`。

## 新增但还不熟悉的部分

- 已接入的 CH340 串口 `ID_SERIAL=1a86_USB_Serial`，看起来不是可靠唯一序列号；其余 3 个 CH340 是否相同仍待实测。
- HWK 触觉传感器协议已知存在 `CHANNEL=0x01 CMD=0x05` 查询芯片 UID 的设计；当前接入的 `r1` 已查询成功，但实测响应类型为 `0x02` 而不是文档中的 `0x03`。
- 已接入的两个 UGREEN 25173 capture 设备暴露相同 `ID_SERIAL=ITE_UGREEN_25173_00000001` 和 `ID_SERIAL_SHORT=00000001`，不能用序列号区分左右。
- 目标硬件接入后是否存在可直接使用的 `/dev/serial/by-id` 和 `/dev/v4l/by-id` 稳定链接尚未确认。
- 硬件映射表的初版文件名和字段结构已确定；`r1` 已按协议内 UID 标定，`gopro.right` 已按 USB 拓扑 fallback 标定，其余设备尚未标定。

## 正在处理

当前已完成触觉驱动级 UID 分发改造。正在等待标定 `l1/l2` 的 `HWK_CHIP_UID`，填入 `/home/hit/ROS/config/hardware_identity_map.yaml` 后才能让总启动前校验通过。

## 尚未完成

- 尚未建立 `l1/l2` 到硬件身份的映射表。
- 尚未建立 `gopro_left` 到采集卡硬件身份的唯一硬件序列号映射；当前仅使用 USB 拓扑 fallback。
- 尚未验证同一传感器改变 `/dev/ttyUSBX` 枚举后仍发布到同一 topic。
- 尚未生成动态设备路径配置。
- 尚未验证改变插拔顺序或 USB 口后 topic 名称仍稳定。
- 尚未在映射完成后复测场景三 MCAP 和场景四 UI。

## 已部署 / 已测试 / 已验证状态

- 已部署：部分。`hardware_identity_scan.py`、`hwk_query_device_info.py`、`hardware_identity_map.yaml`、触觉 UID 分发驱动和总启动前映射校验均已落地；`l1/l2` 尚未标定。
- 已测试：部分。已运行 `py_compile`、`colcon build --packages-select hwk_pressure_driver`、`hardware_identity_scan.py validate`、`hwk_query_device_info.py --cmd uid`，并短时启动 `pressure_driver_node` 验证 `r1` 可按 UID 发布到固定 topic；`r2` 映射校验已通过当前串口解析。总映射校验仍因 `l1/l2` 缺失 UID 且当前未接入 `r1` 按预期失败。
- 已验证：否。当前没有完成插拔顺序变化后的稳定映射验证。

## 当前风险

- 多个同型号 CH340 可能没有唯一序列号，导致仅靠 USB 设备属性无法区分四个触觉设备。
- 多个同型号 HDMI 采集卡可能没有唯一序列号，导致仅靠采集卡属性无法区分左右 GoPro。
- 当前 `ID_PATH` / by-path 依赖 USB 插口拓扑，不能满足“无论怎么接入都稳定”的最终目标。
- 如果硬件身份不足，必须设计人工标定流程，否则容易把左右手或 l1/l2/r1/r2 映射错。
- 当前 `r1` 设备信息通道会响应，但响应类型实测为 `0x02`，不是文档中的 `0x03`；后续接入其他触觉传感器时需要确认是否一致。
- 任何 topic 名称变化都可能影响场景三 MCAP 配置和场景四 UI 显示。

## 下一步

建议下次优先标定 `l1/l2`，记录它们的 `HWK_CHIP_UID` 并写入 `/home/hit/ROS/config/hardware_identity_map.yaml`。全部 required 设备都有映射后，再执行总启动和插拔顺序稳定性验证。

推荐命令：

```bash
python3 /home/hit/ROS/scripts/hardware_identity_scan.py scan
python3 /home/hit/ROS/scripts/hwk_query_device_info.py --port /dev/ttyUSBX --addr 6 --package-id 29 --cmd uid --gripper-id l1 --timeout 1.0
lsusb
lsusb -t
python3 /home/hit/ROS/scripts/hardware_identity_scan.py validate --map /home/hit/ROS/config/hardware_identity_map.yaml
```

建议后续实现顺序：

```text
1. 接入并标定 `l1/l2` 的序列号；
2. 把两个 UID 填入 `/home/hit/ROS/config/hardware_identity_map.yaml`；
3. 执行 `hardware_identity_scan.py validate`，确认 4 个触觉和 2 个 GoPro 均解析成功；
4. 执行 `./start_all_sensor.sh`，确认固定 topic 全部出现；
5. 改变插拔顺序和 `/dev/ttyUSBX` 枚举做稳定性验证；
6. 复测固定 topic、Octopus UI 和 MCAP。
```
