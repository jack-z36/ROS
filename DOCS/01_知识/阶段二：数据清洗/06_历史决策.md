# 阶段二历史决策

本页记录已经影响当前理解模型的历史决策。普通执行时不应读取旧归档全文。

## common_frame 降级

common_frame / 旧 robot_base 转换已降级为历史兼容，不再作为生产主链路。当前生产语义以左右 arm-base TCP pose 为主。

## 旧 canonical 方案归档

旧 canonical dataset 五场景方案已经归档。其设计可用于历史复盘，但不得作为当前阶段二路线权威。

## LeRobot v3 桥接

当前已经跑通 LeRobot v3 桥接路径，用于验证数据进入训练格式生态。正式数据产物语义和训练侧 action 表达仍需按当前工程与约束继续演进。
