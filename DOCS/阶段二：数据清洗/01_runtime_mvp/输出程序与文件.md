# Runtime MVP 输出程序与文件

## 当前软件产出

当前尚未新增 Runtime MVP 代码或脚本。

当前已有文档产出：

| 文档 | 路径 | 说明 |
| --- | --- | --- |
| Runtime MVP 六件套 | `/home/hit/ROS/DOCS/阶段二：数据清洗/01_runtime_mvp/` | 记录 Runtime MVP 目标、背景、约束、记录、进度和预期输出。 |
| 任务记录机制 | `/home/hit/ROS/DOCS/阶段二：数据清洗/03_tasks/` | 记录 active/completed 任务、对话确认区和执行计划。 |

## 预期软件产出

| 产出 | 职责 | 状态 |
| --- | --- | --- |
| Runtime 入口 | 接收场景选择和运行参数 | 未开始 |
| 配置读取能力 | 读取并校验阶段二配置 | 未开始 |
| run 目录管理 | 为每次运行创建独立目录 | 未开始 |
| fake service | 验证 Runtime 调用链路 | 未开始 |
| run log | 记录输入、配置、步骤、输出和耗时 | 未开始 |
| processing manifest | 记录影响数据语义的配置和版本信息 | 未开始 |
| error summary | 记录失败场景、步骤和原因 | 未开始 |
| `./start_data_clean.sh --dev` 入口 | 进入阶段二开发者交互菜单 | 未开始 |

## 数据产物落点

真实数据产物默认写入：

`/home/hit/ROS/asset/阶段二：数据清洗/`

Runtime 调试产物应写入独立 run 目录，不能污染正式产物目录。
