# L2-01 ACT Types 层 人类验收清单

> 规则依据：`DOCS/02_约束/工作流/阶段四开发工作流/attachments/人类验收关卡规则.md`

## 说明

AI 已跑完 L2 Gate（全量 `act/tests/types/` 单测通过），判定 L2-01 ACT Types 层达到可合入状态。
但 Gate 通过不等于可以合入。请人类按以下清单亲自运行确认，**签字通过后才允许合入 `model_deploy` 并删除三级开发分支**。AI 不得越过本关卡自行合入。

- 验收运行目录：`/home/hit/ROS`
- L2 Git 分支：`feat/model_deploy/l2-01-types`
- 集成分支：`model_deploy`

## 验收项

| # | 运行命令 | 观察点 | 通过现象 | 验收性质 | 结果 |
|---|---|---|---|---|---|
| 1 | `cd /home/hit/ROS && pytest src/model_deploy/act/tests/types/test_action_spec.py -v` | stdout | 全部 PASSED；`ACTION_DIM=16`；`split_bimanual_action` 段序交替（左tcp7+左width1+右tcp7+右width1） | 机械 | [ ] 通过 |
| 2 | `cd /home/hit/ROS && pytest src/model_deploy/act/tests/types/test_state_codec.py -v` | stdout | 全部 PASSED；`encode_state` 输出 16D 分组段序（左tcp7+右tcp7+左width1+右width1）；quaternion 模长校验生效（模长≠1 报 ValueError） | 机械 | [ ] 通过 |
| 3 | `cd /home/hit/ROS && pytest src/model_deploy/act/tests/types/test_action_codec.py -v` | stdout | 全部 PASSED；`split_action → as_vector()` round-trip 与原始一致 | 机械 | [ ] 通过 |
| 4 | `cd /home/hit/ROS && pytest src/model_deploy/act/tests/types/ -v` | stdout | 全量 PASSED；负向测试覆盖边界（错维度/错 dtype/模长≠1/width 越界/空/None） | 机械 | [ ] 通过 |

## 失败现象与排查入口

| 失败现象 | 排查入口 |
|---|---|
| `ModuleNotFoundError: No module named 'act.types...'` | 检查 `src/model_deploy/act/types/__init__.py` 及 `act/tests/types/__init__.py` 是否存在；确认从仓库根目录运行 |
| 维度断言失败（ACTION_DIM/STATE_DIM≠16） | 检查 `act/types/action_spec.py` 常量段与 split 切片 `[0:7]/[7]/[8:15]/[15]` |
| state/action 段序混淆 | state 应为分组、action 应为交替；核对 `encode_state` 与 `as_vector` |
| quaternion 模长校验漏 | 检查 `state_codec.py` 的 `_check_quaternion`，确认 `abs(norm-1.0)>1e-3` 抛 ValueError |
| width 越界未报错 | 检查 `state_codec.py` 的 `_check_gripper_width`，确认 `<0` 或 `>1` 抛 ValueError |

## 签字位置

请在 `05_acceptance/l2-01-types/验收结果.md` 末尾「人类验收」段签字（勾选通过 + 用户名 + 日期）：

```markdown
## 人类验收

- 验收人：<用户名>
- 验收日期：YYYY-MM-DD
- 验收结论：[ ] 已通过　[ ] 不通过
- 逐项结果：
  - 验收项 1：pytest .../test_action_spec.py -v → 全部 PASSED，ACTION_DIM=16，split 交替段序 → [ ] 通过
  - 验收项 2：pytest .../test_state_codec.py -v → 全部 PASSED，encode 16D 分组，quaternion 校验 → [ ] 通过
  - 验收项 3：pytest .../test_action_codec.py -v → 全部 PASSED，round-trip 一致 → [ ] 通过
  - 验收项 4：pytest .../types/ -v → 全量 PASSED，负向测试覆盖边界 → [ ] 通过
- 备注：<若不通过，填写失败项和反馈>
```

未填写或勾选「不通过」时，视为人类验收未通过，AI 不得合入。
