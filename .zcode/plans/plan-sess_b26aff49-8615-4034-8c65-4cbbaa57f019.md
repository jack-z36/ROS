## 目标

把维度4 从「7 个分散命令卡、人类逐条手动跑」改成「一个 .sh 脚本统一跑 + 终端分层分组简洁打印 + 维度4 当终端输出翻译表」。同时更新有映射关系的 md，并在 md 里明确 .sh 脚本的设计需求，供后续 L3 微元任务生成该脚本。

**本次不创建 .sh 脚本本身**（用户明确：只写设计需求，留给后续 L3）。

## 终端输出格式（已与用户确认）

- **分层分组**：按 types / service / runtime / ui·边界 分块，每块下列标签（与维度3图②六层落点视觉对齐）。
- **FAIL 附简要定位**：FAIL 行下方缩进打印 文件/微元/pytest 节点/错误摘要。
- 结尾汇总 `N PASS / N FAIL / N BLOCKED`。

## 验证标签清单（每标签 = 一个子功能模块，可定位到维度3图②微元）

把现有 7 个粗验收项拆成细粒度标签：

| 标签 | 层 | 对应微元 | 验证内容 |
|---|---|---|---|
| `contract.encoded_state_dim` | types | ObservationSnapshot 数据/计算 | 16D 维度校验 |
| `contract.importable` | types | — | 契约可 import（L2-03/L2-06 零猜测）|
| `collector.mock_snapshot` | service | snapshot() 计算函数 | mock 全字段→snapshot (16,) |
| `collector.missing_reject` | service | snapshot() + missing_fields | 缺字段→None+missing 列表 |
| `collector.stale_reject` | service | snapshot() + stale_fields | 过期→None+stale 列表 |
| `preprocess.image` | service | preprocess_observation_image | 图像预处理 shape/dtype |
| `buffer.latest_only` | runtime | set_observation 内部状态更新 | 写 A/B→读 B |
| `buffer.max_age` | runtime | latest_observation 计算函数 | max_age_s 生效 |
| `adapter.no_ros_importable` | ui | — | types/service/runtime 无 ROS 可 import |
| `boundary.no_overreach` | 边界 | — | 无推理/ActionChunk/safety/硬件越界 |
| `boundary.no_config_repo` | 边界 | — | config/repo 无新增 .py |
| `adapter.real_subscription` | ui | create_subscriptions | 真实 ROS 订阅（BLOCKED_ENV）|

## 三文件改动

### 1. `04_L2验收机制.md`（重构 §3、§4）

- **§3 Gate 验收项** → 重构为「验证标签表」：标签 / 层 / 对应微元 / 测试输入 / 通过现象 / 失败现象。替换原粗粒度 6 行表。
- **§4 建议命令** → 重构为「统一验收脚本设计需求」章节，明确：
  - 脚本路径建议：`scripts/l2_02_verify.sh`（或同等位置）
  - **输出格式规约**：分层分组 + FAIL 定位 + 汇总行（贴终端范例）
  - **标签-微元映射表**（上表），要求脚本每个标签的 PASS/FAIL 能映射回维度3图②微元
  - **退出码规约**：全 PASS→0；有 FAIL→1；仅 BLOCKED→0（环境约束非失败）
  - **BLOCKED 判定**：缺 ROS / 缺 L2-01 上游 → 标 BLOCKED 而非 FAIL
  - **声明**：本节是后续 L3 微元任务生成 .sh 的设计依据
- 保留 §5 Blocked、§6 失败现象、§7 下游判断不变。

### 2. `05_人类验收机制.md`（§2 清单同步）

- §2 人类验收清单：把"设计包结构/边界审查/Pi0.5映射/HTML对齐/ROS真机边界"5 项保留（这些是文档审查，不是代码测试）。
- **新增一项**：`代码验收脚本` → 命令为 `bash scripts/l2_02_verify.sh`，观察点为"终端分层分组输出 + 汇总行"，通过现象"无 FAIL（BLOCKED 可接受）"。
- §3 签字格式同步加这一项。

### 3. HTML `L2架构交互可视化.html`（维度4 重构）

把维度4 从 7 个 vfy-item 改为**两部分**：
- **Part A：怎么验收**（精简）：一句"运行 `bash scripts/l2_02_verify.sh`" + 说明分层分组输出格式 + FAIL 定位 + 汇总行含义 + BLOCKED ≠ 失败。配一个终端输出范例（用 `<pre>` 或现有 `.cmd-block` 风格）。
- **Part B：终端输出翻译表**（核心）：用表格/卡片，每个验证标签一行，列：标签 / 所属层 / 对应维度3图②微元 / PASS 含义 / FAIL 含义（FAIL 时该查哪个文件/微元）。这就是"翻译表"——让你看终端任何一行都能查到含义和出问题的模块。
- 保留 BLOCKED callout 和签字位置说明。
- `data-agent-source` 保持指向 `04_L2验收机制.md` / `05_人类验收机制.md`。

## 不改动的部分

- 维度1/2/3 不动。
- `00_INDEX.md` 对齐表：维度4 行的 "Human-visible meaning" 更新为"运行 .sh + 终端输出翻译表"，其余不动。
- 不创建 .sh 脚本、不改 src 源码、不生成 L3。

## 验证方式（改完后）

- HTML 标签平衡 + kind 标签仍为正式 5 类（grep 校验）。
- 三个 md 的脚本设计需求/标签表相互一致（标签名一一对应）。
- HTML 翻译表的每个标签都能映射回维度3图②的具体 classbox 微元。