# L3 数据定义类主体模板

适用目标：定义枚举、dataclass、TypedDict、Pydantic model、schema 字段或原子数据定义文档。

````md
## 5. 数据定义输出

### 需要定义的对象

| 对象 | 类型 | 放置位置 | 下游使用者 |
|---|---|---|---|
|  | enum / dataclass / schema / 原子文档 |  |  |

### 字段或取值

| 字段 / 取值 | 类型 | 含义 | 默认值 | 合法性要求 |
|---|---|---|---|---|
|  |  |  |  |  |

## 6. 数据定义验收重点

- 能被 import 或被文档链接引用。
- 能实例化或能被 schema 校验工具读取。
- 字段类型、默认值和非法值处理符合 L2 定义。
- 相关原子数据定义文档已创建或复用，并在 L2/L3 中用 `[[wikilink]]` 引用。
````

