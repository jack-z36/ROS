# Web 生产交互语义

普通 Web 入口面向批次数据集构建，不是单文件调试器。用户选择 raw MCAP 后，系统按生产链路生成中间产物和最终 LeRobot v3 dataset。

## 关键语义

- 普通 Web 固定走生产语义。
- 最终 dataset 可输出到用户选择的本机父目录。
- 中间产物、报告、日志和 sidecar 仍应保留在受控目录。
- 单个文件失败不应阻断其他成功 episode 发布。

## 边界

Web 当前行为如果变化，应更新本页和对应工程记录。具体 UI 代码入口、运行命令和调试记录属于工程文档。
