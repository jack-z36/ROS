# 旧 layer-based ACT L2 隔离区

本目录保存旧版 `types / config / assembly / publish / hardware` 拆法的 ACT L2 卡片。

这些文件只允许作为人工历史参考或迁移素材，不是当前阶段四 ACT 开发主线。当前主线以 `00_L1_ACT部署程序任务文档.md`、`01_L1_ACT功能模块边界.md` 和 `02_L1_ACT功能模块协作架构.md` 定义的 7 个功能闭环 L2 为准。

Agent 默认不得读取本目录作为 L2 / L3 生成依据。
