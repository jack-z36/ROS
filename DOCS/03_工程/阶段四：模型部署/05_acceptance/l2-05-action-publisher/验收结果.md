# L2-05 Action Publisher — 验收结果 (deploy_045)

> 本文件由 deploy_045 执行 Agent 初始化的结果骨架。人类/验收 Agent 负责补充
> 观察证据与签字；真机项默认 BLOCKED，不记录通过。

## 1. 验收环境与命令

- 运行目录：`/home/hit/ROS/worktrees/l2-05-action-publisher`
- Python：`python3`（pytest 7.4.4）；ROS 2：`rclpy` 可导入（用于类型，未连接真实 graph）
- 集成测试：`PYTHONPATH=src python3 -m pytest src/model_deploy/act/tests/integration/test_l2_05_gate.py -v`
- 统一脚本：`bash src/model_deploy/act/scripts/l2_05_verify.sh [--case <name>]`

## 2. 汇总 (2026-07-13, direct-local)

| 层级 | 标签 | 结果 | 说明 |
|---|---|---|---|
| types | TYPES_CONTRACT (G01) | PASS | C1-C6 frozen 契约与不变式 |
| config | CLI_DEFAULT_OFF (G02) | PASS | 不传 CLI → `command_output_enabled=False` |
| config | CLI_EXPLICIT_ON (G03) | PASS | 显式 CLI → C7=True，审计可见 |
| service | B1_TOPIC_PAYLOADS (G04/G05/G06) | PASS | B1 16D→C4；PASS/ADJUSTED；拒绝；拆分；0/50/100 |
| ui | B2_MESSAGE_BUNDLE (G07/G08) | PASS | 五消息、无 status、无 partial |
| ui | B3_COMMAND_GATE (G09-G15/G17) | PASS | 门控/PARTIAL/gripper/status/多 tick 同步 |
| boundary | BOUNDARY_CLEAN (G16) | PASS | 无 subscription/timer/mode/accepted/TF/IK/SDK |
| boundary | NO_RUNTIME_ARTIFACT (G16) | PASS | repo/runtime 无 L2-05 产物 |
| ros-observe | ROS_OBSERVE (G18) | BLOCKED | 无实时观察目标（dry-run 验收），command 静默由契约保证 |
| hardware | HARDWARE (G19) | BLOCKED | 无人工授权/急停/现场证据（BLOCKED_HARDWARE_EXPECTED） |

- 集成测试：`41 passed`
- `verify --case local`：`8 PASS / 0 FAIL / 2 BLOCKED`，退出码 `0`
- 各人类 case（command-disabled / permit-blocked / topic-payloads / ros-message-bundle / command-enabled-mock）均 `EXIT=0`；`ros-observe` 记 2 BLOCKED `EXIT=0`；未知 case `EXIT=2`

## 3. 边界扫描证据 (G16)

静态扫描 `types/action_publish.py`、`service/action_output_adapter.py`、`ui/action_publisher.py`：

- 禁止 token（正则 `\b(create_subscription|create_timer|RuntimeConfig\.mode|publishes_command_topics|\.accepted|MoveIt|IK|TF|Modbus|serial|RM65)\b`）在去除 docstring/注释后均无命中。
- 禁止 import root：`rospy`/`serial`/`modbus`/`threading`/`queue` 均无（仅 `ui/action_publisher.py` 有**惰性** `rclpy` import，设计允许）。
- `src/model_deploy/act/repo/` 与 `src/model_deploy/act/runtime/` 不含 `action_publish.py`/`action_output_adapter.py`/`action_publisher.py`（无 L2-05 产物泄漏）。

## 4. 真机风险控制

- verify 默认不触发真实 driver，不含 command-enabled 真机 case。
- G18/G19 在本环境为预期 BLOCKED，未声称真机 PASS；真实执行需现场验收卡授权。

## 5. 人类验收签字

- 验收人：jack-z36
- 验收日期：2026-07-13
- 验收结论：[x] 已通过  [ ] 不通过
- 逐项结果：
  - H01 统一验证脚本：`bash src/model_deploy/act/scripts/l2_05_verify.sh --case local` -> 8 PASS / 0 FAIL / 2 BLOCKED，EXIT=0 -> [x] 通过
  - H02 CLI 默认关闭：`PYTHONPATH=src python3 -c "from model_deploy.act.config.schema import DeployConfig; DeployConfig.from_mapping()" ` -> `command_output_enabled=False` 审计可见 -> [x] 通过
  - H03 permit 阻断：`--case permit-blocked` -> command 通道 0 写入，outcome=BLOCKED，EXIT=0 -> [x] 通过
  - H04 B1 Topic 载荷：`--case topic-payloads` -> 16D→C4，拆分/拒绝/0-50-100 映射生效，EXIT=0 -> [x] 通过
  - H05 B2 消息 bundle：`--case ros-message-bundle` -> 五消息、无 status、无 partial，EXIT=0 -> [x] 通过
  - H06 B3 mock 发布：`--case command-enabled-mock` -> 四路 command 写入且 PARTIAL 计数正确，EXIT=0 -> [x] 通过
  - H07 ROS 观察：BLOCKED（无实时 observation 目标，dry-run 验收；command 静默由契约保证）-> [x] 记录完整
  - H08 边界静态审查：G16 边界扫描无 subscription/timer/mode/accepted/TF/IK/SDK，repo/runtime 无 L2-05 产物 -> [x] 通过
  - H09 真实硬件：BLOCKED（无人工授权/急停/现场证据，BLOCKED_HARDWARE_EXPECTED，未伪称 PASS）-> [x] 记录完整
- 备注：用户在会话中确认「验收通过」并授权提交与合入；G18/G19 为真机阻塞项，按规则不阻断本地验收。
