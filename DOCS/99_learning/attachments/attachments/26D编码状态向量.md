---
tags:
  - 附件
  - pi05
  - deploy
---

# 26D编码状态向量

> [!abstract]
> 把结构化双臂状态压成模型可读的固定长度向量

## 基本信息

| 属性 | 值 |
| --- | --- |
| 变量名 | `encoded_state, encode_bimanual_state(state)` |
| 参考系 | 不适用；这是程序数据流节点，不是几何坐标变换结果 |
| 相对原点 | 不适用 |
| 物理锚点 | 纯数学对象，承载物理状态 |
| 阶段属性 | 观测快照内的中间态 |
| 是否最终输出 | 否 |
| 数据类型 | `np.ndarray shape=(26,)` |
| 数据结构 | 26个标量共同描述左臂 6D、右臂 6D、左右手各 1D、左右 EE 位置/RPY 各 6D |
| 所在文件 | `pi05_test/pi05/common/src/pi05/common/data/state_codec.py:27-43` |
| 现实含义 | 把结构化双臂状态压成模型可读的固定长度向量 |

## 关键澄清

### 1. 它在哪个参考系下？

不适用。这里的“参考系”不是机器人空间坐标系，而是程序流水线中的数据阶段。

### 2. 它相对哪个原点？

不适用。它不是空间点或位姿变换结果。

### 3. 它对应哪个物理点 / 物理对象？

纯数学对象，承载物理状态。如果它是缓存、请求或 batch，则无对应物理点，这是纯数学对象 / 程序容器。

### 4. 它是不是最终输出？

否。

### 5. 它不是什么？

它不应被误解为“整个推理程序的最终命令”。只有 ROS 控制命令输出才是下游执行系统真正收到的命令。

## 对应源码

```python
np.concatenate([...]); if vector.size != STATE_DIM: raise ValueError
```

## 一句话说清楚

> 把结构化双臂状态压成模型可读的固定长度向量

## 在数据流中的位置

- 上游：BimanualState
- 下游：策略推理 batch
