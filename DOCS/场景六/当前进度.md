# 当前进度

## 当前状态

场景六已建立文档目录和固定五类文档。数据清洗交互式启动器已经实现并通过轻量验证，但真实标定参数和全量 49 个 MCAP 的最终批量验收仍未完成。

已新增场景六输出程序与文件清单：`/home/hit/ROS/DOCS/场景六/输出程序与文件.md`。

已新增面向网页版 ChatGPT 作为背景上下文使用的原理说明文档：`/home/hit/ROS/DOCS/场景六/数据清洗产物原理说明_给ChatGPT上下文.md`。

## 已完成

- 梳理 `/home/hit/ROS/src/data_clean` 功能：
  - 位姿 topic 的 TCP 变换。
  - GoPro 图像中的 ArUco 夹爪宽度提取。
  - 输出 MCAP 保留原始 topic，并新增 gripper width topic。
- 建立独立 conda 环境：
  - `/home/hit/ROS/.conda-envs/data-clean`
- 修复并验证运行环境问题：
  - MCAP zstandard 解压依赖。
  - Octopus schema 缩进兼容。
  - OpenCV ArUco segfault 问题。
- 完成交互式启动器：
  - `./start_data_clean.sh`
  - `core.mcap_clean_launcher`
- 已验证：
  - `--dry-run --latest 5`
  - `--latest 1`
  - `--dry-run --all --workers auto`
  - `DATA_CLEAN_RAW_JSON=1 --latest 1`
  - 配置错误时的 topic 检查失败提示。

## 正在处理

- 将数据清洗工具从可运行状态沉淀为“场景六”的长期文档体系。
- 保持 `AGENTS.md`、`public_rules.md` 与场景六目录一致。

## 未完成

- 未确认真实 TCP transform 参数。
- 未确认真实 ArUco marker id 和夹爪宽度标定范围。
- 未完成全部 49 个 MCAP 的正式清洗验收。
- 未验证清洗后 MCAP 是否满足后续训练、分析或回放程序的最终格式要求。

## 风险

- 当前 `config/data_clean_smoke_test.yaml` 名称仍带 `smoke_test`，容易让用户误解它只是测试配置；但它目前被作为默认配置使用。
- `overwrite: true` 会覆盖已有同名输出，适合快速迭代，但正式数据管理时需要谨慎。
- 默认并行 6 个文件可能对磁盘和内存造成压力；如果批量清洗不稳定，可改用 `--workers 1` 或更小数值。

## 下次优先事项

建议下次优先：

1. 确认并写入真实 TCP 和 ArUco 标定参数。
2. 用 `./start_data_clean.sh --dry-run --all --workers auto` 复核全量计划。
3. 用小批量命令验证稳定性：

   ```bash
   ./start_data_clean.sh --latest 3 --workers 3
   ```

4. 再执行全部清洗：

   ```bash
   ./start_data_clean.sh --all --workers auto
   ```

5. 对清洗后 MCAP 抽样检查 topic 与消息数量。
