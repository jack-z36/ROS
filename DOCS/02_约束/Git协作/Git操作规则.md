# Git 操作规则

## 适用范围

- 阶段：全局
- 任务模式：Git 状态、提交、推送、拉取、远端、分支、合并或同步
- 适用对象：仓库根目录 `.` 工作区
- 不适用对象：阶段专属分支策略正文

本文件只承载全局 Git 底线规则和阶段规则路由。阶段二、阶段四等具体分支模型、提交节奏和合并流程必须放在各自阶段专属文件中。

## 通用强制规则

1. 只有用户明确提出 Git 写操作，或当前任务文件/阶段规则明确授权自动同步时，才允许提交、推送、创建分支、切换分支或合并。
2. 所有拉取默认使用 `git pull --ff-only`。
3. 遇到远端领先、分叉、冲突、非预期工作区变更或需要改写历史时，立即停止，不自动 merge、rebase 或 force push。
4. 禁止使用 `git push --force` 或其他改写远端历史的操作，除非用户明确要求并确认风险。
5. 提交前必须检查：
   - `git status --short --branch`
   - 当前远端是否为 `origin`
   - 当前远端 URL 是否为 `https://github.com/jack-z36/ROS.git`
   - 当前 Git 用户是否符合本仓库协作身份
   - 工作区和暂存区是否只包含本次任务允许的变更
   - 是否存在新增嵌套 `.git` 仓库、超大文件、缓存、私有配置或应忽略产物
6. 提交信息末尾必须注明北京时间，格式为 `北京时间 YYYY-MM-DD HH:MM`，除非用户明确指定提交信息不可改。

## 阶段规则入口

| 阶段 | 必读规则 |
|---|---|
| 阶段二：数据清洗 | `DOCS/02_约束/Git协作/阶段二：数据清洗 Git操作规则.md` |
| 阶段四：模型部署 | `DOCS/02_约束/Git协作/阶段四：模型部署 Git操作规则.md` |

执行阶段任务时，必须先读取本文件，再读取对应阶段专属 Git 规则。阶段专属文件只定义本阶段的分支、同步和合并策略，不得重复或降低本文件的全局底线。
