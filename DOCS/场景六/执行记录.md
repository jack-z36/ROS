# 执行记录

## 2026-05-03

用户要求总结当前数据清洗上下文，并生成场景六的一系列文件夹。

已创建：

- `/home/hit/ROS/DOCS/场景六`
- `/home/hit/ROS/DOCS/场景六/目标描述.md`
- `/home/hit/ROS/DOCS/场景六/背景信息.md`
- `/home/hit/ROS/DOCS/场景六/执行约束.md`
- `/home/hit/ROS/DOCS/场景六/执行记录.md`
- `/home/hit/ROS/DOCS/场景六/当前进度.md`

本次沉淀的事实：

- `data_clean` 是离线 MCAP 清洗程序，不是 ROS2 节点。
- 已建立独立 conda 环境 `/home/hit/ROS/.conda-envs/data-clean`。
- 已新增交互式启动入口 `./start_data_clean.sh` 和 Python launcher `core.mcap_clean_launcher`。
- 用户关心的核心交互是清洗文件数量选择，默认数量为最近 1 个。
- 支持最近 N 个、全部、dry-run、workers auto 或指定数。
- 当前机器默认最高性能并行数定为 6。
- 当前配置仍存在测试 TCP/ArUco 标定参数，正式批量清洗前需确认真实参数。

本轮同时更新索引：

- `/home/hit/ROS/AGENTS.md`
- `/home/hit/ROS/DOCS/public_rules.md`

本次未执行新的全量清洗。

## 2026-05-03 10:31:55 CST：从首次梳理到场景六建立的完整迭代回顾

用户要求回顾从第一条对话开始的全部上下文，并将迭代过程记录到本执行记录文件中。

### 1. 初始梳理 `/home/hit/ROS/src/data_clean`

用户最初提出：梳理 `/home/hit/ROS/src/data_clean` 这个程序，简单介绍它的功能。

当时完成的探索：

- 按工作区规则先阅读 `/home/hit/ROS/DOCS/public_rules.md`。
- 扫描 `src/data_clean` 目录，确认该程序是 Python 离线清洗工具，不是 ROS2 节点。
- 识别核心文件：
  - `core/mcap_clean_batch.py`
  - `core/mcap_io.py`
  - `core/mcap_process_config.py`
  - `core/ros2_codec.py`
  - `core/tcp_transform.py`
  - `core/gripper_width.py`
  - `core/validator.py`

形成的初步结论：

- `data_clean` 用于批量读取 ROS2 MCAP 文件。
- 对 Baton Mini 位姿 topic 做 TCP 坐标变换。
- 对 GoPro 图像 topic 做 ArUco marker 检测，生成夹爪宽度 `std_msgs/msg/Float32` topic。
- 输出新 MCAP，保留原始 topic，并新增夹爪宽度 topic。
- 当时指出 `PoseStreamConfig.output_topic` 在现有写入逻辑中主要用于报告统计，位姿消息仍写回原 topic。

### 2. 首次尝试运行真实数据 `/home/hit/ROS/mcap`

用户随后提供清洗数据目录：`/home/hit/ROS/mcap`，要求尝试脚本能否正常工作。

实际发现：

- `/home/hit/ROS/mcap` 中存在 49 个 `.mcap` 文件。
- 直接运行：

  ```bash
  python3 src/data_clean/core/mcap_clean_batch.py --help
  ```

  会失败，原因是脚本直接执行时找不到 `core` 包。

- 改用 `PYTHONPATH=src/data_clean python3 -m core.mcap_clean_batch` 后，又发现当前系统 Python 环境缺少可用的 `mcap.reader`、`mcap_ros2` 等依赖。
- 进一步检查后确认 Octopus 仓库自带 MCAP Python 包，位于：
  - `/home/hit/ROS/src/VTLA_octopus-master/octopus/3rdparty/mcap/python/mcap`
  - `/home/hit/ROS/src/VTLA_octopus-master/octopus/3rdparty/mcap/python/mcap-ros2-support`

对首个 MCAP 做 summary 检查，确认其包含 8 个 topic：

- `/baton_mini_left/fast_odom`
- `/baton_mini_right/fast_odom`
- `/gopro_left/image_raw`
- `/gopro_right/image_raw`
- `/pressure/left_hand/gripper_1`
- `/pressure/left_hand/gripper_2`
- `/pressure/right_hand/gripper_1`
- `/pressure/right_hand/gripper_2`

### 3. 发现和修复运行链路问题

为了让脚本实际处理 MCAP，逐步发现并处理以下问题：

1. **zstandard 压缩依赖缺失**
   - 真实 MCAP 使用 zstandard 压缩。
   - 当前 Python 环境缺少 `zstandard` 时，读取 MCAP 报：

     ```text
     unsupported compression type zstandard
     ```

   - 曾临时安装到 `/tmp/data_clean_pydeps` 做验证，后续改为 conda 环境。

2. **Octopus 写入的 ROS2 schema 有缩进**
   - `mcap_ros2` 动态解析 `nav_msgs/msg/Odometry` schema 时，因 schema 文本存在统一缩进导致解析失败。
   - 在 `core/ros2_codec.py` 中加入 schema 规范化逻辑。
   - 在 `core/mcap_io.py` 中输出原始 channel/schema 时也使用规范化后的 schema。

3. **系统 OpenCV ArUco 模块 segfault**
   - 系统自带 `cv2.aruco.detectMarkers` 对空白图也会段错误。
   - 初步加过图像连续内存修复：
     - 在 `core/gripper_width.py` 中将图像转为连续 `uint8` 灰度数组。
   - 最终通过独立 conda 环境安装可用的 `opencv-contrib-python-headless` 解决。

4. **缺少配置文件**
   - 当时仓库没有找到现成的 `data_clean` YAML 示例。
   - 新增了 `/home/hit/ROS/config/data_clean_smoke_test.yaml` 作为清洗配置。

### 4. 建立独立 conda 环境

用户提出：用 conda 配置环境，避免之后环境搞混。

执行过程：

- 机器上最初没有 `conda`、`mamba`、`micromamba`。
- 下载并安装 Miniconda 到：

  ```text
  /home/hit/ROS/.miniconda3
  ```

- 由于初次安装尝试会写 `/home/hit/.conda`，曾遇到只读文件系统问题；随后使用工作区内 HOME：

  ```text
  /home/hit/ROS/.conda-home
  ```

- 创建独立环境：

  ```text
  /home/hit/ROS/.conda-envs/data-clean
  ```

- 安装依赖：
  - `numpy`
  - `scipy`
  - `PyYAML`
  - `zstandard`
  - `opencv-contrib-python-headless`
  - 本地 editable 安装 `mcap`
  - 本地 editable 安装 `mcap-ros2-support`

已验证该环境中：

- Python 为 3.12。
- `cv2.aruco.detectMarkers` 不再 segfault。
- `mcap` 和 `mcap_ros2` 可导入。
- `data_clean` 可用该环境成功运行。

### 5. 首次 smoke test 成功

通过临时或后续正式配置，对首个真实 MCAP 成功完成清洗：

输入：

```text
/home/hit/ROS/mcap/2026-04-30_02:00:27.248201505.mcap
```

输出：

```text
/tmp/data_clean_smoke_output/2026-04-30_02:00:27.248201505.mcap
```

随后也验证过输出到：

```text
/home/hit/ROS/mcap_cleaned/2026-04-30_02:00:27.248201505.mcap
```

成功报告显示：

- 输入 topic 数：8
- 输出 topic 数：10
- 左 Baton 位姿：908 -> 908
- 右 Baton 位姿：922 -> 922
- 左 GoPro 图像帧：547，生成 `/gopro_left/gripper_width` 547 条
- 右 GoPro 图像帧：547，生成 `/gopro_right/gripper_width` 547 条

### 6. 第一个启动脚本版本

用户询问如何运行脚本后，先说明了长命令的含义：

```bash
PYTHONPATH=/tmp/data_clean_pydeps:src/data_clean:... \
python3 -m core.mcap_clean_batch --config config/data_clean_smoke_test.yaml
```

后来改为 conda 环境后，运行方式变为：

```bash
PYTHONPATH=/home/hit/ROS/src/data_clean \
/home/hit/ROS/.conda-envs/data-clean/bin/python \
  -m core.mcap_clean_batch \
  --config config/data_clean_smoke_test.yaml
```

用户要求写成启动脚本，于是新增：

```text
/home/hit/ROS/start_data_clean.sh
```

第一版 `start_data_clean.sh` 的作用：

- 自动定位工作区。
- 自动使用 `/home/hit/ROS/.conda-envs/data-clean/bin/python`。
- 自动设置 `PYTHONPATH=/home/hit/ROS/src/data_clean`。
- 默认使用 `config/data_clean_smoke_test.yaml`。
- 将原始 JSON 输出转换为中文摘要。

同时将 `config/data_clean_smoke_test.yaml` 的输出目录调整为：

```text
/home/hit/ROS/mcap_cleaned
```

### 7. 用户反馈：输出不易读且只处理一个文件

用户实际运行：

```bash
./start_data_clean.sh
```

观察到：

- 终端仍然能看到一整行 JSON，人工难以解读。
- `mcap_cleaned` 下仍然只有一个 MCAP。

排查结论：

- `mcap` 目录有 49 个 MCAP。
- `mcap_cleaned` 只有 1 个。
- 原因是配置文件当时仍写死：

  ```yaml
  file_glob: "2026-04-30_02:00:27.248201505.mcap"
  ```

修复：

- 将 `file_glob` 改为：

  ```yaml
  file_glob: "*.mcap"
  ```

- 将脚本输出改造成中文摘要，显示：
  - 成功/失败
  - 输入输出文件
  - topic 数
  - 位姿消息数
  - 夹爪宽度消息数
  - 插值帧数
  - 最终总计、成功、跳过、失败

### 8. 系统提问并制定交互完善方案

用户随后指出：数据清洗脚本功能，特别是与用户交互的逻辑还需要完善；第一点是用户选择清洗文件数量。

围绕交互逻辑进行了系统提问，并确定以下决策：

- 默认进入交互菜单。
- 默认清洗最近 1 个文件。
- “清洗 N 个文件”表示清洗最近 N 个 MCAP。
- 已有输出默认直接覆盖。
- 开始前显示计划并确认。
- 进度显示使用动态进度。
- 单个文件失败时继续处理后续文件。
- 输出目录固定为 `/home/hit/ROS/mcap_cleaned`。
- 不保存额外报告文件，只显示终端。
- 文件预览显示名称和大小。
- 启动前读取首个 MCAP 做 topic 检查。
- 当前测试 TCP/ArUco 参数启动前提醒，但不强制阻断。
- 支持 `--dry-run`。
- 菜单保持精简：
  - 回车：最近 1 个
  - `n`：最近 N 个
  - `a`：全部
  - `q`：退出
- 保留非交互参数：
  - `--latest 5`
  - `--all`
  - `--dry-run`
  - `--workers auto|N`
- 当前机器为 Intel Core Ultra 5 125H、18 逻辑 CPU、约 30GiB 内存、Intel Arc 核显；当前清洗主要走 CPU，不依赖 GPU。
- 默认最高性能并行数定为 6。
- 失败半成品输出默认删除。
- Ctrl+C 中断时保留已成功文件，清理未完成输出。

### 9. 实现交互式启动器

根据方案，新增：

```text
/home/hit/ROS/src/data_clean/core/mcap_clean_launcher.py
```

并将 `start_data_clean.sh` 简化为薄包装，只负责：

- 设置 conda Python。
- 设置 `PYTHONPATH`。
- 自动补默认 `--config`。
- 调用：

  ```bash
  python -m core.mcap_clean_launcher
  ```

`mcap_clean_launcher.py` 负责：

- 扫描输入目录。
- 按文件名时间倒序选择最近文件。
- 提供交互菜单。
- 支持非交互参数。
- 支持 dry-run。
- 显示清洗计划。
- 显示标定提醒。
- 检查首个 MCAP topic。
- 逐文件调用 `process_mcap_file`。
- 显示进度和中文摘要。
- 失败清理半成品输出。
- 支持 raw JSON 模式。

### 10. 交互式启动器验证结果

已验证命令：

```bash
bash -n start_data_clean.sh
```

通过。

已验证 Python 编译：

```bash
PYTHONPATH=/home/hit/ROS/src/data_clean \
/home/hit/ROS/.conda-envs/data-clean/bin/python \
  -m py_compile src/data_clean/core/mcap_clean_launcher.py
```

通过。

已验证 dry-run：

```bash
./start_data_clean.sh --dry-run --latest 5
```

结果：

- 正常显示最近 5 个文件。
- 显示文件大小和输出路径。
- 首个 MCAP topic 检查通过。
- 显示 TCP/ArUco 标定提醒。
- 不执行清洗。

已验证 raw JSON：

```bash
DATA_CLEAN_RAW_JSON=1 ./start_data_clean.sh --latest 1
```

结果：

- 成功清洗最近 1 个文件。
- 只输出一行原始 JSON，没有中文提示混入。

已验证普通中文摘要：

```bash
./start_data_clean.sh --latest 1
```

结果：

- 成功清洗最近 1 个文件。
- 显示动态进度。
- 显示中文摘要和最终汇总。

已验证交互菜单：

```bash
printf 'q\n' | ./start_data_clean.sh --dry-run
```

结果：

- 退出，不执行清洗。

```bash
printf '\n' | ./start_data_clean.sh --dry-run
```

结果：

- 默认选择最近 1 个。

```bash
printf 'n\n3\n' | ./start_data_clean.sh --dry-run
```

结果：

- 选择最近 3 个。

已验证全部文件 dry-run：

```bash
./start_data_clean.sh --dry-run --all --workers auto
```

结果：

- 选择 49 个文件。
- 并行数解析为 6。

已验证配置错误提示：

- 临时将一个 pose topic 改为不存在的 `/missing_pose_topic`。
- 运行 dry-run。
- 显示：

  ```text
  配置检查失败: missing configured pose topic "/missing_pose_topic"
  ```

### 11. 建立场景六文档体系

用户要求总结当前上下文，并生成场景六的一系列文件夹。

已使用 `ros-scene-docs` 技能，创建：

- `/home/hit/ROS/DOCS/场景六/目标描述.md`
- `/home/hit/ROS/DOCS/场景六/背景信息.md`
- `/home/hit/ROS/DOCS/场景六/执行约束.md`
- `/home/hit/ROS/DOCS/场景六/执行记录.md`
- `/home/hit/ROS/DOCS/场景六/当前进度.md`

同时更新：

- `/home/hit/ROS/AGENTS.md`
- `/home/hit/ROS/DOCS/public_rules.md`

场景六已被加入索引，并规定后续涉及 `src/data_clean` 或 `start_data_clean.sh` 时，要先阅读场景六五个固定文档。

### 12. 当前状态与未完成事项

当前状态：

- `data_clean` 代码可以在独立 conda 环境中运行。
- `start_data_clean.sh` 已成为推荐入口。
- 用户交互式文件数量选择已经实现。
- 单文件真实清洗已验证成功。
- 多文件选择和全量 dry-run 已验证。

仍未完成：

- 真实 TCP transform 参数尚未确认。
- 真实 ArUco marker id 和夹爪宽度标定范围尚未确认。
- 全部 49 个 MCAP 尚未执行最终完整清洗验收。
- 清洗后 MCAP 尚未被后续训练、分析或回放程序最终验收。

后续建议：

1. 先确认真实标定参数并更新 `config/data_clean_smoke_test.yaml`。
2. 运行：

   ```bash
   ./start_data_clean.sh --dry-run --all --workers auto
   ```

3. 小批量验证：

   ```bash
   ./start_data_clean.sh --latest 3 --workers 3
   ```

4. 最后执行全量清洗：

   ```bash
   ./start_data_clean.sh --all --workers auto
   ```

## 2026-05-03 13:24:15 CST：修复启动器找不到 MCAP 文件的问题

用户运行：

```bash
cd /home/hit/ROS
./start_data_clean.sh
```

终端显示：

```text
没有找到需要处理的 MCAP 文件。请检查 batch.input_dir 和 batch.file_glob。
```

排查结论：

- `config/data_clean_smoke_test.yaml` 中配置仍然正确：
  - `batch.input_dir: /home/hit/ROS/mcap`
  - `batch.output_dir: /home/hit/ROS/mcap_cleaned`
  - `batch.file_glob: "*.mcap"`
- 但 `/home/hit/ROS/mcap` 目录当时不存在，因此启动器确实无法扫描到输入文件。
- 在 `/home/hit/ROS/mcap_cleaned/mcap` 下发现 49 个 `.mcap` 文件。
- 抽样检查 `/home/hit/ROS/mcap_cleaned/mcap/2026-04-30_06:03:18.669272746.mcap`，该文件只有 8 个原始 topic，没有 `/gopro_left/gripper_width` 和 `/gopro_right/gripper_width`，说明它是原始输入 MCAP，不是清洗后 MCAP。
- 抽样检查 `/home/hit/ROS/mcap_cleaned/2026-04-30_06:03:18.669272746.mcap`，该文件有 10 个 topic，包含两个 gripper width topic，说明它才是清洗后输出。

修复动作：

```bash
mv /home/hit/ROS/mcap_cleaned/mcap /home/hit/ROS/mcap
```

修复后确认：

- `/home/hit/ROS/mcap` 下恢复 49 个 `.mcap` 原始输入文件。
- `/home/hit/ROS/mcap_cleaned/mcap` 已不存在，避免输出目录下继续混入原始输入目录。
- 同时更新 `src/data_clean/core/mcap_clean_launcher.py`，将“没有找到文件”的提示细化为：
  - 输入目录不存在。
  - 输入路径不是目录。
  - 输入目录存在但没有匹配到文件。
  - 并提示可用 `--input-dir` 指定真实目录。
- 执行：

  ```bash
  ./start_data_clean.sh --dry-run --latest 1
  ```

  结果：

  - 启动器显示可清洗文件 49 个。
  - 默认输出目录为 `mcap_cleaned`。
  - 最近 1 个文件预览正常。
  - 首个 MCAP topic 检查通过。
  - dry-run 模式未执行真实清洗。
- 额外用不存在的输入目录验证新提示：

  ```bash
  ./start_data_clean.sh --dry-run --latest 1 --input-dir /home/hit/ROS/not_existing_mcap_dir
  ```

  输出能明确显示“输入目录不存在”。

## 2026-05-03 13:53:11 CST：实现数据清洗配置/标定向导

用户要求按计划实现数据清洗配置自动标定向导，将手写 YAML 参数升级为“标定向导 + 自动诊断 + 生成已标定配置”。

本次代码改动：

- 扩展 `/home/hit/ROS/src/data_clean/core/mcap_process_config.py`：
  - `PoseStreamConfig` 支持可选 `transform`。
  - `AppConfig` 支持可选 `calibration` 元数据。
  - 新增 `config_is_calibrated` 判断 TCP 和 gripper 是否已标定。
- 更新 `/home/hit/ROS/src/data_clean/core/mcap_io.py`：
  - 位姿清洗时优先使用 `pose_streams[].transform`。
  - 如果某个 pose stream 没有独立 transform，则回退顶层 `transform`。
- 新增 `/home/hit/ROS/src/data_clean/core/mcap_calibration_wizard.py`：
  - 交互式选择夹爪完全闭合和完全张开的 MCAP。
  - 自动扫描 GoPro 图像 topic，识别 ArUco 字典、marker ID 和像素距离。
  - 自动生成左右 GoPro 的 `marker_min`、`marker_max`。
  - 询问左右 Baton Mini 位姿单位，并让用户输入 TCP x/z 尺量偏移。
  - 生成新配置 `/home/hit/ROS/config/data_clean_calibrated.yaml`，不覆盖 smoke 配置。
- 更新 `/home/hit/ROS/src/data_clean/core/mcap_clean_launcher.py`：
  - 新增 `--calibrate`。
  - 交互菜单新增 `c = 配置/标定向导`。
  - 启动时显示当前配置是否已标定。
  - 未标定配置会显示醒目中文警告。
- 更新 `/home/hit/ROS/start_data_clean.sh`：
  - 默认配置优先级改为：命令行 `--config` 或 `DATA_CLEAN_CONFIG` > `config/data_clean_calibrated.yaml` > `config/data_clean_smoke_test.yaml`。
  - `--help` 中加入 `--calibrate` 示例和配置优先级说明。

本次验证：

```bash
/home/hit/ROS/.conda-envs/data-clean/bin/python -m compileall src/data_clean/core
```

结果：通过。

```bash
./start_data_clean.sh --help
```

结果：显示 `--calibrate` 入口和配置优先级。

```bash
./start_data_clean.sh --dry-run --latest 1
```

结果：

- 使用 smoke 配置。
- 显示“未完整标定/测试配置”。
- 可扫描 49 个 MCAP。
- 首个 MCAP topic 检查通过。
- dry-run 未执行真实清洗。

真实单文件清洗验证：

```bash
rm -rf /tmp/data_clean_single_test
./start_data_clean.sh --latest 1 --workers 1 --output-dir /tmp/data_clean_single_test
```

结果：

- 清洗成功 1 个文件。
- 输入 topic 数：8。
- 输出 topic 数：10。
- `/gopro_left/gripper_width` 和 `/gopro_right/gripper_width` 均存在。
- 输出文件位于 `/tmp/data_clean_single_test/2026-04-30_06:03:18.669272746.mcap`。

```bash
printf 'q\n' | ./start_data_clean.sh --dry-run
```

结果：

- 菜单显示 `c 配置/标定向导`。
- 输入 `q` 可正常退出。

临时测试标定向导：

```bash
PYTHONPATH=/home/hit/ROS/src/data_clean \
/home/hit/ROS/.conda-envs/data-clean/bin/python -m core.mcap_calibration_wizard \
  --config /home/hit/ROS/config/data_clean_smoke_test.yaml \
  --output /tmp/data_clean_calibrated_test.yaml
```

使用现有两个 MCAP 做临时闭合/张开样本，结果：

- 左 GoPro 识别 `DICT_4X4_50`，marker `0/1`。
- 右 GoPro 识别 `DICT_4X4_50`，marker `0/1`。
- 临时配置 `/tmp/data_clean_calibrated_test.yaml` 可被 `load_app_config` 加载。
- 使用临时配置运行 `./start_data_clean.sh --config /tmp/data_clean_calibrated_test.yaml --dry-run --latest 1` 时显示“已标定”。

风险验证：

- 使用同一个 MCAP 同时作为闭合和张开样本时，向导会报“张开距离不大于闭合距离”，并阻止生成配置。
- `DATA_CLEAN_RAW_JSON=1 ./start_data_clean.sh --dry-run --latest 1` 不输出中文提示。

本次仍未完成：

- 尚未使用真实“完全闭合”和“完全张开”标定 MCAP 生成正式 `/home/hit/ROS/config/data_clean_calibrated.yaml`。
- 尚未输入真实 TCP 尺量偏移。
- 尚未执行全量 49 个 MCAP 的正式清洗验收。

## 2026-05-03 实时 GoPro 标定向导重设计实现

用户指出旧标定逻辑要求选择“夹爪闭合/张开 MCAP”，但 MCAP 中包含大量与标定无关的传感器数据，交互不适合小白用户。因此本次将标定入口重构为实时 GoPro 标定中心。

本次修改：

- 重写 `/home/hit/ROS/src/data_clean/core/mcap_calibration_wizard.py`：
  - 删除面向用户的闭合/张开 MCAP 选择流程。
  - 新增标定中心菜单：夹爪宽度实时标定、TCP 截图点选标定、查看当前标定状态、退出。
  - 新增 ROS2 实时图像订阅器，订阅 `/gopro_left/image_raw` 与 `/gopro_right/image_raw`，直接把 `sensor_msgs/msg/Image` 转为 OpenCV/Numpy 图像，不依赖 `cv_bridge`。
  - 新增 GoPro topic 检查和自动 GoPro-only 启动逻辑。
  - 新增实时 ArUco 检测、画面叠加、终端数字反馈、2 秒采样与质量门槛。
  - 夹爪标定顺序固定为左闭合、左张开、右闭合、右张开；每步要求至少 50 帧、成对 marker 检测率 >= 90%、距离波动 <= 3 px。
  - 新增 TCP 截图点选：Baton Mini 参考点、夹爪 TCP 点、比例尺点 A/B，按毫米比例尺计算 `tcp_offset.x/z`。
  - 支持部分保存配置；覆盖 `config/data_clean_calibrated.yaml` 前自动生成时间戳 `.bak`。
- 更新 `/home/hit/ROS/src/data_clean/core/mcap_process_config.py`：
  - 新增 `calibration_item_status`。
  - 新增 `calibration_missing_items`。
  - `config_is_calibrated` 改为只有左/右夹爪与左/右 TCP 四项全部完成才返回 true。
- 更新 `/home/hit/ROS/src/data_clean/core/mcap_clean_launcher.py`：
  - 未完整标定时显示具体未完成分项。
- 新增 GoPro-only 启动入口：
  - `/home/hit/ROS/start_gopro_only.sh`
  - `/home/hit/ROS/scripts/start_gopro_only.py`
  - 从 `config/all_sensor_nodes.yaml` 读取左右 GoPro 设备和 topic 参数，只启动左右 GoPro，不启动 Baton Mini、触觉或 Octopus。
  - 支持 `--sides left|right`，标定向导只会自动补启动缺失图像 topic 的那一侧，避免重复启动已经有画面的 GoPro。
- 更新 `/home/hit/ROS/start_data_clean.sh`：
  - 运行前 source ROS Jazzy 和工作区环境，保证进入标定时可订阅 ROS2 topic。
  - source ROS 环境时临时关闭 `set -u`，避免 ROS setup 中未定义变量导致脚本退出。
- 更新场景六文档：
  - 目标描述、背景信息、执行约束、当前进度均改为实时 GoPro 标定中心口径。

本次验证：

```bash
/home/hit/ROS/.conda-envs/data-clean/bin/python -m compileall src/data_clean/core
```

结果：通过。

```bash
python3 -m py_compile scripts/start_gopro_only.py
```

结果：通过。

```bash
./start_gopro_only.sh --help
```

结果：可显示 GoPro-only 启动脚本帮助。

```bash
./start_data_clean.sh --help
```

结果：可显示清洗启动器帮助，并保留 `--calibrate` 入口。

```bash
./start_data_clean.sh --dry-run --latest 1
```

结果：

- 可扫描 49 个 MCAP。
- 最近 1 个 MCAP 预览正常。
- 首个 MCAP topic 检查通过。
- 未完整标定时显示未完成分项：左手夹爪、右手夹爪、左手 TCP、右手 TCP。

```bash
DATA_CLEAN_RAW_JSON=1 ./start_data_clean.sh --dry-run --latest 1
```

结果：未输出中文提示，符合机器可读模式要求。

本次未做：

- 未实际启动 GoPro 节点做实时画面采样；该步骤需要用户现场连接 GoPro/采集卡并进行动作配合。
- 未生成正式 `/home/hit/ROS/config/data_clean_calibrated.yaml`。
- 未执行真实清洗或全量 49 个 MCAP 验收。

## 2026-05-03 实时标定 GoPro 子进程刷屏与中断修复

用户反馈：进入标定中心后选择 `1 夹爪宽度实时标定`，终端持续输出 `v4l2_camera_node` 的 `Error dequeuing buffer: No such device (19)`，并且按 Ctrl+C 无法干净停止。

现场排查：

- 发现残留了多套 GoPro-only / `ros2 launch gopro_camera_launch` / `v4l2_camera_node` 进程。
- 已只清理这些相机相关残留进程，未清理 Baton Mini、触觉节点或 Octopus。
- 根因判断：
  - 标定向导启动的 GoPro 子进程继承了主终端 stdout/stderr，底层 V4L2 错误会直接刷满交互界面。
  - Ctrl+C 后向导仍会询问是否保留 GoPro 子进程，错误刷屏导致用户看不到确认提示。
  - “按 Enter 开始采样”曾通过后台输入线程实现，异常路径下容易继续占用 stdin。
  - 图像订阅只判断“曾经收到过图像”，没有判断后续图像是否已经停止更新。

本次修改：

- 更新 `/home/hit/ROS/src/data_clean/core/mcap_calibration_wizard.py`：
  - 向导自动启动的 GoPro-only 子进程 stdout/stderr 改为写入 `/home/hit/ROS/log/data_clean_gopro_only_*.log`，不再刷主终端。
  - 启动 GoPro 后在终端只显示日志文件路径。
  - Ctrl+C 或标定异常时，自动停止本向导启动的 GoPro 子进程，不再询问是否保留。
  - 正常从菜单 `q` 退出时仍保留原逻辑：询问是否保留 GoPro 节点。
  - 实时图像增加“新鲜度”判断：超过 2 秒的旧帧不再当作有效图像。
  - 预览和采样阶段如果某侧 GoPro 超过 5 秒没有新图像，会报错退出当前标定，提示检查采集卡或相机连接。
  - 去掉后台输入线程，改为主循环非阻塞读取 Enter，Ctrl+C 更容易干净中断。

本次验证：

```bash
/home/hit/ROS/.conda-envs/data-clean/bin/python -m compileall src/data_clean/core
python3 -m py_compile scripts/start_gopro_only.py
```

结果：通过。

```bash
./start_data_clean.sh --dry-run --latest 1
```

结果：仍可扫描 49 个 MCAP，首个 MCAP topic 检查通过。

```bash
DATA_CLEAN_RAW_JSON=1 ./start_data_clean.sh --dry-run --latest 1 | wc -l
```

结果：输出 `0`，raw JSON 模式仍未混入中文提示。

```bash
./start_gopro_only.sh --help
```

结果：GoPro-only 启动脚本帮助正常。

当前残留检查：

```bash
pgrep -af 'start_gopro_only|gopro_pose_record|v4l2_camera_node' || true
```

结果：未发现相机节点残留。

## 2026-05-03 浏览器版实时 GoPro 标定中心实现

用户要求按“浏览器版实时 GoPro 标定中心执行方案”实现：不更换当前稳定的 `opencv-contrib-python-headless`，OpenCV 只做后台 ArUco 检测和画面叠加，浏览器负责实时预览、按钮控制、TCP 点选和状态展示。

本次修改：

- 重构 `/home/hit/ROS/src/data_clean/core/mcap_calibration_wizard.py`：
  - 新增基于标准库 `ThreadingHTTPServer` 的本地标定服务，不引入 FastAPI/Flask 依赖。
  - 默认从 `127.0.0.1:8765` 开始自动寻找空闲端口，并尝试自动打开浏览器。
  - 终端打印最终 URL；浏览器未自动打开时可手动访问。
  - 新增内嵌 HTML/CSS/JS 页面，页面只显示当前手单画面。
  - 新增 `/state`、`/frame.jpg`、`/snapshot.jpg` 和控制类 POST 接口。
  - 实时画面通过 OpenCV `imencode(".jpg")` 输出，不再调用 `cv2.imshow()`、`cv2.namedWindow()` 或 `cv2.waitKey()`。
  - 夹爪标定流程由浏览器按钮控制，固定顺序为左闭合、左张开、右闭合、右张开。
  - 每步采样 2 秒；不合格时停在当前步，只允许重采，不写坏参数。
  - 每完成一侧夹爪后立即写入 `config/data_clean_calibrated.yaml`，并自动备份旧配置。
  - TCP 点选改为浏览器截图点选，依次点击 Baton Mini 参考点、夹爪 TCP 点、比例尺点 A/B，并在页面显示点位编号。
  - TCP 写入前自动从最近 MCAP 抽样判断各 pose stream 单位，并按单位转换网页测得的毫米偏移。
  - 当前抽样结果：`/baton_mini_left/fast_odom` 判断为 `mm`，`/baton_mini_right/fast_odom` 判断为 `m`。
  - 浏览器关闭后，服务端心跳超时会自动退出标定中心。
  - 如果浏览器不可用，可在终端输入 `g` 回车进入夹爪宽度终端兜底；TCP 点选仍必须使用浏览器。
- 更新场景六文档：
  - `目标描述.md`
  - `背景信息.md`
  - `执行约束.md`
  - `当前进度.md`

本次验证：

```bash
/home/hit/ROS/.conda-envs/data-clean/bin/python -m compileall src/data_clean/core
python3 -m py_compile scripts/start_gopro_only.py
```

结果：通过。

```bash
PYTHONPATH=/home/hit/ROS/src/data_clean \
/home/hit/ROS/.conda-envs/data-clean/bin/python - <<'PY'
from core.mcap_calibration_wizard import _find_open_port, _html_page, _infer_pose_units
from core.mcap_process_config import load_app_config
config=load_app_config('/home/hit/ROS/config/data_clean_smoke_test.yaml')
print('port', _find_open_port())
html = _html_page()
print('html_checks', all(s in html for s in ['夹爪宽度实时标定', 'TCP 点选', 'snapshot']))
print('units', _infer_pose_units(config))
PY
```

结果：

- 端口自动选择返回 `8765`。
- 页面 HTML 包含夹爪标定、TCP 点选和截图点选元素。
- TCP 单位推断返回左手 `mm`、右手 `m`。

```bash
rg -n "cv2\\.imshow|namedWindow|waitKey|destroyWindow|destroyAllWindows" src/data_clean/core/mcap_calibration_wizard.py || true
```

结果：无匹配，确认标定向导不再使用 OpenCV GUI 窗口。

```bash
./start_data_clean.sh --help
./start_data_clean.sh --dry-run --latest 1
DATA_CLEAN_RAW_JSON=1 ./start_data_clean.sh --dry-run --latest 1 | wc -l
```

结果：

- 启动器帮助正常。
- dry-run 仍可扫描 49 个 MCAP，首个 MCAP topic 检查通过。
- raw JSON dry-run 输出行数为 `0`，未混入中文提示。

本次未做：

- 未实际连接 GoPro 做浏览器实时画面和点选硬件验收。
- 未生成正式 `config/data_clean_calibrated.yaml`。
- 未执行真实清洗或全量 49 个 MCAP 验收。

## 2026-05-03 夹爪标定最小采样帧数调整为 30 帧

用户反馈实时标定中“至少 50 帧”的要求偏高，希望改为 30 帧即可。

本次修改：

- 更新 `/home/hit/ROS/src/data_clean/core/mcap_calibration_wizard.py`：
  - `MIN_SAMPLE_FRAMES` 从 `50` 改为 `30`。
  - 其他质量门槛保持不变：成对 marker 检测率 >= 90%，距离波动 <= 3 px。
- 更新 `/home/hit/ROS/DOCS/场景六/背景信息.md` 中当前标定流程说明。

预期效果：

- 每步仍采样 2 秒。
- 如果 GoPro 实际帧率较低，只要采到 30 帧且检测质量合格，就允许通过当前闭合/张开步骤。
