# 执行记录

## 2026-05-06

- 操作：补充场景三输出程序与文件清单。
- 新增文档：`/home/hit/ROS/DOCS/场景三/输出程序与文件.md`。
- 内容：整理 8 路传感器启动、Octopus 配置、Octopus MCAP 录制器、触觉驱动和目标 MCAP 输出目录。
- 结果：已在 `当前进度.md` 中加入该清单索引。
- 说明：本次只修改 DOCS 文档，没有修改 ROS 节点、Octopus 代码、launch 或配置代码。

## 2026-04-27 14:32:38 CST

- 操作：根据用户要求新增场景三文档体系。
- 核对：已阅读 `/home/hit/ROS/DOCS/public_rules.md`、`/home/hit/ROS/AGENTS.md`、场景二固定文档和 `/home/hit/ROS/DOCS/Octopus_architecture.md`。
- 核对：已查看 `/home/hit/ROS/config/all_sensor_nodes.yaml`，确认当前相机侧默认目标 topic 为 `/baton_mini_right/odometry`、`/baton_mini_left/odometry`、`/gopro_right/image_raw`、`/gopro_left/image_raw`。
- 核对：已查看 `/home/hit/ROS/src/hwk_pressure_driver/config/pressure_sensors.yaml`，确认当前触觉侧默认目标 topic 为 `/pressure/left_hand/gripper_1` 与 `/pressure/left_hand/gripper_2`。
- 核对：已查看 Octopus `config.cpp` 与 `mcap-recorder.cpp`，确认录制 topic 来自 `recording.mcap.topics`，且当前 schema definition 显式支持范围不包含 `nav_msgs/msg/Odometry` 和 `hwk_pressure_interfaces/msg/PressureFrame`。
- 新增：`/home/hit/ROS/DOCS/场景三/目标描述.md`。
- 新增：`/home/hit/ROS/DOCS/场景三/背景信息.md`。
- 新增：`/home/hit/ROS/DOCS/场景三/执行约束.md`。
- 新增：`/home/hit/ROS/DOCS/场景三/执行记录.md`。
- 新增：`/home/hit/ROS/DOCS/场景三/当前进度.md`。
- 修改：`/home/hit/ROS/AGENTS.md`，加入场景三索引。
- 修改：`/home/hit/ROS/DOCS/public_rules.md`，加入场景三公共索引和 DOCS 管理规则。
- 备注：本次只创建和更新文档，未修改 Octopus、相机节点、触觉驱动或启动脚本代码。

## 2026-04-27 14:40:35 CST

- 操作：根据用户最新描述，校准场景三最终理想蓝图。
- 用户目标：运行 `start_all_sensor.sh` 后自动开启 6 路目标数据源；再运行一个脚本自动启动数采程序 Octopus；随后在 Octopus 图形化界面实时看到 6 路数据情况，并能直接选择 / 导出一些 topic 到一个总 `.mcap` 文件。
- 修改：更新 `/home/hit/ROS/DOCS/场景三/目标描述.md`，加入两个脚本入口、实时查看、GUI 选择 topic 和总 MCAP 导出目标。
- 修改：更新 `/home/hit/ROS/DOCS/场景三/背景信息.md`，新增“最终理想蓝图”和“Octopus 启动脚本背景”。
- 修改：更新 `/home/hit/ROS/DOCS/场景三/执行约束.md`，补充 `start_all_sensor.sh`、Octopus 独立启动脚本、ROS discovery 一致性和“6 个节点 / 6 路数据”的口径约束。
- 修改：更新 `/home/hit/ROS/DOCS/场景三/当前进度.md`，记录当前尚未完成 `start_all_sensor.sh` 启动触觉、Octopus 启动脚本、GUI topic 选择和 MCAP 验证。
- 备注：本次只更新文档，未修改脚本或代码。

## 2026-04-27 15:03:51 CST

- 操作：执行场景三推进任务，将 4 路相机启动链路扩展为 6 路目标 topic，并补齐 Octopus 启动入口。
- 已阅读：`/home/hit/ROS/DOCS/public_rules.md`、场景三固定文档、`/home/hit/ROS/DOCS/Octopus_architecture.md`、Baton Mini 架构与运行指南、GoPro 架构与运行指南。
- 新增：`/home/hit/ROS/launch/all_sensor_nodes.launch.py`，恢复总 launch 入口，并在 Baton Mini、GoPro 之外加入 `hwk_pressure_driver` 启动。
- 修改：`/home/hit/ROS/config/all_sensor_nodes.yaml`，加入 `pressure` 配置，指向 `/home/hit/ROS/src/hwk_pressure_driver/config/pressure_sensors.yaml`，默认验收 `/pressure/left_hand/gripper_1` 和 `/pressure/left_hand/gripper_2`。
- 修改：`/home/hit/ROS/scripts/all_sensor_status.py`，加入触觉驱动 preflight 检查、串口可访问性检查和 postlaunch topic 验收。
- 修改：`/home/hit/ROS/start_all_sensor.sh`，修复 `fio` 拼写错误，并在默认 local-only 模式下强制 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST`。
- 新增：`/home/hit/ROS/start_octopus.sh`，作为 Octopus 独立启动入口；默认设置 Octopus 与传感器脚本同为 `LOCALHOST` discovery 范围。
- 新增：`/home/hit/ROS/scripts/configure_octopus_scanner.py`，用于写入 `/home/hit/.config/scanner.json`，将录制 topic 固定为场景三 6 个目标 topic，MCAP 输出目录设为 `/home/hit/ROS/mcap`。
- 修改：Octopus `/home/hit/ROS/src/VTLA_octopus-master/octopus/src/utils/config.cpp`，修复 `recording.mcap` 配置读取判断，并将空配置默认 topic 改为场景三 6 个目标 topic。
- 修改：Octopus `/home/hit/ROS/src/VTLA_octopus-master/octopus/src/pages/op-panel.cpp`，在 Operation 面板显示可勾选 topic 列表，录制时只订阅被勾选的 topic。
- 修改：Octopus `/home/hit/ROS/src/VTLA_octopus-master/octopus/src/mcap-recorder.cpp`，增加 `nav_msgs/msg/Odometry` 与 `hwk_pressure_interfaces/msg/PressureFrame` 的 MCAP schema definition，并在没有可用 topic 或文件打开失败时返回失败。
- 构建：执行 `colcon build --packages-select hwk_pressure_interfaces hwk_pressure_driver octopus`，使用本地 FFmpeg 8 与 Qt 6.11 环境变量，结果 3 个包构建成功；Octopus 仅出现既有 CMake/Shader 警告。
- 配置验证：执行 `python3 /home/hit/ROS/scripts/configure_octopus_scanner.py --config /home/hit/.config/scanner.json --recording-path /home/hit/ROS/mcap`，确认 `scanner.json` 已包含 6 个目标 topic。
- 启动前检查：执行 `python3 /home/hit/ROS/scripts/all_sensor_status.py preflight --config /home/hit/ROS/config/all_sensor_nodes.yaml`，Baton Mini、GoPro、两个触觉串口均通过检查。
- 总启动验证：执行 `timeout 60s env STARTUP_WAIT=25 /home/hit/ROS/start_all_sensor.sh`，日志文件为 `/home/hit/ROS/log/start_all_sensor/20260427_150026.log`。
- 总启动结果：postlaunch 检查通过，`/baton_mini_right/odometry`、`/baton_mini_left/odometry`、`/gopro_right/image_raw`、`/gopro_left/image_raw`、`/pressure/left_hand/gripper_1`、`/pressure/left_hand/gripper_2` 全部可见；输出为 `5/5 个节点全部通过检查`。
- 触觉观察：日志中两个触觉串口均打开成功，并出现 `First valid pressure frame received`，说明两个触觉 topic 有真实有效帧。
- 清理确认：`timeout` 后已停止总 launch，随后检查未发现残留 Baton Mini、GoPro、pressure 或总 launch 进程，LOCALHOST ROS graph 为空。
- Octopus 短测：执行 `timeout 8s /home/hit/ROS/start_octopus.sh`，确认 Octopus 可启动并读取场景三 `scanner.json`；短测后手动终止残留 Octopus 进程并执行 `ros2 daemon stop`。
- 注意：短测中 Octopus GUI 启动成功，但未执行人工 GUI 点击开始 / 停止采集，因此本轮尚未生成 `.mcap` 文件，也尚未执行 `ros2 bag info` 内容验证。

## 2026-04-27 15:44:20 CST

- 操作：根据用户最新要求，补充场景三第二阶段目标。
- 用户目标：场景三第二阶段在原 6 路目标 topic 基础上，再增加 2 个触觉传感器 topic；一键运行数量由 6 路变为 8 路，其他目标保持不变。
- 核对：已阅读 `/home/hit/ROS/DOCS/public_rules.md`、场景三固定文档和 `/home/hit/ROS/DOCS/Octopus_architecture.md`。
- 核对：已查看 `/home/hit/ROS/src/hwk_pressure_driver/config/pressure_sensors.yaml`，其中已预留右手触觉 topic 模板：`/pressure/right_hand/gripper_1` 与 `/pressure/right_hand/gripper_2`。
- 修改：`/home/hit/ROS/AGENTS.md`，将场景三索引标题从 6 个目标 topic 更新为 8 个目标 topic。
- 修改：`/home/hit/ROS/DOCS/public_rules.md`，将公共目标索引中的场景三 topic 数量更新为 8 个，并明确为 2 个 Baton Mini、2 个 GoPro 和 4 个触觉传感器 topic。
- 修改：`/home/hit/ROS/DOCS/场景三/目标描述.md`，将第二阶段核心目标、目标 topic 表、启动流程和成功标准更新为 8 路。
- 修改：`/home/hit/ROS/DOCS/场景三/背景信息.md`，补充第二阶段新增的右手两个触觉 topic，并记录当前 6 路配置后续需要扩展到 8 路。
- 修改：`/home/hit/ROS/DOCS/场景三/执行约束.md`，将默认验收 topic、启动边界、数据口径和 Octopus 录制配置约束更新为 8 路。
- 修改：`/home/hit/ROS/DOCS/场景三/当前进度.md`，记录第一阶段 6 路已短测通过，第二阶段新增 2 路触觉 topic 尚待配置和实测。
- 备注：本次只更新文档体系，未修改启动脚本、ROS 配置、触觉驱动配置或 Octopus 代码。

## 2026-04-27 16:05:55 CST

- 操作：用户反馈 8 个硬件已接线后，继续推进场景三第二阶段 8 路启动检查。
- 已阅读：`/home/hit/ROS/DOCS/public_rules.md`、`/home/hit/ROS/DOCS/场景三/当前进度.md`、`/home/hit/ROS/DOCS/Octopus_architecture.md`、Baton Mini 架构与运行指南、GoPro 架构与运行指南。
- 修改：`/home/hit/ROS/config/all_sensor_nodes.yaml`，将触觉目标 topic 从 2 路扩展为 4 路：左手 2 路 + 右手 2 路。
- 修改：`/home/hit/ROS/src/hwk_pressure_driver/config/pressure_sensors.yaml`，启用右手两个触觉传感器配置；其中 `right_gripper_1_port` 暂用当前枚举到的稳定 by-path `/dev/serial/by-path/pci-0000:00:14.0-usb-0:2.4.1.1:1.0-port0`。
- 修改：`/home/hit/ROS/scripts/configure_octopus_scanner.py`，默认写入 8 个场景三目标 topic。
- 修改：Octopus `/home/hit/ROS/src/VTLA_octopus-master/octopus/src/utils/config.cpp`，将空配置默认录制 topic 扩展为 8 个场景三目标 topic。
- 配置验证：执行 `python3 /home/hit/ROS/scripts/configure_octopus_scanner.py --config /home/hit/.config/scanner.json --recording-path /home/hit/ROS/mcap`，确认 `/home/hit/.config/scanner.json` 已包含 8 个目标 topic。
- 设备枚举：当前两台 Baton Mini 网络均可达，`192.168.1.10` 和 `192.168.2.10` 的 SDK 端口检查通过。
- 设备枚举：当前两路 GoPro 采集卡实际 by-path 为 `/dev/v4l/by-path/pci-0000:00:0d.0-usb-0:3.4.1.3:1.0-video-index0` 与 `/dev/v4l/by-path/pci-0000:00:0d.0-usb-0:3.4.1.4:1.0-video-index0`。
- 修改：`/home/hit/ROS/config/all_sensor_nodes.yaml`，将 GoPro right/left 的 `video_device` 更新为当前实际存在的两个 by-path。
- 设备枚举：当前系统只发现 3 个 CH340 触觉串口：左手两个稳定 symlink，以及新增 `/dev/ttyUSB2`；`lsusb` 也只显示 3 个 `1a86:7523 QinHeng Electronics CH340 serial converter`。
- 修改：`/home/hit/ROS/config/99-hwk-pressure.rules`，记录当前新增 CH340 的 ID_PATH，并为 `hwk_pressure_right_gripper_1` 预置 udev 规则；右手 gripper 2 仍等待第四个 CH340 的真实 ID_PATH。
- 构建：执行 `colcon build --packages-select octopus`，结果构建成功；仅出现既有 CMake/Shader/Qt moc 警告。
- 启动前检查：执行 `python3 /home/hit/ROS/scripts/all_sensor_status.py preflight --config /home/hit/ROS/config/all_sensor_nodes.yaml`。
- 检查结果：Baton Mini right、Baton Mini left、GoPro right、GoPro left、左手两个触觉串口、右手 gripper 1 串口均通过；仅 `right_gripper_2_port` 失败，原因是 `/dev/hwk_pressure_right_gripper_2` 不存在。
- 阻塞结论：场景三 8 路正式启动当前被第四个触觉串口阻塞；需要先让右手 gripper 2 对应的 CH340 在系统中枚举出来，再继续执行 `start_all_sensor.sh`。

## 2026-04-27 16:16:24 CST

- 操作：用户确认 `/dev/ttyUSB0` 到 `/dev/ttyUSB3` 均已出现后，继续排查右手 gripper 2 触觉串口。
- 核对：`/dev/ttyUSB3` 的 `ID_PATH` 为 `pci-0000:00:14.0-usb-0:2.4.4.2:1.0`，对应 by-path 为 `/dev/serial/by-path/pci-0000:00:14.0-usb-0:2.4.4.2:1.0-port0`。
- 修改：`/home/hit/ROS/src/hwk_pressure_driver/config/pressure_sensors.yaml`，将 `right_gripper_2_port` 改为当前稳定 by-path。
- 修改：`/home/hit/ROS/config/99-hwk-pressure.rules`，补充 `hwk_pressure_right_gripper_2` 的 udev 稳定命名规则。
- 启动前检查：执行 `python3 /home/hit/ROS/scripts/all_sensor_status.py preflight --config /home/hit/ROS/config/all_sensor_nodes.yaml`，结果通过；2 个 Baton Mini、2 个 GoPro、4 个触觉串口全部 OK。
- 总启动验证：执行 `timeout 75s env STARTUP_WAIT=30 /home/hit/ROS/start_all_sensor.sh`，日志文件为 `/home/hit/ROS/log/start_all_sensor/20260427_161444.log`。
- 总启动结果：postlaunch 检查通过，输出为 `5/5 个节点全部通过检查`；其中 `HWK pressure` 为 `topics=4/4`。
- 触觉观察：日志中 4 个触觉串口均打开成功，并全部出现 `First valid pressure frame received`：
  - `/pressure/left_hand/gripper_1`
  - `/pressure/left_hand/gripper_2`
  - `/pressure/right_hand/gripper_1`
  - `/pressure/right_hand/gripper_2`
- 目标 topic 验证：本轮 8 个场景三目标 topic 均已通过 postlaunch 检查：
  - `/baton_mini_right/odometry`
  - `/baton_mini_left/odometry`
  - `/gopro_right/image_raw`
  - `/gopro_left/image_raw`
  - `/pressure/left_hand/gripper_1`
  - `/pressure/left_hand/gripper_2`
  - `/pressure/right_hand/gripper_1`
  - `/pressure/right_hand/gripper_2`
- 清理确认：`timeout` 后总 launch 已停止；检查未发现残留 Baton Mini、GoPro、pressure 或 Octopus 进程，LOCALHOST ROS graph 仅剩系统基础 topic。
- 注意：本轮仍未进入 Octopus GUI 人工开始 / 停止采集，因此尚未生成 `.mcap` 文件，也尚未执行 `ros2 bag info` 内容验证。

## 2026-04-27 17:09:51 CST

- 操作：用户执行场景三验证时运行 `/home/hit/ROS/start_all_sensor.sh`，启动前检查失败，未进入传感器 launch 和 topic 验证阶段。
- 用户提示：另一个终端做 ROS topic 验证时，需要先执行 `ros2 daemon stop`，再使用 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST ros2 topic list --no-daemon`；总配置文件为 `/home/hit/ROS/config/all_sensor_nodes.yaml`。
- 启动前检查结果：Baton Mini right/left、GoPro right/left、左手两个触觉串口均 OK。
- 失败项：`right_gripper_1_port` 对应 `/dev/serial/by-path/pci-0000:00:14.0-usb-0:2.4.1.1:1.0-port0` 不存在。
- 失败项：`right_gripper_2_port` 对应 `/dev/serial/by-path/pci-0000:00:14.0-usb-0:2.4.4.2:1.0-port0` 不存在。
- 本机复核：当前 `/dev/serial/by-path` 只枚举出两个 CH340 串口，分别对应 `/dev/ttyUSB0` 和 `/dev/ttyUSB1`。
- 本机复核：当前只存在 `/dev/hwk_pressure_left_gripper_1 -> ttyUSB0` 和 `/dev/hwk_pressure_left_gripper_2 -> ttyUSB1`，没有 `/dev/ttyUSB2`、`/dev/ttyUSB3`，也没有右手两个 udev symlink。
- 临时结论：本轮场景三验证被右手两个触觉传感器串口未枚举阻塞；应先检查右手两个 CH340/USB 线/集线器插口是否接好，恢复 `/dev/ttyUSB2` 和 `/dev/ttyUSB3` 或对应 by-path 后，再重新执行 `./start_all_sensor.sh`。

## 2026-04-27 17:16:37 CST

- 操作：继续分析并处理右手两个触觉串口找不到的问题。
- 核对：`lsusb` 和 `lsusb -t` 当前只看到 2 个 `1a86:7523 QinHeng Electronics CH340 serial converter`，均已由 `ch341` 驱动绑定；系统没有看到右手两个 CH340。
- 核对：当前存在的两个 CH340 分别是 `3-2.4.3.2.2` 和 `3-2.4.3.2.4`，对应左手两个触觉串口。
- 内核日志结论：`right_gripper_1` 曾在 15:56:28 以 `3-2.4.1.1` 枚举为 `ttyUSB2`；`right_gripper_2` 曾在 16:09:36 / 16:12:51 以 `3-2.4.4.2` 枚举为 `ttyUSB3`。
- 内核日志结论：两个右手 CH340 分别在 16:30:38 和 16:30:34 出现 `USB disconnect`，之后未再重新枚举。
- 操作：尝试用 `usbreset` reset `right_gripper_1` 所在父级 hub，结果当前普通用户无权限打开 `/dev/bus/usb` 设备，输出 `can't open [Permission denied]`；本轮无法由当前会话直接执行 root 级 USB hub reset。
- 发现：`/etc/udev/rules.d/99-hwk-pressure.rules` 仍只有左手两条规则，和仓库 `/home/hit/ROS/config/99-hwk-pressure.rules` 不一致；仓库规则已包含右手两条规则，但尚未安装到 `/etc`。
- 新增：`/home/hit/ROS/scripts/hwk_pressure_usb_recover.sh`，用于诊断当前 CH340 数量、expected by-path、udev 规则是否安装，并在 root 权限下支持安装 udev 规则和 reset 右手父级 USB hub。
- 诊断脚本结果：普通用户运行 `/home/hit/ROS/scripts/hwk_pressure_usb_recover.sh`，输出 `count=2 expected=4`，缺失：
  - `/dev/serial/by-path/pci-0000:00:14.0-usb-0:2.4.1.1:1.0-port0`
  - `/dev/serial/by-path/pci-0000:00:14.0-usb-0:2.4.4.2:1.0-port0`
- 当前结论：这不是 ROS、launch 或配置解析问题，而是右手两个 CH340 已从 USB 总线掉线；需要先让这两个设备重新枚举，再继续 8 路启动验证。

## 2026-04-27 17:29:45 CST

- 操作：用户启动 Octopus 后点击开始录制，Octopus 录制器发现 8 个目标 topic，但对两个 GoPro 图像 topic 输出 QoS 不兼容警告。
- 用户输出：`/gopro_left/image_raw` 和 `/gopro_right/image_raw` 均提示 `offering incompatible QoS`，`Last incompatible policy: RELIABILITY_QOS_POLICY`，并提示不会发送消息给 recorder。
- 复核：执行 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST ros2 topic info /gopro_left/image_raw -v` 和 `/gopro_right/image_raw -v`，确认两个 GoPro publisher 的 Reliability 均为 `BEST_EFFORT`。
- 复核：执行 `ros2 topic info /baton_mini_right/odometry -v` 和 `/pressure/left_hand/gripper_1 -v`，确认 Baton Mini odometry 与压力 topic publisher 的 Reliability 为 `RELIABLE`。
- 修改：`/home/hit/ROS/src/VTLA_octopus-master/octopus/src/mcap-recorder.cpp`，新增按 topic publisher QoS 自动选择订阅 QoS 的逻辑；当发现任一 publisher 是 `BEST_EFFORT` 时，recorder 使用 Best Effort 订阅，否则保持 Reliable。
- 修改：`/home/hit/ROS/src/VTLA_octopus-master/octopus/src/mcap-recorder.h`，新增 `qos_for_topic()` 私有方法声明。
- 修复：将 MCAP 录制回调中的 `topic` 从引用捕获改为值捕获，避免 `create_subscription()` 返回后回调持有悬空引用。
- 构建：执行 `colcon build --packages-select octopus`，构建成功；仅出现既有 `/media/media.h` 相关警告。
- 验证：安装二进制 `/home/hit/ROS/install/octopus/lib/octopus/octopus` 已在 17:28 更新。
- 验证：执行 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST python3 /home/hit/ROS/scripts/all_sensor_status.py postlaunch --config /home/hit/ROS/config/all_sensor_nodes.yaml`，结果 `5/5 个节点全部通过检查`，其中 `HWK pressure topics=4/4`。
- 注意：当前 PID `69270` 仍是 17:24 启动的旧 Octopus 进程；QoS 修复需要关闭旧 Octopus 后重新运行 `/home/hit/ROS/start_octopus.sh` 才会生效。

## 2026-04-28 17:16:54 CST

- 操作：根据用户反馈“Octopus 打开时显示 FPS 约 40，开始录制 MCAP 后显示 FPS 约 10，且 `ros2 topic hz /gopro_right/image_raw` 从 30 慢慢下降”，排查录制时帧率不稳原因。
- 核对：已阅读 `/home/hit/ROS/DOCS/public_rules.md`、场景三固定文档、`/home/hit/ROS/DOCS/Octopus_architecture.md`、GoPro 架构文档和 GoPro 运行指南。
- MCAP 验证：执行 `ros2 bag info /home/hit/ROS/mcap/2026-04-28_09:08:14.204096036.mcap`。
- MCAP 结果：文件大小 `917.2 MiB`，duration `13.604631762s`，总消息数 `5353`，8 个目标 topic 均有消息。
- GoPro 计数：`/gopro_left/image_raw` 为 `817` 帧，约 `60.05Hz`；`/gopro_right/image_raw` 为 `815` 帧，约 `59.91Hz`。
- 触觉计数：四个压力 topic 均约 `63.4-63.5Hz`；Baton Mini 左右 odometry 约 `9.7-9.9Hz`。
- 关键判断：当前录制器并没有只写入 10fps；相反，两路 GoPro 正在按接近 60fps 进入 MCAP。Octopus 界面 FPS 和额外 `ros2 topic hz` 订阅者在录制时下降，更像是显示链路/测速订阅者受到录制时 CPU、DDS 传输、Zstandard 压缩和写盘压力影响。
- 配置核对：当前 `gopro_camera.yaml` 使用 `time_per_frame: [1, 60]`，同时写有 `publish_rate: 30.0`。
- 本地包核对：当前安装的 `ros-jazzy-v4l2-camera 0.7.1` 的本地参数头文件、二进制字符串和 ROS Index 参数说明均未显示 `publish_rate` 参数；`publish_rate: 30.0` 不能作为当前节点的有效锁帧依据。
- 修改：`/home/hit/ROS/src/gopro_camera_launch/config/gopro_camera.yaml`，将 GoPro `time_per_frame.denominator` 从 `60` 改回 `30`，并移除无效的 `publish_rate` 配置项。
- 修改：`/home/hit/ROS/learning/gopro_framerate_optimization_log.md`，补充本次 MCAP 反推结论，并修正“使用 publish_rate 锁帧”的错误记录。
- 构建：执行 `colcon build --packages-select gopro_camera_launch`，结果构建成功。
- 安装验证：`/home/hit/ROS/install/gopro_camera_launch/share/gopro_camera_launch/config/gopro_camera.yaml` 已同步为 `time_per_frame.denominator: 30`，且不再包含 `publish_rate`。
- 临时结论：优先把源头 GoPro 发布频率降回 30fps，再重新构建 `gopro_camera_launch` 并实测；如果录制时 UI FPS 仍明显下降，再考虑把 Octopus MCAP 压缩从 Zstandard 改为 None/Lz4 或把录制写盘改成异步队列。

## 2026-04-28 17:28:30 CST

- 操作：继续解释“已设置 30fps，但新 MCAP 仍为 60fps”的原因。
- 核对：`/home/hit/ROS/src/gopro_camera_launch/config/gopro_camera.yaml` 和安装目录 `/home/hit/ROS/install/gopro_camera_launch/share/gopro_camera_launch/config/gopro_camera.yaml` 均已是 `time_per_frame.denominator: 30`。
- 运行态核对：当前没有 `v4l2_camera_node`、Octopus 或 `start_all_sensor.sh` 残留进程。
- 驱动层核对：执行 `v4l2-ctl -d /dev/video4 --get-fmt-video --get-parm` 与 `/dev/video6`，两张采集卡当前都报告 `640x480 YUYV` 且 `Frames per second: 60.000 (60/1)`。
- 本地包核对：`strings /opt/ros/jazzy/lib/libv4l2_camera.so /opt/ros/jazzy/lib/v4l2_camera/v4l2_camera_node` 只发现 `output_encoding`、`pixel_format`、`image_size`，未发现 `time_per_frame`、`publish_rate` 或相关帧率参数名。
- 本地头文件核对：`/opt/ros/jazzy/include/v4l2_camera/parameters.hpp` 只暴露 `getOutputEncoding()`、`getPixelFormat()`、`getImageSize()` 等接口，未发现 `time_per_frame` getter。
- 关键结论：当前 `ros-jazzy-v4l2-camera 0.7.1` 实际不会从 YAML 中消费 `time_per_frame` 或 `publish_rate` 来限帧；所以 YAML 写了 30fps，但采集卡仍保持 60fps，Octopus MCAP 也就录到 60fps。
- 实测：在空闲设备上执行 `v4l2-ctl -d /dev/video4 --set-parm=30 --get-parm` 和 `/dev/video6`，两张采集卡均成功变为 `Frames per second: 30.000 (30/1)`。
- 修改：`/home/hit/ROS/src/gopro_camera_launch/launch/gopro_pose_record.launch.py`，新增 `frame_rate` launch 参数，默认 `30`；启动 `v4l2_camera_node` 前先执行 `v4l2-ctl -d <video_device> --set-parm <frame_rate>`，并等待该进程退出后再启动相机节点。
- 修改：`/home/hit/ROS/config/all_sensor_nodes.yaml`，为 `gopro.right` 和 `gopro.left` 显式增加 `frame_rate: 30`。
- 修改：`/home/hit/ROS/launch/all_sensor_nodes.launch.py`，将 GoPro `frame_rate` 配置透传给 `gopro_pose_record.launch.py`。
- 修改：`/home/hit/ROS/src/gopro_camera_launch/package.xml`，新增 `v4l-utils` 运行依赖。
- 修改：`/home/hit/ROS/scripts/all_sensor_status.py`，GoPro 启动前检查中如果缺少 `v4l2-ctl` 则直接失败，避免无法设置帧率时悄悄启动。
- 修改：更新 GoPro 架构文档、运行指南和帧率优化记录，记录当前实机以 launch 前置 `v4l2-ctl --set-parm` 为准。
- 构建：执行 `colcon build --packages-select gopro_camera_launch`，构建成功。
- 验证：安装目录 `/home/hit/ROS/install/gopro_camera_launch/share/gopro_camera_launch/launch/gopro_pose_record.launch.py` 已包含 `v4l2-ctl`、`frame_rate` 和 `OnProcessExit`。
- 验证：执行 `python3 -m py_compile` 检查 `gopro_pose_record.launch.py`、`all_sensor_nodes.launch.py` 和 `all_sensor_status.py`，均通过。
- 验证：执行 `v4l2-ctl -d /dev/video4 --get-parm` 与 `/dev/video6`，两张采集卡当前均为 `Frames per second: 30.000 (30/1)`。
- 验证：执行 `python3 /home/hit/ROS/scripts/all_sensor_status.py preflight --config /home/hit/ROS/config/all_sensor_nodes.yaml`，启动前检查通过。

## 2026-04-28 17:40:17 CST

- 操作：根据用户反馈 “Octopus Right GoPro UI 不显示”，回到场景三数据源链路排查 `/gopro_right/image_raw` 是否正常发布。
- 复核：`hardware_identity_resolved.yaml` 和 `/home/hit/ROS/config/all_sensor_nodes.yaml` 中 right/left GoPro 设备映射正确；right 为 `...usb-0:3.4.1.3...`，left 为 `...usb-0:3.4.1.4...`。
- 日志定位：此前启动日志中两个 GoPro 节点实际都串成了 left 参数，出现重复 `/gopro_left/gopro_left_camera`，并有一个节点报 `Device or resource busy (16)`。
- 根因：同一个 `gopro_pose_record.launch.py` 被总 launch include 两次；新增前置 `v4l2-ctl` 后，相机节点通过 `OnProcessExit` 延迟启动，导致 `LaunchConfiguration` 在延迟动作中暴露出 right/left 参数作用域串扰。
- 修改：`/home/hit/ROS/launch/all_sensor_nodes.launch.py` 中每个 GoPro include 外层增加 `GroupAction(scoped=True, forwarding=False)`。
- 修改：`/home/hit/ROS/src/gopro_camera_launch/launch/gopro_pose_record.launch.py` 改为用 `OpaqueFunction` 在当前 include 上下文中立即解析所有 launch 参数，再创建 `v4l2-ctl` 和延迟启动的 `v4l2_camera_node`。
- 构建：执行 `colcon build --packages-select gopro_camera_launch`，构建成功。
- 语法检查：执行 `python3 -m py_compile` 检查 `gopro_pose_record.launch.py` 和 `all_sensor_nodes.launch.py`，均通过。
- 短测：执行 `timeout 55s env AUTO_BUILD=0 STARTUP_WAIT=18 /home/hit/ROS/start_all_sensor.sh`，日志为 `/home/hit/ROS/log/start_all_sensor/20260428_173834.log`。
- 短测结果：两个 `v4l2-ctl` 都输出 `Frame rate set to 30.000 fps`。
- 短测结果：Right GoPro 使用 `usb-0000:00:0d.0-3.4.1.3`，节点 `/gopro_right/gopro_right_camera`，topic 检查 `1/1`。
- 短测结果：Left GoPro 使用 `usb-0000:00:0d.0-3.4.1.4`，节点 `/gopro_left/gopro_left_camera`，topic 检查 `1/1`。
- 总启动结果：postlaunch 输出 `5/5 个节点全部通过检查`。
- 清理确认：`timeout` 后未发现残留 `v4l2_camera_node`、Octopus 或 `start_all_sensor.sh` 进程。
- 当前结论：Right GoPro UI 不显示的源头发布问题已修复；下一步需要重新启动 Octopus GUI 做画面和 MCAP 录制回归。
