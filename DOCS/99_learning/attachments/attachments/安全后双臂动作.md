---
tags:
  - 附件
  - pi05
  - deploy
---

# 安全后双臂动作

> [!abstract]
> 通过安全检查后可以发布的单步双臂控制目标

## 基本信息

| 属性 | 值 |
| --- | --- |
| 变量名 | `BimanualAction, result.action, command.action` |
| 参考系 | 不适用；这是程序数据流节点，不是几何坐标变换结果 |
| 相对原点 | 不适用 |
| 物理锚点 | 物理对象控制目标 |
| 阶段属性 | 最终发布前 |
| 是否最终输出 | 是 |
| 数据类型 | `BimanualAction dataclass` |
| 数据结构 | 左臂 6D + 右臂 6D + 左手 1D + 右手 1D 的结构化单步动作 |
| 所在文件 | `pi05_test/pi05/deploy/src/pi05/deploy/runtime/safety_guard.py:35-73; common/robot/action_spec.py:22-39` |
| 现实含义 | 通过安全检查后可以发布的单步双臂控制目标 |

## 关键澄清

### 1. 它在哪个参考系下？

不适用。这里的“参考系”不是机器人空间坐标系，而是程序流水线中的数据阶段。

### 2. 它相对哪个原点？

不适用。它不是空间点或位姿变换结果。

### 3. 它对应哪个物理点 / 物理对象？

物理对象控制目标。如果它是缓存、请求或 batch，则无对应物理点，这是纯数学对象 / 程序容器。

### 4. 它是不是最终输出？

是。

### 5. 它不是什么？

它不应被误解为“整个推理程序的最终命令”。只有 ROS 控制命令输出才是下游执行系统真正收到的命令。

## 对应源码

```python
return SafetyResult(action=BimanualAction(left_arm=left, right_arm=right, left_hand=..., right_hand=...), accepted=True)
```

## 一句话说清楚

> 通过安全检查后可以发布的单步双臂控制目标

## 在数据流中的位置

- 上游：单步14D候选动作
- 下游：ROS控制命令输出
