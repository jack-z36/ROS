# 执行约束

## 必读文档

每次处理场景六前，必须先阅读：

- `/home/hit/ROS/DOCS/public_rules.md`
- `/home/hit/ROS/AGENTS.md`
- `/home/hit/ROS/DOCS/场景六/目标描述.md`
- `/home/hit/ROS/DOCS/场景六/背景信息.md`
- `/home/hit/ROS/DOCS/场景六/执行约束.md`
- `/home/hit/ROS/DOCS/场景六/执行记录.md`
- `/home/hit/ROS/DOCS/场景六/当前进度.md`

涉及 Octopus MCAP 录制链路时，还必须阅读：

- `/home/hit/ROS/DOCS/Octopus_architecture.md`

## 环境约束

- 默认使用 `/home/hit/ROS/.conda-envs/data-clean/bin/python`。
- 不应依赖系统 Python 里的 OpenCV、MCAP 或 ROS2 Python 包状态。
- 不应要求用户手工设置长 `PYTHONPATH`，应通过 `./start_data_clean.sh` 完成。
- 如果更新依赖，应优先保持在 `data-clean` conda 环境内，避免污染 ROS 工作区环境。

## 配置边界

- 默认配置文件为 `/home/hit/ROS/config/data_clean_smoke_test.yaml`。
- `batch.input_dir` 默认指向 `/home/hit/ROS/mcap`。
- `batch.output_dir` 默认指向 `/home/hit/ROS/mcap_cleaned`。
- `batch.file_glob` 保持 `*.mcap`，具体数量由交互式启动器控制。
- 不应把临时测试配置作为长期配置散落在 `/tmp` 或仓库根目录。

## 清洗行为约束

- 默认清洗最近 1 个文件。
- “最近 N 个”按 MCAP 文件名中的时间从新到旧选择。
- 默认覆盖同名输出。
- 如果某个文件失败，应删除该文件对应的半成品输出。
- 如果用户中断，应保留已成功文件，清理未完成输出。
- `DATA_CLEAN_RAW_JSON=1` 应只输出原始 JSON，不能混入中文提示。

## 验收边界

不能把以下情况算作最终完成：

- 只跑通 `--dry-run`，但没有跑通过真实清洗。
- 只清洗一个测试 MCAP，却声称全部数据已完成清洗。
- 终端输出可读，但输出 MCAP 没有验证 topic 数或新增 topic。
- 使用测试 TCP/ArUco 参数，却没有明确提醒用户这些参数未真实标定。

