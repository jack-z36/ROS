# L2 通用头部模板

本模板是所有 L2 能力模块说明文件的统一开头。实际生成 L2 时，先填写本头部，再接入一个功能类别专用主体模板，最后接入 `L2_common_footer.md`。

````md
# L2 能力模块说明：<能力名称>

## 1. 能力名称

```text
<能力名称>
```

## 2. 所属位置

阶段：
L1：
场景：
模块类别：<数据定义类 / 数据读写类 / 数据计算类 / 流程编排类>
来源功能模块清单：

## 3. 一句话目标

```text
<只用一句话说明本能力解决什么问题>
```

## 4. 能力角色

说明本能力在当前 L1 / 场景中扮演什么角色。不要在这里展开字段、路径、算法或调度细节；这些内容放到类别主体模板中。

```text
<例如：定义运行上下文语义 / 管理 run 目录 / 判断配置是否可执行 / 编排单场景执行顺序>
```

## 5. 上游关系

说明本能力依赖哪些上游事实、能力或产物。这里写“关系”，不强制写成数据输入。

-

## 6. 下游关系

说明本能力会被哪些下游模块、任务或用户动作依赖。这里写“关系”，不强制写成数据输出。

-

## 7. 上游接口对齐检查

开发本能力前，必须先按 `DOCS/阶段二：数据清洗/约束文件/上游依赖接口对齐约束.md` 检查直接上游功能。

| 上游功能 | 上游接口 / 产物 | 本能力如何依赖 | 对齐状态 | 处理方式 |
|---|---|---|---|---|
|  |  |  | 已对齐 / 缺定义 / 有冲突 | 复用 / 调整本能力 / 新增上游接口任务 / 暂停确认 |

## 8. 职责边界

本能力负责：

1.
2.

本能力不负责：

1.
2.
3.
````
