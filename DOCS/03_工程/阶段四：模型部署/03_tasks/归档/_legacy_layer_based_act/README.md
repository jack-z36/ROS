# 旧 layer-based ACT L3 隔离区

本目录保存旧版 layer-based ACT L3、dispatch 和验收卡片。

这些文件已经从当前 active 路径移出，只允许作为人工历史参考或迁移素材。执行 agent、验收 agent 和循环工程主 Agent 不得把本目录作为当前任务来源。

当前 L3 必须重新生成到：

```text
03_tasks/task/active/<new-l2-id>/
03_tasks/task/dispatch/<new-l2-id>.yaml
03_tasks/cards/<new-l2-id>/
```
