# 执行记录目录统一

## 基本信息

- 时间：2026-05-15 18:00
- 归属 L1：阶段二公共工作流
- 归属场景：不限定单一 Service 场景
- 关联 L2：无
- 关联 L3：无

## 用户请求

用户提出：阶段二下的执行记录以及当前状态文件可以统一收敛到 `执行记录/` 文件夹下，文件名使用“月+日+小时”或微元任务名称命名。

## 执行内容

1. 新增 `DOCS/阶段二：数据清洗/执行记录/README.md`，定义阶段二文档执行记录和当前状态快照的统一落点。
2. 更新 `DOCS/阶段二：数据清洗/AGENTS.md`，加入执行记录层和执行记录入口。
3. 更新 `AGENTS.md`，在阶段二工作入口中加入 `DOCS/阶段二：数据清洗/执行记录`。
4. 更新 `DOCS/阶段二：数据清洗/文件存放规范.md`，区分 Runtime 程序运行记录与文档层执行记录。
5. 更新 `DOCS/工作流/阶段二开发范式.md`，要求 L3 执行完成后的执行记录和状态快照写入统一目录。
6. 更新 `DOCS/工作流/attachments/L3微元任务文件模板.md`，将“完成后交接”的更新位置改为统一执行记录目录。
7. 更新 `DOCS/阶段二：数据清洗/03_tasks/README.md`，将完成后的记录更新规则指向统一执行记录目录。

## 涉及文件

- `AGENTS.md`
- `DOCS/阶段二：数据清洗/AGENTS.md`
- `DOCS/阶段二：数据清洗/文件存放规范.md`
- `DOCS/阶段二：数据清洗/03_tasks/README.md`
- `DOCS/阶段二：数据清洗/执行记录/README.md`
- `DOCS/阶段二：数据清洗/执行记录/051518_执行记录目录统一.md`
- `DOCS/工作流/阶段二开发范式.md`
- `DOCS/工作流/attachments/L3微元任务文件模板.md`

## 观察结果

阶段二已有多个旧位置的 `执行记录.md` 和 `当前进度.md`。本次没有批量迁移旧文件，避免破坏既有索引和历史引用。

从本次规则建立后，阶段二新增长记录优先写入 `DOCS/阶段二：数据清洗/执行记录/`。

## 当前状态

统一目录已经建立，规则已经登记到阶段二入口、文件存放规范、L3 模板和阶段二开发范式中。

2026-05-15 18:10 追加调整：阶段二执行记录命名统一改为 `MMDDHH_<简短主题>.md`；如果记录来自明确 L3 微元任务，则使用 `MMDDHH_<L3微元任务名>.md`。执行过程和当前状态写入同一个文件。

## 未完成事项

旧的 `执行记录.md` 和 `当前进度.md` 尚未迁移，只作为历史索引保留。

## 下一步建议

如果后续确认要彻底迁移旧记录，应单独做一次“历史记录迁移任务”，先生成旧文件到新目录的映射表，再逐步移动并更新索引。
