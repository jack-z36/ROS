---
tags:
  - 附件
  - pi05
  - deploy
---

# 分散观测字段

> [!abstract]
> 各 topic 最近一次到达的局部观测，尚未组成策略输入

## 基本信息

| 属性 | 值 |
| --- | --- |
| 变量名 | `_images, _values, _stamps` |
| 参考系 | 不适用；这是程序数据流节点，不是几何坐标变换结果 |
| 相对原点 | 不适用 |
| 物理锚点 | 物理对象状态 / 中间态容器 |
| 阶段属性 | 中间态 |
| 是否最终输出 | 否 |
| 数据类型 | `dict[str, torch.Tensor] / dict[str, Any]` |
| 数据结构 | 多个字典分别保存图像 tensor、机器人状态数值和时间戳 |
| 所在文件 | `pi05_test/pi05/deploy/src/pi05/deploy/runtime/observation_collector.py:31-73` |
| 现实含义 | 各 topic 最近一次到达的局部观测，尚未组成策略输入 |

## 关键澄清

### 1. 它在哪个参考系下？

不适用。这里的“参考系”不是机器人空间坐标系，而是程序流水线中的数据阶段。

### 2. 它相对哪个原点？

不适用。它不是空间点或位姿变换结果。

### 3. 它对应哪个物理点 / 物理对象？

物理对象状态 / 中间态容器。如果它是缓存、请求或 batch，则无对应物理点，这是纯数学对象 / 程序容器。

### 4. 它是不是最终输出？

否。

### 5. 它不是什么？

它不应被误解为“整个推理程序的最终命令”。只有 ROS 控制命令输出才是下游执行系统真正收到的命令。

## 对应源码

```python
self._images[name] = image; self._values[key] = vector
```

## 一句话说清楚

> 各 topic 最近一次到达的局部观测，尚未组成策略输入

## 在数据流中的位置

- 上游：ROS观测输入通道
- 下游：完整策略观测快照
