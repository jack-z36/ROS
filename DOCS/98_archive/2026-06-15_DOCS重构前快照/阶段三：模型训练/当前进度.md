# 阶段三：模型训练当前进度

## 当前状态

阶段三尚未正式启动具体场景。

当前只建立阶段目录和阶段级粗粒度文档，用于承接后续训练任务。

## 已明确的方向

- 第一版先训练小尺寸开源 VLA。
- 第一版目标是跑通数据闭环，不追求最强模型效果。
- 训练数据应来自阶段二 canonical dataset 的 exporter。
- 训练标签应遵守 UMI 风格 relative trajectory action。
- 训练输出应能被阶段四部署链路解释为 candidate action。

## 尚未完成

- 尚未选定第一版 VLA 模型仓库。
- 尚未创建训练场景目录。
- 尚未实现 canonical dataset 到训练格式的 exporter。
- 尚未确定 observation history 长度、action horizon、图像预处理和姿态表示。
- 尚未完成任何训练运行、验证集评估或 checkpoint 管理。

## 下钻规则

阶段三当前没有具体场景。后续新增训练任务时，应在 `DOCS/阶段三：模型训练` 下创建场景目录，并补齐五个固定场景文档。
