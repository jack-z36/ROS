---
tags:
  - 附件
  - pi05
  - deploy
---

# 完整策略观测快照

> [!abstract]
> 模型一次推理所需的完整策略观测

## 基本信息

| 属性 | 值 |
| --- | --- |
| 变量名 | `ObservationSnapshot, snapshot` |
| 参考系 | 不适用；这是程序数据流节点，不是几何坐标变换结果 |
| 相对原点 | 不适用 |
| 物理锚点 | 物理对象状态 + 纯数学容器 |
| 阶段属性 | 推理前中间态 |
| 是否最终输出 | 否 |
| 数据类型 | `ObservationSnapshot dataclass` |
| 数据结构 | 图像映射 + BimanualState + 26D encoded_state + captured_at_s |
| 所在文件 | `pi05_test/pi05/deploy/src/pi05/deploy/runtime/shared_buffer.py:22-29; observation_collector.py:75-103` |
| 现实含义 | 模型一次推理所需的完整策略观测 |

## 关键澄清

### 1. 它在哪个参考系下？

不适用。这里的“参考系”不是机器人空间坐标系，而是程序流水线中的数据阶段。

### 2. 它相对哪个原点？

不适用。它不是空间点或位姿变换结果。

### 3. 它对应哪个物理点 / 物理对象？

物理对象状态 + 纯数学容器。如果它是缓存、请求或 batch，则无对应物理点，这是纯数学对象 / 程序容器。

### 4. 它是不是最终输出？

否。

### 5. 它不是什么？

它不应被误解为“整个推理程序的最终命令”。只有 ROS 控制命令输出才是下游执行系统真正收到的命令。

## 对应源码

```python
return ObservationSnapshot(images=images, state=state, encoded_state=encode_bimanual_state(state), captured_at_s=now)
```

## 一句话说清楚

> 模型一次推理所需的完整策略观测

## 在数据流中的位置

- 上游：分散观测字段
- 下游：最新完整观测缓存 / 推理请求消息
