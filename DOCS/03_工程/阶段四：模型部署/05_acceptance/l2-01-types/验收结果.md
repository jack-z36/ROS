# L2-01 Types 层重构验收结果

## 基本信息

| 字段 | 内容 |
|---|---|
| L2 | L2-01 Types 层重构 |
| L2 分支 | `model_deploy-l2-01-types` |
| 集成分支 | `model_deploy` |
| 验收目录 | `DOCS/03_工程/阶段四：模型部署/05_acceptance/l2-01-types/` |
| 最低验证层级 | unit |
| 真机风险 | none |

## 验收目标

确认 Types 层已经把 state/action 的单一真相源改成 TCP pose + gripper width 语义，并且所有向量维度、段序、拆分/拼装和错误处理都能被单测复现。验收通过后，下游 Config、Assembly、Publish 才能依赖 16D 契约继续改造。

## 前置条件

- 当前分支是 `model_deploy-l2-01-types`。
- deploy_001、deploy_002、deploy_003、deploy_004 已完成或处于待最终验收状态。
- 可维护测试已经放入 `src/model_deploy/pi05/` 下对应测试目录。
- 不需要 ROS、模型 bundle、机械臂、夹爪或真机环境。

## Required L3

| L3 | 状态 | 对应运行验收场景 | 证据 |
|---|---|---|---|
| deploy_001 | 待执行 | S1、S2 | 待补 |
| deploy_002 | 待执行 | S1、S2 | 待补 |
| deploy_003 | 待执行 | S3 | 待补 |
| deploy_004 | 待执行 | S1、S2、S3 | 待补 |

## 运行验收场景

| 场景 | 目的 | 验证层级 |
|---|---|---|
| S1 维度与段序 | 验证 `ACTION_DIM`、`STATE_DIM`、TCP pose、gripper width 的段序符合 TO-BE Contract | unit |
| S2 round-trip | 构造已知 16D 向量，split 后再 as_vector，确认还原一致 | unit |
| S3 非法输入 | 构造 14D、15D、17D、NaN 或错误 shape，确认被明确拒绝 | unit |

## 运行命令

在仓库根目录执行：

```bash
python3 -m pytest src/model_deploy/pi05/tests -k "action_spec or state_codec or action_codec or types"
```

如果 deploy_004 新增了更窄的 Types 层测试文件，优先执行更窄命令，并把实际命令登记到“实际执行记录”：

```bash
python3 -m pytest <Types层测试文件路径>
```

## 测试输入

- 一个可识别段序的 16D action 向量，例如左右 TCP pose 和 gripper width 使用不同数值段。
- 一个可识别段序的 16D state 向量。
- 错误长度向量：14D、15D、17D。
- 非法数值：NaN、Inf 或不符合 shape 的输入。

## 观察点

- pytest stdout 中所有 Types 层测试为 passed。
- 失败用例必须是预期异常，不得静默截断、填零或吞掉错误。
- round-trip 断言显示输入 16D 与输出 16D 数值一致。
- 测试日志保存到 `logs/` 时，文件名建议使用 `YYYYMMDD_l2-01_pytest.log`。

## 通过现象

- `ACTION_DIM == 16`，第一版不含触觉时 `STATE_DIM == 16`。
- `split_bimanual_action(...).as_vector()` 能还原构造的 16D action。
- state 编码输出维度为 16D，段序与文档一致。
- 错误维度和非法数值会导致明确失败，不会进入下游运行链路。
- 验收过程中没有启动 ROS 节点，也没有触发 dry-run、shadow-run、real-policy 或 real-robot。

## 失败现象与排查入口

- 维度仍为 14D 或 26D：优先检查 `action_spec.py` 和 `state_codec.py` 常量。
- round-trip 不一致：优先检查 split/as_vector 的段序。
- 非法 shape 未报错：优先检查 `action_codec.py` 和 state/action codec 的输入校验。
- 测试导入失败：优先检查包路径、测试路径和新增测试文件落点。

## 实际执行记录

| 项 | 内容 |
|---|---|
| 执行时间 | 待执行后填写 |
| 执行人 / Agent | 待执行后填写 |
| 实际命令 | 待执行后填写 |
| 结果 | 待执行后填写 |
| 日志链接 | 待补 |
| 脚本链接 | 如未新增验收脚本，写“不适用” |

## 未验证项

- 待执行后填写；没有未验证项时写“无”。

## L2 Gate 结论

- [ ] Required L3 全部完成。
- [ ] S1 维度与段序通过。
- [ ] S2 round-trip 通过。
- [ ] S3 非法输入通过。
- [ ] 当前代码路径均使用 `src/model_deploy/pi05/...`。
- [ ] 无未解释的失败项或跳过项。
- [ ] 允许进入 L2-02。
- [ ] 允许触发 L2 自动同步。

## 自动同步结果

```text
L2 分支提交：
L2 分支推送：
合入 model_deploy：
model_deploy 推送：
剩余工作区状态：
```
