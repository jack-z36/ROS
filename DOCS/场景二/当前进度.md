# 当前进度

## 已完成

- 已完成场景二目标描述文档：`/home/hit/ROS/DOCS/场景二/目标描述.md`。
- 已完成场景二背景信息文档：`/home/hit/ROS/DOCS/场景二/背景信息.md`。
- 已明确场景二当前阶段目标：Ubuntu 上位机同时运行两个 Baton Mini SDK 节点，发布 `/baton_mini_right/...` 与 `/baton_mini_left/...` 两组 ROS2 topic。
- 已记录两台 Baton Mini 的基础配置：右相机 `192.168.1.10` + `baton_mini_right`，左相机 `192.168.2.10` + `baton_mini_left`。
- 已校准快速里程计 topic 命名：当前统一使用 `fast_odom`。
- 已完成执行约束文档：`/home/hit/ROS/DOCS/场景二/执行约束.md`。
- 已新增一键启动入口：`/home/hit/ROS/start_all_sensor.sh`。
- 已新增总 launch：`/home/hit/ROS/launch/all_sensor_nodes.launch.py`。
- 已新增集中配置：`/home/hit/ROS/config/all_sensor_nodes.yaml`。
- 已完成静态验证和构建验证：Python 语法检查、`bash -n`、YAML 解析、`colcon build --packages-select baton_mini gopro_camera_launch`、三个 launch 的 `--show-args`。
- 已修复 `start_all_sensor.sh` 在 `set -u` 下 source ROS setup 时触发 `AMENT_TRACE_SETUP_FILES: 未绑定的变量` 的问题。
- 已将 Baton Mini 本机 `local_ip` 改为 `auto`，启动时根据当前 USB 网卡路由自动解析。
- 已将 GoPro 设备路径改为 `/dev/v4l/by-path/...video-index0`，避免 `/dev/video*` 编号因重插拔漂移。
- 已增强 `/home/hit/ROS/start_all_sensor.sh`：启动前检查硬件，启动后在终端打印 4 个节点的成功 / 警告 / 失败状态。
- 已短时验证一键启动：`STARTUP_WAIT=15 timeout --signal=INT --kill-after=5s 35s ./start_all_sensor.sh` 输出 `4/4 个节点全部通过检查`。
- 已将 topic 发布策略改为配置驱动：`config/all_sensor_nodes.yaml` 中每个 topic 都有 `enabled` 与 `name`。
- 当前默认只启用 4 个业务 topic：`/baton_mini_right/odometry`、`/baton_mini_left/odometry`、`/gopro_right/image_raw`、`/gopro_left/image_raw`。
- 已定位当前缺少 `/baton_mini_right/...` 的原因：右侧 `192.168.1.10` 可 ping，但 SDK 必需 TCP 端口 `8000`、`9994`、`9996`、`9997`、`9998` 均未打开，导致本机 `baton_mini_right` 进程 `login fail` 后退出。
- 已增强预检：Baton Mini 启动前会检查 TCP 端口；已增强启动失败处理：默认 `STOP_ON_FAILURE=1`，任一节点失败时停止已启动节点，避免半启动状态。
- 已将默认启动后等待时间调整为 `20` 秒，并修复连续 Ctrl+C 时重复 cleanup 的问题。
- 已在用户恢复 Baton Mini 设备端口后完成一次 4/4 真实启动验证：`/baton_mini_right`、`/baton_mini_left`、`/gopro_right/gopro_right_camera`、`/gopro_left/gopro_left_camera` 均在本机运行，且期望 topic 全部出现。
- 已确认 `/home/hit/ROS/src/gopro_camera_launch` 当前仍被总 launch 使用，负责隐藏封装 `v4l2_camera_node`、转发公开图像 topic、维护 GoPro 参数和单相机调试入口。
- 已完成新最小 topic 策略的真实硬件短时验证：4 个节点均通过，且每个节点只检查 1 个已启用 topic。
- 已实测新策略运行中普通可见 topic 只剩 `/baton_mini_left/odometry`、`/baton_mini_right/odometry`、`/gopro_left/image_raw`、`/gopro_right/image_raw` 加 ROS 基础 topic。
- 已确认短时验证结束后无残留相机进程，localhost 范围 topic 只剩 `/parameter_events` 与 `/rosout`。
- 已增强 `start_all_sensor.sh` 停止逻辑，停止时对总 launch 的进程组做 cleanup，降低子节点残留概率。
- 已新增残留清理脚本 `/home/hit/ROS/cleanup_ros_residue.sh`，可用于清理当前工作区 all-sensor launch、Baton Mini、GoPro V4L2、图像 relay、压力节点和 ROS daemon 残留。
- 已增强 `start_all_sensor.sh`：运行节点前默认先执行 `colcon build` 编译工作区全部 ROS 包，编译成功后重新 source `install/setup.bash` 再进入预检和启动。
- 已将左右 GoPro 配置统一为 30Hz：`config/all_sensor_nodes.yaml` 中 right/left `frame_rate: 30`，`gopro_camera.yaml` 中 `time_per_frame: 1/30`，GoPro launch 启动前执行 `v4l2-ctl --set-parm 30`。

## 正在处理

- 当前未保持相机节点常驻运行；需要采集时重新执行 `./start_all_sensor.sh`。
- 尚未验证 Octopus 是否能看到所有目标 topic。
- 普通 Wi-Fi ROS_DOMAIN 中仍可能看到远端 `/octopus`、`/picotele_*`、`/realsense/*`、`/xr/pico` 等 topic；脚本默认使用 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST` 做本机相机启动检查，避免远端 topic 干扰。

## 下一步

- 需要正式采集时重新运行 `./start_all_sensor.sh`，并在另一个终端执行 `ros2 daemon stop` 后，再执行 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST ros2 topic list --no-daemon`。
- 如果怀疑上次启动后仍有残留进程，先执行 `./cleanup_ros_residue.sh --dry-run` 查看匹配范围，确认后执行 `./cleanup_ros_residue.sh` 清理。
- `./start_all_sensor.sh` 现在默认会先编译全部 ROS 包；需要跳过编译时可使用 `AUTO_BUILD=0 ./start_all_sensor.sh`，需要只编部分包时可设置 `BUILD_PACKAGES="包名1 包名2"`。
- 若 MCAP 中发现 right/left GoPro 频率不一致，先停止当前相机节点，再重新执行 `./start_all_sensor.sh`；运行中采集卡被占用时，`v4l2-ctl --set-parm 30` 会返回 `Device or resource busy`，不会热更新当前运行实例。
- 实际验证网络连通、topic 发布和 Octopus / ros2 bag 录制。
