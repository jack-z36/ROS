# 阶段二：数据清洗当前进度

## 当前状态

阶段二是当前主推进阶段。

场景一正在推进中，程序一和程序二已完成，程序三仍在开发。场景二到场景五尚未开始编码实现。

## 场景一进度

场景一已有完整文档和部分实现：

- 程序一：数据清洗程序，已完成。
- 程序二：夹爪配置自动生成程序，已完成。
- 程序三：坐标系配置生成程序，正在执行。

已形成的能力：

- `./start_data_clean.sh` 已作为统一入口。
- 清洗程序能读取 raw MCAP，检查 topic，并输出清洗后的 MCAP。
- 夹爪配置自动生成程序已支持实时 GoPro 标定中心。
- 左右 Baton Mini 位姿转换参数已拆分为两个独立配置文件。
- 浏览器版实时标定中心已可用。

## 场景二到场景五进度

尚未开始编码。文档已按新的五场景结构重新设计，待场景一完成后逐步推进。

## 尚未完成

- 场景一程序三坐标系配置生成尚未完成。
- 场景二：尚未开始滤波器、异常值检测、IK 求解器和 MuJoCo 仿真验证的开发。
- 场景三：尚未开始时间轴对齐程序的开发。
- 场景四：尚未开始 canonical dataset 构建程序的开发。
- 场景五：模型和数据格式尚未确定，导出器暂无法开发。
- canonical dataset 的 schema 文件、step_index、masks 等尚未正式落盘。

## 下钻规则

本文件只记录阶段级进度。需要查看具体场景的程序、命令、配置文件和已验证项时，应进入对应场景目录读取五个固定文档和 `输出程序与文件.md`。
