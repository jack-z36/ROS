# 阶段二：数据清洗阶段产出

## 文档定位

本文件记录阶段二在任务执行过程中已经形成或依赖的软件资产。

阶段二最终要生成什么数据集，写在 `阶段目标描述.md` 中；本文件只记录软件层面的产出：程序/脚本有什么功能，输入输出是什么，以及在哪里查看更详细的软件说明。

## 软件资产总览

| 软件资产 | 主要路径 | 功能 | 详细信息入口 |
| --- | --- | --- | --- |
| 数据清洗启动入口 | `/home/hit/ROS/start_data_clean.sh` | 面向用户的统一入口，设置 conda Python、`PYTHONPATH` 和默认配置，并调用清洗 launcher 或标定流程。 | 阶段二场景一 |
| 默认清洗配置 | `/home/hit/ROS/config/data_clean_smoke_test.yaml` | 声明输入/输出目录、文件 glob、并行数、位姿流、左右 transform 文件和夹爪宽度流。 | 阶段二场景一 |
| 正式标定配置 | `/home/hit/ROS/config/data_clean_calibrated.yaml` | 由标定向导生成的正式配置。 | 阶段二场景一 |
| 左手位姿转换配置 | `/home/hit/ROS/config/data_clean_left_transform.yaml` | 保存左手 Baton Mini 专用 `start_from_common`。 | 阶段二场景一 |
| 右手位姿转换配置 | `/home/hit/ROS/config/data_clean_right_transform.yaml` | 保存右手 Baton Mini 专用 `start_from_common`。 | 阶段二场景一 |
| data_clean 源码包 | `/home/hit/ROS/src/data_clean` | 离线 MCAP 清洗和标定工具的源码根目录。 | `data_clean_architecture.md` |
| 浏览器版实时标定中心 | `/home/hit/ROS/src/data_clean/core/mcap_calibration_wizard.py` | 通过本地浏览器页面完成夹爪宽度实时标定和 common frame 位姿标定。 | 阶段二场景一 |
| GoPro-only 启动入口 | `/home/hit/ROS/start_gopro_only.sh` | 只启动左右 GoPro 节点，供实时标定中心使用。 | 阶段二场景一 |

## 各场景预期软件产出

| 场景 | 预期软件产出 | 状态 |
| --- | --- | --- |
| 场景一 | 夹爪宽度提取模块、位姿转换模块、标定向导 | 部分完成 |
| 场景二 | 滤波器模块、异常值检测器、数据补全器、IK 求解器、MuJoCo 仿真验证器 | 未开始 |
| 场景三 | topic 对齐器、时间轴生成器、alignment report 生成器 | 未开始 |
| 场景四 | dataset 初始化器、episode builder、action chunk 生成器、schema/mask/report writer | 未开始 |
| 场景五 | 格式导出器、导出验证器 | 未开始（模型待定） |

## 详细信息查找规则

- 想了解阶段二软件总体架构，读 `/home/hit/ROS/src/data_clean/data_clean_architecture.md`。
- 想了解场景一的完整软件清单，读 `DOCS/阶段二：数据清洗/场景一/输出程序与文件.md`。
- 想了解配置边界和运行约束，读对应场景的 `执行约束.md`。
