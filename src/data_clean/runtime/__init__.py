"""Pipeline orchestration — batch processing and CLI entry points."""

# Imports are intentionally disabled due to broken relative paths in dependent modules.
# This file exists to mark the runtime package.
