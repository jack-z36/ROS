# L2-06 验收结果：deploy_055 L2 Gate 跨模块集成与验收脚本

## 1. 验收结论

**PASS_LOCAL**

执行 Agent 一次性完成 l2-06 L3 任务，未触碰任何 production source。所有 12 个 Gate 场景（G01–G12）在 local scope 下通过，baseline 0 FAIL，3 个外部 scope 正确记为 BLOCKED（不允许伪造 PASS）。

## 2. 交付物清单

| 路径 | 类型 | 说明 |
|---|---|---|
| `src/model_deploy/act/tests/integration/test_l2_06_gate.py` | integration test | 43 个 pytest case，覆盖 G01–G12 |
| `src/model_deploy/act/tests/fixtures/l2_06_fake.yaml` | typed fake fixture | bundle 留空以验证 fail-fast；不携带 command_output.enabled |
| `src/model_deploy/act/scripts/l2_06_verify.sh` | verify 脚本 | 唯一验证入口；scope/policy/case 分发；PASS/FAIL/BLOCKED 标签 + summary + exit code |
| `DOCS/03_工程/阶段四：模型部署/05_acceptance/l2-06-control-loop/验收结果.md` | 本文件 | L3 验收结果 |
| `DOCS/03_工程/阶段四：模型部署/05_acceptance/l2-06-control-loop/L2验收报告.md` | L2 报告 | L2 Gate 整体报告 |
| `DOCS/03_工程/阶段四：模型部署/05_acceptance/l2-06-control-loop/logs/deploy_055_verify_local.log` | 脚本证据 | local scope 完整输出 |

## 3. 验证命令与结果

| 命令 | 结果 |
|---|---|
| `PYTHONPATH=src python3 -m pytest src/model_deploy/act/tests/integration/test_l2_06_gate.py -v` | **43 passed in 1.15s** |
| `PYTHONPATH=src python3 -m pytest src/model_deploy/act/tests -q` | **867 passed, 4 skipped**（skip 仅匹配 §2 allowlist 外部原因） |
| `bash src/model_deploy/act/scripts/l2_06_verify.sh --scope local --policy fake --config src/model_deploy/act/tests/fixtures/l2_06_fake.yaml` | **exit 0**（13 PASS / 0 FAIL / 3 BLOCKED） |

## 4. Gate 场景覆盖

| 标签 | 测试类 | 含义 | 状态 |
|---|---|---|---|
| G01 | `TestG01Types` | types 纯净 + 无 forbidden token/import，静态边界扫描 | PASS |
| G02 | `TestG02Config` | default-off、YAML 不能静默启用、arg 仅 2 旗、canonical 资源/spec 身份、loader fail-fast | PASS |
| G03 | `TestG03Seam` | pipeline 干跑、collector→snapshot、service 预测、safety PASS、publisher provenance | PASS |
| G04 | `TestG04ChannelMetrics` | request 校验、result XOR、LatestQueue 容量 1 close、metrics 不可变快照 | PASS |
| G05 | `TestG05Worker` | serial success、error 恢复、CLOCK_INVALID 致命闸口 | PASS |
| G06 | `TestG06Scheduling` | correlation/active chunk、prefetch@horizon、result-id 错配 latch fault | PASS |
| G07 | `TestG07FallbackOutput` | 六 outcome OBSERVED/PUBLISHED/BLOCKED/REJECTED/FAILED/PARTIAL + 观测缺失 fallback | PASS |
| G08 | `TestG08Lifecycle` | preflight pass、SPEC_IDENTITY_MISMATCH、PERMIT_SOURCE_MISSING、deny fail-closed、shutdown 收敛 | PASS |
| G09 | `TestG09FullGate` | 完整 tracer bullet（真实 worker）：chunk_activated>0、inference_success>0、published>=1、有界 shutdown | PASS |
| G10 | `TestG10RosDryRun` | dry-run command=0 契约 + ROS 可用标志自检 | PASS（local-contract）；`BLOCKED_ENV`（真实 ROS） |
| G11 | `TestG11RealPolicyDryRun` | 真实 loader 在空 bundle 下 fail-fast | PASS（local-contract）；`BLOCKED_ARTIFACT`（真实 bundle/GPU） |
| G12 | `TestG12RealCommand` | 默认 fail-closed：deny permit、YAML 无命令开关、未授权 command=0 | PASS（local-contract）；`BLOCKED_HARDWARE_EXPECTED`（真机） |

## 5. 修改范围审计

- 允许修改（任务 §10）：
  - `src/model_deploy/act/tests/integration/test_l2_06_gate.py`（修改 `FakePolicy.predict_action_chunk` 的策略域四元数从 `[0,0,0,1]` 改为 `[-1,-1,-1,1]`，以匹配 `ActionStateNormalizer` 的 min-max 反归一化语义）
  - `src/model_deploy/act/scripts/l2_06_verify.sh`（新增 rclpy 部分安装探测：若 `rclpy` importable 但 `rclpy.node` 不可用，则隔离 ROS site-packages，使 test 文件可在 no-ROS 上下文下被 pytest 收集）
  - `DOCS/03_工程/阶段四：模型部署/03_tasks/task/active/l2-06-control-loop/deploy_055_L2Gate跨模块集成与验收脚本.md`（§15 成功标准勾选 + §18 执行总结）
  - `DOCS/03_工程/阶段四：模型部署/05_acceptance/l2-06-control-loop/`（验收结果 + L2 报告 + 日志）
- 未修改 production source：
  - `src/model_deploy/act/{types,config,repo,service,runtime,ui}/*.py` 全部只读

## 6. 未验证项（unverified）

- 真实 ROS 2 graph 端到端（G10）：本环境 `rclpy` 部分安装（无 `rclpy.node`），记 `BLOCKED_ENV`。local-contract 已证明 dry-run command=0 契约。
- 真实 model bundle + GPU（G11）：`load_act_runtime_resources` 在空 bundle 下 fail-fast 已证明；真实 PASS 需外部 artifact，记 `BLOCKED_ARTIFACT`。
- 真实 command 路径 / driver / E-stop / permit topology（G12）：默认 deny permit + `command_output_enabled=False` 已证明 fail-closed；真实 PASS 需硬件授权，记 `BLOCKED_HARDWARE_EXPECTED`。
- 生产 `rclpy` `act_deploy_node` 启动路径：仅在 `B11 main` 内使用，本 L3 不调用 `main()`（避免误起 ROS 进程）；仅通过 `_act_init` 顺序的等价 FakeNode 路径证明。

## 7. 下一步

- 提交 `deploy_055_验收卡片.md`（PASS_LOCAL）→ 主 Agent 归档本 L3 到 `03_tasks/completed/l2-06-control-loop/`，并启动 L2 整体 Gate 验收。
- L2-06 agent_context/HTML 已在 `03a/00_INDEX` 中标注为 v2 source-aligned；如需进一步 HTML 投影更新，按 `00_INDEX` 的 "HTML-MD 语义对齐表" 单独切任务。
