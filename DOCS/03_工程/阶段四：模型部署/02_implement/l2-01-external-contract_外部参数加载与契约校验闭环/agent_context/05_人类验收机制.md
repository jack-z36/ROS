# L2-01 人类验收机制

## 1. 人类验收目标

让人类确认：L2-01 只提供外部参数加载与契约校验；第一版没有独立平滑处理，也没有 smoothstep / blend / RTC 类配置入口。

## 2. 人类验收清单

| 验收项 | 运行目录 | 命令 | 测试输入 | 观察点 | 通过现象 | 失败现象 |
|---|---|---|---|---|---|---|
| 查看设计包结构 | 仓库根目录 | `find DOCS/03_工程/阶段四：模型部署/02_implement/l2-01-external-contract_外部参数加载与契约校验闭环 -maxdepth 2 -type f | sort` | L2-01 设计包 | 文件列表 | 只有 HTML + `agent_context/` 固定 MD | 根目录散落 Agent MD 或六层目录 |
| 检查语义对齐 | 仓库根目录 | `python3 skills/stage4-l2-designer/scripts/validate_l2_design_package.py DOCS/03_工程/阶段四：模型部署/02_implement/l2-01-external-contract_外部参数加载与契约校验闭环` | 设计包 | stdout | PASS | 报 HTML/MD 不对齐 |
| 检查平滑已去除 | 仓库根目录 | `rg -n 'smoothstep|blend_steps|cross_chunk|rtc_alignment|action_smoothing' DOCS/03_工程/阶段四：模型部署/02_implement/l2-01-external-contract_外部参数加载与契约校验闭环` | 设计包 | rg 输出 | 仅出现在“禁止/不负责/去除”语境 | 出现作为可配置能力 |
| 浏览人类 HTML | 文件浏览器 | 打开 `L2架构交互可视化.html` | HTML | 页面视图 | 能看到“去除平滑处理”和权威 MD 来源 | HTML 与 MD 语义冲突 |

## 3. 签字位置

后续 L2-01 Gate 通过后，人类验收结论写入：

```text
DOCS/03_工程/阶段四：模型部署/05_acceptance/l2-01-external-contract/验收结果.md
```

签字格式：

```markdown
## 人类验收

- 验收人：<用户名>
- 验收日期：YYYY-MM-DD
- 验收结论：[ ] 已通过  [ ] 不通过
- 逐项结果：
  - 验收项 1：<命令> -> <通过现象> -> [ ] 通过
- 备注：
```

## 4. 真机风险

不适用。本 L2 不订阅 topic、不发布 topic、不发送硬件命令。
