# 通过 SSH 传输文件到远程电脑

## 前提条件
- 远程电脑 IP：192.168.102.152
- 远程电脑已开启 SSH 服务
- 你知道远程电脑的用户名和密码

## 方法一：使用 scp 命令（推荐）

### 基本格式
```powershell
scp "本地文件路径" 用户名@192.168.102.152:远程目标路径
```

### 具体示例
```powershell
# 传输到远程电脑的用户主目录
scp "E:\20260701_15.zip" your_username@192.168.102.152:~/

# 传输到远程电脑的指定目录
scp "E:\20260701_15.zip" your_username@192.168.102.152:/home/your_username/datasets/

# 如果远程电脑使用非标准 SSH 端口（如 2222）
scp -P 2222 "E:\20260701_15.zip" your_username@192.168.102.152:~/
```

### 操作步骤
1. 打开 PowerShell 或 CMD
2. 将 `your_username` 替换为远程电脑的实际用户名
3. 执行命令，输入密码
4. 等待传输完成

## 方法二：使用 sftp 命令（交互式）

```powershell
# 连接到远程电脑
sftp your_username@192.168.102.152

# 在 sftp 交互界面中
sftp> put "E:\20260701_15.zip" ~/20260701_15.zip
sftp> exit
```

## 方法三：使用 PowerShell 的 Copy-Item（Windows 10+）

```powershell
# 需要先安装 OpenSSH 客户端
Copy-Item -Path "E:\20260701_15.zip" -Destination "your_username@192.168.102.152:~/"
```

## 验证传输成功

传输完成后，在远程电脑上验证：
```bash
# SSH 登录远程电脑
ssh your_username@192.168.102.152

# 检查文件是否存在
ls -lh ~/20260701_15.zip

# 检查文件大小是否一致
du -h ~/20260701_15.zip
```

## 常见问题

### 1. 连接被拒绝
- 检查远程电脑 SSH 服务是否运行
- 检查防火墙是否允许 SSH 连接

### 2. 权限错误
- 确保远程目录有写入权限
- 尝试传输到 `/tmp/` 目录

### 3. 传输速度慢
- 使用 `-C` 参数启用压缩：`scp -C "E:\20260701_15.zip" ...`
- 检查网络连接质量

### 4. 断点续传
- 对于大文件，可以使用 `rsync`：
```bash
rsync -avz --progress "E:\20260701_15.zip" your_username@192.168.102.152:~/
```

## 完整操作流程

1. **确认远程电脑信息**
   - IP: 192.168.102.152
   - 用户名: ________（需要你填写）
   - 密码: ________（需要你输入）

2. **执行传输命令**
   ```powershell
   scp "E:\20260701_15.zip" 你的用户名@192.168.102.152:~/
   ```

3. **输入密码**
   - 输入远程电脑的密码（不会显示）

4. **等待完成**
   - 根据文件大小和网络速度，可能需要几分钟到几十分钟

5. **验证结果**
   - 登录远程电脑检查文件

现在告诉我远程电脑的用户名，我可以帮你生成具体的命令。