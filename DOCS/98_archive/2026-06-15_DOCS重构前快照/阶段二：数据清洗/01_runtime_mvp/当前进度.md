# Runtime MVP 当前进度

## 当前状态

Runtime MVP 已建立文档 Harness，尚未开始代码实现。

`03_tasks/` 已规划为 Runtime MVP 和后续阶段二任务的执行记录入口。本轮只搭建文档和任务制度，不修改代码。

## 已完成

- 明确 Runtime MVP 的目标和边界。
- 建立六件套文档。
- 明确 Runtime 不承载真实业务算法。
- 明确 Runtime MVP 先跑通空流程调度能力，真实业务由 Service 后续逐场景接入。
- 明确未来开发者入口方向为 `./start_data_clean.sh --dev`。
- 任务记录机制已调整为 `03_tasks/task/active/<功能组>/`；Runtime MVP 现有 L3 按功能组迁移到 `task/active/runtime-g1`、`task/active/runtime-g2`、`task/active/runtime-g3`、`task/active/runtime-g5`。
- 已完成 Runtime MVP 功能十 `Runtime smoke test 模块` 的 L2 能力说明和 `RuntimeSmokeTestCase`、`RuntimeSmokeTestSuite`、`RuntimeSmokeTestResult` 三个原子数据定义。

## 未完成

- 尚未实现场景选择。
- 尚未实现配置读取和输入检查。
- 尚未实现 run 目录创建。
- 尚未实现 fake service。
- 尚未实现 run log、processing manifest 和 error summary。
- 尚未跑通 fake pipeline smoke test。
- 尚未实现 `./start_data_clean.sh --dev`。
- 尚未创建 Runtime/Types/Config/Repo 代码文件。

## 下次优先事项

下一步从 `03_tasks/task/active/<功能组>/` 中选择对应 L3 执行；后续用 fake service 跑通最小 Runtime 闭环，再接入真实 service 场景。
