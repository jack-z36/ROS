# 状态机与写入规则

## 消费 Agent

- 主 Agent

## 本文职责

本文只作为状态相关行为文件的路由入口。

## 不负责

本文不承载状态恢复、L3 状态固化或循环快照写入的规则正文。

## 必读行为文件

| 行为 | 必读文件 |
|---|---|
| 状态恢复 | `behaviors/01_状态恢复行为.md` |
| L3 状态固化 | `behaviors/10_L3状态固化行为.md` |
| 循环快照写入 | `behaviors/14_循环快照写入行为.md` |

## 状态枚举入口

状态枚举由 `behaviors/10_L3状态固化行为.md` 和 `DOCS/03_工程/阶段四：模型部署/00_status/INDEX.md` 共同约束；不得在本文维护第二份状态规则。

