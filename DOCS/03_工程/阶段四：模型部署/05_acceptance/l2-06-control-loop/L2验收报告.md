# L2-06 整体验收报告：ControlLoop 中央运行调度闭环

## 1. L2 验收结论

**PASS_LOCAL（条件：所有外部 scope 保持 BLOCKED 状态，硬件/真机结论需人类验收授权）**

L2-06 跨模块集成验证入口已建立并通过。所有 10 个 L3（deploy_051–054, 055, 056–060）的 production source 与 agent_context/HTML 设计投影一致；唯一验证脚本 `l2_06_verify.sh` 在 local scope 下 13 PASS / 0 FAIL / 3 BLOCKED（外部），baseline 867 passed / 4 skipped（仅外部 allowlist 原因）。

## 2. L2 边界（参考 01_L2功能边界.md）

L2-06 以 ROS control timer 为时钟驱动，在不等待模型推理的前提下，拥有：
- 推理 worker/queue、request/result 关联、active/pending chunk、cursor、fallback
- permit/publish 结果归约、运行指标、进程生命周期
- 只通过 L2-02~05 的真实公共接口协作

上游 L2：L2-01（DeployConfig/canonical resources）、L2-02（ObservationBuffer/Snapshot）、L2-03（ActInferenceService.predict_action_chunk）、L2-04（SafetyGuard.filter_action）
下游 L2：L2-05（ActionPublisher.publish）—— L2-06 自身只额外输出 runtime metrics

## 3. 验证层级与结果

| 层级 | 运行内容 | 实际运行 | 结果 |
|---|---|---|---|
| import/unit/local integration | C1–C26、真实 contracts、A1–A5、G01–G09 | `test_l2_06_gate.py` 43 case | 43/43 PASS |
| mock integration | 真实 L2-02~05 + fake policy/FakeNode/clock | `TestG03Seam` + `TestG09FullGate` | PASS |
| local composition | A5 + fake external providers | `TestG08Lifecycle` + `TestG09FullGate` | PASS |
| production entry | B11 + injected resource-loader test seam | `TestG02Config.test_loader_fails_fast_on_empty_bundle` + `TestG12RealCommand.test_cli_parser_has_no_yaml_command_switch` | PASS（fail-fast + 仅 CLI） |
| ROS dry-run | 真实 ROS graph、fake policy、command disabled | 真实 rclpy 部分安装（无 `rclpy.node`） | `BLOCKED_ENV` |
| real-policy dry-run | real bundle/GPU、command disabled | 无真实 artifact | `BLOCKED_ARTIFACT` |
| real command | driver/permit/E-stop/授权 | 默认 fail-closed | `BLOCKED_HARDWARE_EXPECTED` |

## 4. L3 完成度

| L3 | 名称 | 验收状态 |
|---|---|---|
| deploy_051 | 推理通道与运行指标基础 | PASS_LOCAL（见 logs/deploy_051_acceptance_round_1.md） |
| deploy_052 | InferenceWorker 串行推理轴 | PASS_LOCAL（见 logs/deploy_052_acceptance_round_1.md） |
| deploy_053 | ControlLoop 中央状态机 | PASS_LOCAL（见 logs/deploy_053_acceptance_round_1.md） |
| deploy_054 | ActDeployNode 进程入口与原子启动 | PASS_LOCAL（见 logs/deploy_054_acceptance_round_1.md） |
| deploy_055 | L2 Gate 跨模块集成与验收脚本 | **PASS_LOCAL（本次）** |
| deploy_056 | L2-01 启动资源 + PolicyInputSpec 冻结构造 | PASS_LOCAL（见 logs/deploy_056_acceptance_round_1.md） |
| deploy_057 | L2-02 观测装配门面 | PASS_LOCAL（见 logs/deploy_057_acceptance_round_1.md） |
| deploy_058 | L2-03 canonical spec 消费 | PASS_LOCAL（见 logs/deploy_058_acceptance_round_1.md） |
| deploy_059 | L2-04 safety 调用接缝 | PASS_LOCAL（见 logs/deploy_059_acceptance_round_1.md） |
| deploy_060 | L2-05 publish provenance | PASS_LOCAL（见 logs/deploy_060_acceptance_round_1.md） |

## 5. 六层产物落点（与 ACT 落点约束一致）

| 层 | 落点 | 状态 |
|---|---|---|
| types | `src/model_deploy/act/types/` | 无新增 L2-06 源码（仅 consumer） |
| config | `src/model_deploy/act/config/` | 无新增 L2-06 源码（仅 consumer） |
| repo | `src/model_deploy/act/repo/act_runtime_resources.py` | 显式注入 canonical fake（不调生产 loader） |
| service | `src/model_deploy/act/service/{act_inference,safety_guard}.py` | 仅调用 public seam |
| runtime | `src/model_deploy/act/runtime/{inference_channel,runtime_metrics,inference_worker,control_loop}.py` | A1–A4 实现 |
| ui | `src/model_deploy/act/ui/{act_deploy_node,observation_pipeline,action_publisher}.py` | A5 + B9–B12 + C20–C21 |

## 6. 外部 BLOCKED 条件

| 条件 | 证据 | 标签 |
|---|---|---|
| 真实 ROS 2 graph 端到端 | `rclpy` importable but `rclpy.node` 不可用（部分安装） | `BLOCKED_ENV` |
| 真实 model bundle + GPU | `load_act_runtime_resources` 在空 bundle 下 fail-fast；无 artifact | `BLOCKED_ARTIFACT` |
| 真实 command 路径 / driver / E-stop / permit topology | 无 hardware authorization | `BLOCKED_HARDWARE_EXPECTED` |

## 7. 人类验收入口

按 `05_人类验收机制.md` 分项签字；真机独立授权。`/act/metrics` 字段已固定为唯一可观察 runtime 信号（schema_version=1, l2_id="l2-06-control-loop"），无 `/act/command/status` 写权（L2-05 专属）。

## 8. 仍需确认

- 真实 `CommandPermitSource` 的 gate/deadman/E-stop/driver-ready topic 或 driver API
- `safe_stop` 是否需要主动硬件 stop；如需要，必须新增明确 driver port
- gripper 的真实 ROS message type、单位和 topic

这些问题不阻塞 local Gate，但阻塞端到端 L3、shadow-run 和真机结论。
