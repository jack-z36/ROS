# 当前进度

## 当前状态

- 场景三第一阶段已经从“4 个相机节点”推进到“6 路目标 topic 数据源可一键启动”。
- 已新增场景三输出程序与文件清单：`DOCS/03_工程/阶段一：数据采集/场景三/输出程序与文件.md`。
- 场景三第二阶段目标已经补充为“8 路目标 topic 数据源可一键启动”，即在原 6 路基础上新增 2 路触觉传感器 topic。
- 2026-04-27 17:09 本轮用户再次执行场景三验证时，`start_all_sensor.sh` 启动前检查失败；当前系统只枚举出左手两个触觉串口，右手两个触觉串口对应 by-path 不存在。
- 2026-04-27 17:16 已确认右手两个 CH340 曾在 16:30 左右从 USB 总线断开，之后未重新枚举；当前问题不是 ROS topic 或 launch 问题，而是 USB 设备层缺少 2 个 CH340。
- 2026-04-27 17:29 当前 ROS graph 中 8 个目标 topic 均已可见，postlaunch 检查结果为 `5/5 个节点全部通过检查`；Octopus 录制器已修复 GoPro Best Effort QoS 兼容问题，但需要重启 Octopus 后生效。
- 2026-04-28 17:16 已根据最新 MCAP 文件确认 8 个目标 topic 均成功写入；两路 GoPro 在该 MCAP 中接近 60Hz，说明当前问题不是录制器只收到 10fps，而是录制时显示链路和额外测速订阅者被高帧率图像录制、Zstandard 压缩和写盘压力拖慢。
- 2026-04-28 17:28 已确认当前 `v4l_camera_node` 不会从 YAML 消费 `time_per_frame` 或 `publish_rate` 来限帧；真正有效的是 V4L2 层 `v4l2-ctl --set-parm=30`。GoPro launch 已改为启动相机节点前先设置采集卡帧率。
- 2026-04-28 17:40 已修复左右 GoPro launch 参数串扰问题；短测日志 `log/start_all_sensor/20260428_173834.log` 确认 right/left 分别使用独立 USB 设备，并且 `/gopro_right/image_raw` 与 `/gopro_left/image_raw` 均通过 postlaunch 检查。
- 当前 `start_all_sensor.sh` 已能通过一个总 launch 启动：
  - 2 个 Baton Mini 节点；
  - 2 个 GoPro 采集链路；
  - 1 个 `pressure_driver_node`，发布 4 路触觉 topic。
- 当前第一阶段 6 个目标 topic 已在实机短测中全部通过 postlaunch 检查：
  - `/baton_mini_right/odometry`
  - `/baton_mini_left/odometry`
  - `/gopro_right/image_raw`
  - `/gopro_left/image_raw`
  - `/pressure/left_hand/gripper_1`
  - `/pressure/left_hand/gripper_2`
- 第二阶段新增的 2 个触觉目标 topic 为：
  - `/pressure/right_hand/gripper_1`
  - `/pressure/right_hand/gripper_2`
- 第二阶段 8 个目标 topic 已在 `start_all_sensor.sh` 实机短测中通过 postlaunch 检查。
- Octopus 独立启动入口已创建为 `start_octopus.sh`。
- Octopus 的 `scanner.json` 已配置为录制场景三第二阶段 8 个目标 topic，默认 MCAP 输出目录为 `mcap`。

## 已完成

- 已阅读并遵守场景三预读文档要求。
- 已新增 `launch/all_sensor_nodes.launch.py`，恢复 `start_all_sensor.sh` 依赖的总 launch 文件。
- 已将触觉驱动纳入 `config/all_sensor_nodes.yaml`。
- 已扩展 `scripts/all_sensor_status.py`，支持触觉配置、串口和 topic 检查。
- 已修复 `start_all_sensor.sh` 中 `fio` 拼写错误。
- 已创建 `start_octopus.sh`。
- 已创建 `scripts/configure_octopus_scanner.py`。
- 已修复 Octopus `recording.mcap` 配置读取问题。
- 已在 Octopus Operation 面板显示可勾选的录制 topic 列表。
- 已为 Octopus MCAP 录制器补充以下消息类型 schema：
  - `nav_msgs/msg/Odometry`
  - `hwk_pressure_interfaces/msg/PressureFrame`
- 已重新构建成功：
  - `hwk_pressure_interfaces`
  - `hwk_pressure_driver`
  - `octopus`
- 已完成一次 `start_all_sensor.sh` 实机短测：
  - 日志：`log/start_all_sensor/20260427_150026.log`
  - 结果：`5/5 个节点全部通过检查`
  - 触觉侧：两个串口均收到 `First valid pressure frame received`
- 已完成 Octopus 短时启动验证：
  - 能启动 GUI 程序；
  - 能写入并读取场景三 `scanner.json` 配置；
  - 短测后已清理残留进程。
- 已按用户最新要求更新场景三文档体系，将第二阶段目标从 6 路扩展为 8 路。
- 已将 `config/all_sensor_nodes.yaml` 的触觉目标 topic 扩展为 4 路。
- 已将 `src/data_collection/hwk_pressure_driver/config/pressure_sensors.yaml` 的触觉串口配置扩展为左手 2 路 + 右手 2 路。
- 已将 `scripts/configure_octopus_scanner.py` 和 Octopus 空配置默认录制 topic 扩展为 8 个目标 topic。
- 已将 `/home/hit/.config/scanner.json` 写入 8 个目标 topic。
- 已重新构建 `octopus`，使安装二进制同步包含 8 个默认录制 topic。
- 已根据当前硬件枚举结果，将 GoPro right/left 的 `video_device` 更新为当前可用的两个 by-path。
- 已将当前新增 CH340 串口 `/dev/ttyUSB2` 的 by-path 接入 `right_gripper_1_port`。
- 已将第四个 CH340 串口 `/dev/ttyUSB3` 的 by-path 接入 `right_gripper_2_port`。
- 已完成一次 `start_all_sensor.sh` 8 路实机短测：
  - 日志：`log/start_all_sensor/20260427_161444.log`
  - 结果：`5/5 个节点全部通过检查`
  - 触觉侧：4 个串口均收到 `First valid pressure frame received`
- 已新增 `scripts/hwk_pressure_usb_recover.sh`：
  - 普通运行：诊断 CH340 数量、expected by-path、udev 规则状态和近期内核事件；
  - `--install-udev`：安装仓库中的 4 路触觉 udev 规则；
  - `--reset-right-hubs`：reset 右手两个触觉串口曾经所在的父级 USB hub。
- 已修复 Octopus MCAP 录制器 QoS 选择：
  - GoPro `/gopro_right/image_raw` 与 `/gopro_left/image_raw` 的 publisher 是 Best Effort；
  - 录制器现在会根据 publisher QoS 自动选择兼容的订阅 QoS；
  - 已重新构建 `octopus` 成功。
- 已修复 MCAP 录制器回调中 `topic` 引用捕获导致的潜在悬空引用问题。
- 已对 `mcap/2026-04-28_09:08:14.204096036.mcap` 执行 `ros2 bag info`：
  - 文件大小 `917.2 MiB`，duration `13.604631762s`，总消息数 `5353`；
  - 8 个目标 topic 均有消息；
  - `/gopro_left/image_raw` 约 `60.05Hz`，`/gopro_right/image_raw` 约 `59.91Hz`。
- 已将 `src/data_collection/gopro_camera_launch/config/gopro_camera.yaml` 的 GoPro 请求帧率从 `[1, 60]` 改回 `[1, 30]`，并移除当前 `v4l2_camera` 版本不支持的 `publish_rate` 配置项。
- 已执行 `colcon build --packages-select gopro_camera_launch`，并确认安装目录中的 GoPro 配置已同步为 `time_per_frame.denominator: 30`。
- 已实测 `v4l2-ctl --set-parm=30` 能将 `/dev/video4` 和 `/dev/video6` 设置为 `30.000 fps`。
- 已修改 GoPro launch：新增 `frame_rate` 参数，默认 `30`，启动 `v4l2_camera_node` 前先执行 `v4l2-ctl -d <video_device> --set-parm <frame_rate>`。
- 已在 `config/all_sensor_nodes.yaml` 中为左右 GoPro 显式配置 `frame_rate: 30`，并由 `launch/all_sensor_nodes.launch.py` 透传。
- 已重新构建 `gopro_camera_launch`，并确认安装目录中的 launch 已包含前置 `v4l2-ctl --set-parm` 动作。
- 已执行 GoPro 相关 launch / 脚本 Python 语法检查，通过。
- 当前 `/dev/video4` 与 `/dev/video6` 均已由 `v4l2-ctl --set-parm=30` 设置为 `30.000 fps`。
- 已执行 `python3 scripts/all_sensor_status.py preflight --config config/all_sensor_nodes.yaml`，启动前检查通过。
- 已修复 `all_sensor_nodes.launch.py` 中两路 GoPro include 参数作用域串扰，避免 right GoPro 被启动成 left 的 namespace/device。
- 已将 `gopro_pose_record.launch.py` 改为 `OpaqueFunction` 提前解析 launch 参数，保证延迟启动的 `v4l2_camera_node` 使用本实例的确定参数。
- 已完成一次 30fps GoPro 源头短测，日志为 `log/start_all_sensor/20260428_173834.log`。
- 本次短测结果为 `5/5 个节点全部通过检查`。
- 本次短测中 GoPro right 为 `/gopro_right/gopro_right_camera`，`/gopro_right/image_raw` 通过。
- 本次短测中 GoPro left 为 `/gopro_left/gopro_left_camera`，`/gopro_left/image_raw` 通过。

## 正在处理

- 当前正在处理录制时 FPS 稳定性：ROS 源头短测已经确认两路 GoPro 以独立设备启动且 30fps 设置生效；下一步需要重启 Octopus GUI，观察 Right GoPro 画面是否恢复，并确认录制时 UI FPS 是否还会掉到约 10fps。

## 尚未完成

- 尚未在 30fps 配置下重新执行 Octopus GUI Start / Stop 录制。
- 尚未确认新的 `.mcap` 中两路 GoPro 消息数是否约为 30Hz。
- 尚未确认 30fps 配置下 Octopus UI 显示 FPS 是否仍会在录制时掉到约 10fps。
- 尚未在 Octopus GUI 中人工确认 Right GoPro 面板恢复实时画面。

## 当前风险

- 当前右手两个触觉串口依赖 USB 拓扑下的 by-path；如果 USB 插口、集线器层级或设备枚举状态变化，需要重新核对 `/dev/serial/by-path` 并同步更新配置。
- `/etc/udev/rules.d/99-hwk-pressure.rules` 当前仍是旧版，只包含左手两条规则；仓库 `config/99-hwk-pressure.rules` 已包含右手两条规则，需要用 `sudo scripts/hwk_pressure_usb_recover.sh --install-udev` 安装。
- Octopus GUI 短测可以启动，但 `timeout` 发送 SIGTERM 后没有立刻退出，本轮已用手动 kill 清理。正式采集建议通过 GUI 正常关闭或确认进程退出。
- Octopus 显示链路仍主要是原有 Realsense / Inspire 专用 UI，场景三当前关键进展集中在录制链路：Operation 面板 topic 选择 + MCAP 写入。
- 当前 Octopus 默认 MCAP 压缩为 Zstandard；如果 30fps 后 UI 仍明显卡顿，需要继续验证压缩 CPU 开销，可临时将 `/home/hit/.config/scanner.json` 的 `recording.mcap.compression` 从 `2` 改为 `0` 做对照测试。
- 16:14 那次 8 路通过依赖当时 USB 拓扑下的 4 个稳定 by-path；本轮只枚举出左手两个串口，说明当前插线/拓扑状态已经变化。

## 下一步

如果后续右手两个触觉串口再次消失，先运行：

```bash
scripts/hwk_pressure_usb_recover.sh
```

如果右手两个 CH340 仍未出现，可以先安装最新 udev 规则并尝试 reset 右手父级 USB hub：

```bash
sudo scripts/hwk_pressure_usb_recover.sh --install-udev --reset-right-hubs
```

当前下一步是重启传感器和 Octopus：

```bash
cd .
./start_all_sensor.sh
```

```bash
cd .
./start_octopus.sh
```

重新点击 Start / Stop 后，执行：

```bash
ros2 bag info mcap/<生成的文件名>.mcap
```
验收重点是确认 8 个目标 topic 都存在且消息数大于 0，并确认 `/gopro_right/image_raw` 和 `/gopro_left/image_raw` 的平均频率约为 30Hz。
